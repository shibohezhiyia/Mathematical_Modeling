# 跨域类比映射

`validate_analogy_mapping` 用于把流体、生态、交通等领域的结构类比转成可审计搜索提议。映射必须列出：

- 源变量与目标变量、两侧单位和角色；
- 交互关系；
- 守恒或平衡陈述；
- 边界条件与显式假设。

返回值带稳定指纹和 `execution_authorized: false`。即使用户确认，类比也只能进入候选搜索；目标题目的类型、量纲、数据、约束、反例和数值验证仍必须独立通过。缺少映射字段时系统拒绝把领域方程直接套到新题。
