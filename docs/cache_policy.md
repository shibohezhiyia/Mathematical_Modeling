# 缓存复用政策

增量编译缓存只有在节点带有明确的等价证明证书时才允许用 `equivalence_key` 去重。`classify_cache_failure` 将类型契约错误与超时、网络、资源不足、预算耗尽和未完成反例搜索分开，后者不能永久缓存为数学结论。`validate_cache_hit` 在来源、版本、适用域、数据视图和预处理指纹不完整或不匹配时拒绝结论复用；`warm_start_gate` 要求结构未变、有参数映射并保留冷启动对照。
