# Optional backend discovery

`discover_optional_backends` reports whether SciPy, SymPy, CVXPY, Pyomo and
JAX are installed without importing or executing them. Availability is not
authorization: a backend still needs a typed contract, resource plan and
result evidence before it can run.
