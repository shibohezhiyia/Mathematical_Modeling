# 数值后端策略提示

`select_numerical_strategy` 接收显式的条件数、刚性比、事件数和梯度验证状态，输出带证据状态的策略提示：刚性优先刚性求解器，含事件优先事件求解器，病态优先条件数稳健策略；未经梯度交叉验证时不启用导数加速。

这只是后端路由提示，不是刚性、条件数或梯度正确性的证明。后端仍必须执行自己的输入、资源、残差和独立差分检查；缺失诊断会保持 `not_assessed`。
