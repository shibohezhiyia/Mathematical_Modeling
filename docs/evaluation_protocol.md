# 验证集与动力学评价协议

`ValidationTrialLedger` 记录验证集每次使用，冻结最终测试指纹，并拒绝在最终测试后继续适配。最终测试只能用于锁定方案后的评价。

`assess_dynamics_channels` 强制把导数一致性、积分一致性、利用历史的条件预测和只用初值/外部输入的独立轨迹分开记录。某一通道通过不能覆盖另一通道失败或缺失。

`validate_multi_table_manifest` 要求联表关系显式记录实体键、时间键、联接基数、单位绑定和字段来源。高相关性本身不会获得联表权限。
