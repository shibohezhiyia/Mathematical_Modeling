# 反例引导修复（CEGIS 初版）

`core.cegis_repair` 将执行器返回的有限反例转换为结构化修复偏好：

- 输出越界/边界约束反例优先尝试 `minimum`/`maximum`；
- 数值定义域反例优先尝试受控的 `abs`/`sqrt` 等原语；
- 偏差或交互缺失信号可优先尝试加法/乘法。

这些只是有来源的候选顺序，不是自动修复。未知反例原因会原样保留但不发明新算子；每条指令都要求重新通过 IR、量纲、资源、反例重放和留出验证。图搜索报告会同时保存原始反例和 `repair_directives`，因此可以审计“为什么尝试这个变异”。
