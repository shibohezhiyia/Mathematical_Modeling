# 全局敏感性筛查

`assess_global_sensitivity` 使用固定种子的有限参数设计和列置换总效应，筛出会显著改变标量输出的参数。它适合：

- 决定下一轮结构搜索或数据采集优先级；
- 发现输出几乎不随参数变化的退化/不可辨识候选；
- 给出方向一致性，辅助判断是否需要非线性或分段机制。

它不是因果效应、后验概率、Sobol 全局定理或全局可辨识性证明。输出只对记录的参数边界、随机种子和有限探针负责；非有限值、预算不足和零输出方差都会返回 `not_assessed`/`no_output_variation`，不以零影响替代缺失证据。
