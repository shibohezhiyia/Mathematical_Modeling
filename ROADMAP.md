# 开放世界数学建模编译器路线图

目标：把当前“题型注册表驱动”升级为“最小数学原语 + 动态结构合成 + 受限执行 + 反例修正”的开界建模系统。注册表只作为加速缓存，不再决定系统能力边界。

> 所有完成比例必须由冻结、可复算且与开发隔离的基准验证。公开标签、自建真值和结构/来源留出均可提供相应范围内的证据；没有基准证据前，不声明“覆盖 70%–90%”。

本文是实施待办，不代表功能已经实现；未通过对应验收的任务保持未勾选。优先完成“评价可信 → 事实与假设分离 → 受控搜索与修复”的最小闭环，再扩充昂贵后端。

## 当前缺口与实施入口（2026-09-10 复核）

后续推进以本节和 [未完成能力实施方案](docs/implementation_backlog.md) 为入口。详细方案包含代码接入点、实施步骤、进一步优化、依赖顺序及验收条件。下文历史记录中的 `[x]` 只表示当时的局部交付，不表示完整能力已经验收；日期日志与本节冲突时，先按本节定位再复核代码。

本次仅更新实施计划，以下任务尚未因此完成。

| 编号 | 未完成任务 | 当前事实 | 依赖与验收入口 |
| --- | --- | --- | --- |
| R0 | 修正能力声明与特征处理误判 | 前四列截取已替换；现为最多四个单变量关联特征，尚不能覆盖纯交互信号 | 继续补组合筛选；见方案 R0 |
| R1 | 候选生成→绑定→编译→执行→判决 | 主研究候选仍为提议；另有可运行的显式标量图实验 | R0、R7 的受控通道；见 R1 |
| R2 | LLM-SR、PDE、神经 UDE、GNN 数值后端 | 一维规则 PDE、有限神经 UDE 和轻量 GNN 已有受限执行与负例；LLM-SR 仅类型化树/候选池执行，完整多后端竞赛仍未完成 | R1、R7；四项分别验收，见 R2 |
| R3 | 根据数学结构选择模型组合 | 诊断驱动槽位已使结构模型可达；尚未用消融证明其独立收益 | R0、R5；见 R3 |
| R4 | ODE、优化、多表及 PDE 的反例修复 | 已有通用控制器及小型图闭环，全模型族尚未贯通 | R1、相应 R2 后端；见 R4 |
| R5 | 自动判定、留出泛化与消融证据 | 执行回归 32/32；13 个原始题意结构组中当前受限自动建模 13/13、简单工具基线 8/13 | 扩为独立冻结确认集并做真实版本/资源隔离实测；见 R5 |
| R6 | 四层不确定性与独立校准 | 已有分解/校准接口，尚未完成统一实际校准 | R1、R4 与独立数据；见 R6 |
| R7 | 全执行入口的 OS 隔离和资源治理 | Windows Job Object 已有实现；文件/网络仍需更强隔离和平台实测 | 先建设现有受控通道，逐步覆盖 R1–R4；见 R7 |
| R8 | 网页滑块驱动重新求解和新判决 | 有图实验、快照、取消与展示组件，尚未覆盖主研究所有后端 | R1、R4、R6、R7；见 R8 |
| R9 | 模块生命周期与实际调用审计 | 多个曾被列为“闲置”的模块已有 Web/引擎入口 | 可先做；见 R9 |

- [ ] R0：修正误判和过度声明，完成训练段隔离与合法信号保留回归。
- [ ] R1：主入口完成有界动态组合执行并保留每个候选的证据链。
- [ ] R2：四类高级后端分别提供真实训练/求解、负例和独立测试结果。
- [ ] R3：结构候选能在完整模型库环境下进入组合，收益与费用均可复核。
- [ ] R4：每个已支持模型族完成反例重放、重编译、重求解和冻结确认。
- [ ] R5：完成预登记比较、独立评分、配对统计及消融；按证据类型报告结论。
- [ ] R6：达到预登记的独立校准标准，保留不可辨识与不可校准情况。
- [ ] R7：按平台和执行后端完成隔离、资源限制、取消及失败回收验收。
- [ ] R8：改参数→重算→复核→更新判决完成端到端测试。
- [ ] R9：模块状态、调用入口、界面可用性和文档保持一致。

实施顺序：R0/R9 与 R7 现有通道审计 → R1 最小端到端闭环 → R3/R4 → R2 按失败需求逐项加入 → R6/R8 → R5 最终冻结验收。R5 的取样、量规和预算设计从一开始准备，最终测试数据始终不参与开发反馈。高级数学总项不因一个替代筛查器上线而勾选。

## 下一轮优先待办：主流程候选执行闭环（2026-09-12）

本节是 R1/R3/R4/R5/R7/R8 与 E1–E5 的优先落地切片，不是新增一套平行架构。
已有模块优先复用；模块存在或单测通过不代表主流程已接通，以下事项均保持待办，完成后附端到端证据。
本轮首要目标：题面与数据 → 候选及角色绑定 → 受控执行 → 验证反馈 → 结构修复 → 独立确认 → 判决。

### C01：统一受控执行入口与状态（最高优先级；对应 R1/R7/R8）

- [x] 将主研究候选、动态编译 API 和候选执行 API 接入同一任务提交与结果读取接口，复用现有队列、快照、worker 和配额；重计算不占用 HTTP 请求直至完成。
- [ ] 为每个实际后端建立执行适配器与能力声明；统一传递取消、墙钟、内存、输出、临时磁盘和评估次数预算。缺少受控适配器时明确拒绝或标记不支持，不能回退到无监督执行。
- [x] 分开记录执行状态与验证状态：排队/运行/完成/失败/取消/超时，不等同于待验证/通过/反证/不可判定；移除外层固定 `executed` 覆盖内部失败或未评估状态的行为。
- [ ] 验收：从主入口产生的合法候选真实进入 worker；慢任务、超时、取消、内部失败、回收异常均有端到端测试；状态查询保持可响应，旧快照和取消任务不能提交新结论。

接入点：[动态分发](core/dynamic_model_compiler.py)、[主研究](core/modeling_assistant.py)、
[Web 入口](web/app.py)、[执行监督](core/solver_runtime.py)。先完成这一项，再扩大默认自动搜索范围。

当前增量（2026-09-12）：新增 `core/process_execution.py` 与 `core/task_store.py`，动态编译接口支持 `async=true`
并通过 `POST /api/research/dynamic-compile`、`GET /api/research/dynamic-compile/status/<task_id>`
和 `POST /api/research/dynamic-compile/cancel/<task_id>` 使用 spawn 进程、SQLite 持久任务账本、任务生命周期和会话所有权。
运行中的动态 worker 超时或取消会调用 `terminate`/`kill`，而不是只标记线程状态。
动态编译外层状态不再覆盖内部 `not_assessed`/失败状态；新增 Web/服务回归已通过。
该切片已移除 HTTP 请求同步等待，并覆盖动态编译的 primitive graph、ODE、优化、多表和 external method 后端。
动态编译的进程级硬终止与跨重启状态已实现；主研究 `/api/research/run` 与模型训练生产路径均使用 spawn worker，
共享 SQLite WAL 账本执行原子并发准入，取消/超时执行 terminate/kill。测试模式仍保留线程替身以隔离单元测试。

历史增量（已被后续进程化切片 superseded）：`TaskStore` 为主研究异步任务增加 SQLite 生命周期记录，服务重启后旧的
`queued/running/cancelling` 记录标记为 `interrupted`；动态编译使用 `ProcessExecutionService`，
在 spawn worker 中执行全部五类动态合同，超时和取消会 terminate/kill，并将结果和错误持久化。
该实现已通过进程生命周期、超时、取消、Web 异步和动态编译回归。主研究的计算函数仍在旧线程中运行，
因此“主研究全后端硬终止”和跨多进程共享任务调度仍不得勾选，需后续将其改为可序列化的进程 worker。

主研究进程化增量（2026-09-12）：新增 `core/research_process.py`，生产环境的异步
`/api/research/run` 将数据快照、题面契约和运行参数序列化后交给 spawn worker；
同一 `ProcessExecutionService` 负责动态编译与主研究的排队、状态、超时和 terminate/kill，
研究状态查询/取消接口已接入该任务账本。测试模式仍使用原有替身线程，便于 Web 单测隔离。
主研究进程化已覆盖主助手调用的全部当前后端；任务账本目录可通过 `MATHMODEL_TASK_STORE_PATH`
配置，让本机多进程共用 SQLite WAL。同步接口和训练模型接口也已迁移到同一进程服务；服务重启时仅回收超过租约的旧任务，
新鲜任务不会被误标记。多主机租约/心跳仍不在本地部署承诺范围内。

训练进程化增量（2026-09-12）：新增 `core/training_process.py`，生产环境模型训练（单目标与多目标）
通过同一 spawn 服务执行，结果写入受控 joblib 工件，完成后由父进程加载；`POST /api/model/cancel`
支持硬取消。该路径不改变测试模式线程替身，也不把本地进程监督宣传为容器或内核级隔离。

Linux 验收增量（2026-09-12）：新增 `scripts/verify_linux_sandbox.sh`，在真实 Docker/Podman
环境中构建固定摘要镜像、生成 v2 attestation、保存 inspect/version 证据并运行 OS 沙箱测试。
新增 `.github/workflows/linux-sandbox.yml`，可在 GitHub 托管 Ubuntu Runner 上手动或推送触发，
无需开发者本机安装 Linux；运行日志和 14 天工件可供独立复核。

### C02：自动角色、单位与数据来源绑定（依赖 C01；对应 R1/P1/P7）

- [ ] 将题面事实、候选变量和数据画像联结为可审计绑定，记录来源片段/表列、变量角色、单位换算、样本粒度及适用条件；无数据题使用明确题面参数和边界，不生成伪观测。
- [ ] 明确证据充分时自动绑定；有歧义时保留候选解释并优先询问一项最影响求解的问题，提供两三个清晰假设。未知单位不能自动当作已确认无量纲。
- [x] 替换“前四个数值特征”的固定截取：主研究图搜索在独立选择分区计算 Spearman 目标关联和标准化成对交互增益，以列名作单位尺度不敏感的平局键；原始方差只记录。选择、图搜索执行、最终留出三块行级分区互斥。当前四列上限和 32 列成对筛查上限仍是资源契约，更高阶交互尚未解决。
- [ ] 验收：列重排/重命名、单位换算、重复实体、多表键冲突及无数据任务都有正反例；歧义不静默猜测；最终测试数据变化不影响绑定或训练特征选择。

接入点：[候选结构](core/structure_candidates.py)、[语义编译](core/semantic_model_compiler.py)、
[数据图桥](core/data_graph_bridge.py)、[主研究](core/modeling_assistant.py)。

### C03：诊断驱动的结构修复与再搜索（依赖 C01/C02；对应 R4/P3/P5）

- [ ] 统一残差、约束违反、边界失效、参数不稳定和资源失败的诊断输出，将证据映射为有界 IR 修改；大残差不直接判定存在隐变量，超时不直接判定数学模型错误。
- [ ] 外层改变结构、内层由数值优化器拟合参数；每次修改重新检查类型/单位、重新编译执行，并重放此前反例。模型不得修改事实、硬约束、判定阈值或评分器。
- [ ] 把代数图、ODE、优化、多表各自支持的修复路径接回主流程；明确每类可修改节点和预算，保留候选谱系、失败原因及停止条件。
- [ ] 验收：用冻结的失败案例验证结构修改确实发生且可执行；反例不能丢失，全体候选不合格或预算耗尽时不强选可信答案；最终确认数据不回流给生成器。

接入点：复用 [CEGIS 控制器](core/cegis_controller.py)、[ODE 修复](core/ode_cegis.py)
及现有模型族适配器，避免再建立一个只返回建议的修复接口。

### C04：分级筛选、增量计算与预算调度（随 C01 建立，接入 C03；对应 E1–E5/R3）

- [ ] 候选先过静态检查和低成本开发集筛选，再晋级精算与独立确认；按剩余墙钟、内存和 API 预算调度，记录剪枝原因，不缩减最终验证要求。
- [ ] 在安全范围内复用数据快照、特征视图、图编译及公共中间量；缓存键包含图结构、参数、单位、数据/切分版本、后端版本与数值精度，隔离会话和不同证据用途。
- [ ] 参数或图节点变更只重算受影响的下游节点；大数组先估算广播/关联规模再分配，采用分块、背压和有界并发，保留用户可取消性。
- [ ] 验收：缓存开关、冷/热运行和不同并发度下的结果在预登记容差内一致；最终验证不得误用开发缓存；报告各阶段耗时、峰值内存、缓存命中、API 次数及费用，不预先承诺加速倍数。

### C05：扩展可组合的 ODE 机制表达（依赖 C01/C03；对应 R2/R4/P2/P8）

- [ ] 将当前常数/线性/平方候选基底扩展为带类型、单位和定义域检查的跨状态耦合项（如 `x_i*x_j`）、显式时间/外部驱动及分段事件；不是新增题目专用求解器。
- [x] 完成第一步受限机制切片：`ode_cegis` 增加 `cross` 两两状态乘积和 `time` 显式时间项，保留最多三状态、有限 RHS 评估与结构变异预算；新增耦合、受迫、混合基底和标量拒绝回归。该切片不等于外部驱动数据、事件重置或刚性 ODE 已完成。
- [x] 受限动力学增量：增加同网格 `driver:<name>`、优先级/次数预算阈值事件、等步长 method-of-steps 时滞、`RK45/RK23/DOP853/Radau/BDF/LSODA` 选择和显式系数单位传播；事件在采样端点记录 post-reset 状态，时滞与事件组合明确拒绝。新增 15 项 ODE 回归和动态模型族竞争回归；这些是可执行切片，不是任意时滞系统、刚性证明或完整事件网络。
- [x] 为当前受限耦合、驱动、事件和时滞定义维度语义、参数单位、事件方向/边界/重置规则；驱动缺测、时滞历史缺失和非均匀网格明确拒绝，不默认为零或读取未来观测。连续事件面、状态相关重置和驱动外推仍待扩展。
- [ ] 将扩展后的结构纳入同一候选竞争、受控积分与反例修复；按诊断选择合适积分方法，保留容差收敛、约束及扰动稳定性检查。
- [ ] 验收：覆盖可解释的耦合系统、受迫系统和事件切换系统；核对独立参考轨迹或解析性质，同时加入量纲错误、发散、事件遗漏和非辨识负例。不将这些切片宣称为任意 ODE/PDE 自动发现。

接入点：[ODE 候选基底](core/ode_cegis.py)、[数学原语](core/math_primitives.py)、
[原语图运行时](core/primitive_graph_runtime.py) 及现有受控动力学执行器。

### C06：完整主流程对照与消融（协议立即准备；最终执行依赖 C01–C05；对应 R5/P12）

- [ ] 冻结升级前的完整研究流程作为基线，保存源码、提示词、模型版本、数据快照、随机种子和各项预算；对照必须运行两版完整流程，不能用 Ridge 替代旧版系统。
- [ ] 建立有数据/无数据、代数/ODE/优化/多表的评测分层和独立参考判定；开发案例与最终确认案例隔离，失败、超时、拒答保留在分母中；无法评分的题单独报告，不伪造标签。
- [ ] 在相同任务和计算/API 预算下比较有效解率、错误通过率、首次有据结果耗时、总耗时、峰值内存及费用；预登记实际改善门槛、风险容忍度、样本设计和统计方法。
- [ ] 分别消融自动绑定、结构修复、分级调度和计算复用；报告配对效果与置信区间，区分“证据不足”和“没有改进”，禁止用测试总数或公开数据结果宣称真实未见题准确率。
- [ ] 验收：只有达到预登记的效果与风险门槛才扩大自动执行默认范围；没有足够证据时保留实验开关、任务范围和已知退化案例。

接入点：[现有对照模块](core/system_benchmark.py) 及已有冻结、风险门与评分工具。
现有 `Ridge → ModelingEngine` 对照仍保留为预测管线证据，不能改名冒充完整主流程版本对照。

实施顺序：**C01 → C02 → C03 → C05**；C04 随执行链逐步接入；C06 的协议先冻结，最终比较最后执行。
每项完成需同时留下代码接入、正反例测试与作用范围记录，再更新本节及关联 R/P/E 条目。

## 总体完成标准

- [ ] 新题无需增加题目名称、领域对象或专用求解器分支。
- [ ] 问题存在合理分歧时生成结构不同的候选并保留竞争过程；允许条件唯一确定、候选不足或全体候选不合格，不强凑数量。
- [ ] 所有可执行模型都通过类型、单位、来源、资源和安全检查。
- [x] 求解失败能够归因到语义、结构、参数、数据、数值或资源层。`failure_diagnosis` 为每类失败保留独立 `status/evidence/action`，并明确未评估与“残差不是隐变量证明”；solver result contract 不会用缺失残差或资源字段补造结论。
- [x] 自动修复只能修改候选 IR，不能绕过校验或直接伪造结果。`GraphPatchGenerator`、`repair_router` 和 `cegis_repair` 均将模型/API 输出限制为非执行 patch/原语优先级，硬约束、判定器和事实不可变，候选必须重新过完整验证。
- [ ] 输出题意、结构、参数和数值四类不确定性及其校准证据。
- [ ] 结论必须附带适用边界、反例记录、残差和约束证书。
- [x] 成功组合只有通过复现测试后才能进入经验库。`ExperienceStore.promote_verified` 强制复现、反例和安全测试证据，并要求验证作用域。
- [x] 区分类型合法、执行成功、经验检验未发现反例和获得数学证书，不能相互替代。IR/solver/verdict/export 状态机分别保留这些等级，不因执行成功自动批准。
- [x] 搜索器不得更改题面硬约束、原始观测、最终测试集或判定阈值来使候选通过。冻结实验、图 patch、留出登记和判决门均拒绝这些字段的修改。
- [x] 无数据题仍可组合原语和验证数学性质；缺少可辨识信息时保留条件结论，不自行生成“观测真值”。纯机理入口使用 `mechanistic_no_dataset`，未知机制和待验证义务保持未决。

## 近期执行清单：先跑通可信闭环

以下条目是 P0–P12 的优先切片，不要求先完整实现所有阶段。编号用于跟踪依赖，完成时附上测试或基准证据。

| 编号 | 待办 | 依赖 | 验收重点 |
| --- | --- | --- | --- |
| N01 | 已完成：当前动力方程模块的预处理与选变量泄漏修复 | 无 | 先切分，再仅以训练段拟合选列、因果填充和标准化；改变测试数据不能改变训练产物 |
| N02 | 已完成：当前动力方程模块的训练、选参与最终测试分离 | N01 | 拟合接口只接收训练/选参数组；分别报告观测积分一致性与联合状态独立轨迹 |
| N03 | 已完成初版：独立事实抽取与假设提议入口 | 无 | 默认关闭；新机制只进入提议结果，不改变既有数学规范、数值结果与结论证据 |
| N04 | 已完成初版：四个核心对象及未知机制接口 | N03 | 类型、形状、已知量纲、版本引用、证据作用域及请求预算可校验；尚未接通数值编译 |
| N05 | 已完成资源监督初版；权限隔离待完成 | N04 的执行接口 | 通用 ODE/NLP 接入强制时限、OS 内存限制及核心取消接口；不接受任意代码，不宣称文件/网络隔离 |
| N06 | 已完成初版：开发段诊断与只读修复分流；其他模型族继续扩展 | N01、N04 | 数值、观测、结构、参数、语义和资源失败分别生成有证据的诊断，不凭大残差判定隐变量 |
| N07 | 已完成标量图开发与冻结留出切片；多后端、API 闭环待完成 | N02、N05、N06 | 定向修改 IR；旧反例不丢失；预算耗尽不能返回未验证候选作为可信结论 |
| N08 | 已实现候选内部中间量复用；跨图缓存与分级晋级待完成 | N04、N07 | 少算无效候选，但最终检查不减项；缓存开关不改变验收规则 |
| N09 | 已接入留出审计、同预算单实验对照和单模型网页实验；统计基准与完整网页判决待完成 | N02、N07、N08 | 同时报告有效解、错误接受、首次有据结果耗时、总耗时、费用和峰值内存 |

当前代码接入点：

- 事实抽取保持在 [semantic_model_compiler.py](core/semantic_model_compiler.py) 的严格来源边界内，另设假设提议接口，不直接放松事实校验。
- 优先修复 [modeling_assistant.py](core/modeling_assistant.py) 的 `_run_integral_equation_discovery`：全量选变量、双向插值、全量尺度估计以及留出集兼作候选选择的问题。
- 沿用 [mechanistic_modeling.py](core/mechanistic_modeling.py) 的 `_SafeNumericExpression`，补齐规模与运行边界，不退回通用 `eval`。
- 将 [four_layer_modeling.py](core/four_layer_modeling.py) 中的建议性时间预算接入强制执行监督；不能只增加一个 `timeout` 字段就宣称已隔离。

这些是定向静态检查得到的接入点，不代表全项目安全审计已经完成。

### N01–N02 交付记录

- [x] 将动力学发现拆到 [integral_dynamics.py](core/integral_dynamics.py)：固定时间轴前 60% 训练、中 20% 选参、末 20% 测试；缺失目标不移动边界，选列和尺度只拟合训练段。
- [x] 拟合接口不接收最终测试数组；独立轨迹接口仅接收时间、测试前初值与已选方程，不读取未来状态。所有所选状态均拟合方程，不能借用未来驱动变量冒充独立预测。
- [x] 分开显示积分一致性与独立轨迹，同步网页、图表和报告；超时、数值失败或缺失初值不能被积分高分覆盖。
- [x] 新增 [隔离与反例测试](tests/test_integral_dynamics.py)，覆盖测试数据变更、缺失值、跨边界填充、独立预测失败、预算与时间限制；更新 [研究链路回归](tests/test_modeling_assistant.py)。

验证记录（2026-09-05）：运行 `test_integral_dynamics`、`test_modeling_assistant`、`test_mathematical_reasoning`、`test_research_web_api` 和 `test_transparency_e2e`，共 85 项通过、1 项因本机测试数据缺失而跳过；`node --check web/static/js/app.js` 与 `git diff --check` 通过。这是相关模块回归，不是完整项目或依赖版本矩阵认证；当前测试环境 SciPy 1.17.1 高于仓库声明的 `<1.16.0` 上限，声明版本范围内的复验仍需另行完成。

范围：上述完成状态仅针对当前积分动力学通道。全项目验证审计、跨运行/跨研究的最终测试集冻结、操作系统级隔离和开放式结构搜索仍未完成；本通道的求解回调限额不能替代 P4 的进程沙箱。

### N03–N04 初版交付记录

- [x] 新增 [核心对象与类型检查](core/model_hypotheses.py)：不可变事实快照、类型化假设图、未知机制、版本/作用域绑定的证据记录和服务端请求预算。
- [x] 新增 [假设提议器](core/hypothesis_generator.py)，复用已有本地/API 适配接口，但不放松原有事实抽取校验；生成 JSON Schema，拒绝模型自授验证或执行权限。
- [x] 网页新增默认关闭、独立启用的候选提议选项；研究结果、报告与版本化产物标为未求解假设，不进入既有数值证据链。
- [x] 覆盖事实不可变、类型/量纲/依赖错误、越权字段、未知机制、证据失效、并发预算、响应脱敏以及与原研究结果隔离的测试。

使用方法、数据格式和边界见 [候选机制提议说明](docs/hypothesis_proposals.md)。当前只输入题面文字，默认最多一次请求、3 个候选、每个 64 个节点；允许零候选。测试采用可控后端，不代表真实模型生成质量已达标。开放式数值编译、观测数据绑定和 CEGIS 仍待 N05–N07；这不是 P1–P5 全阶段完成。

验证记录（2026-09-05）：候选机制、语义编译、机理建模、研究助手、积分动力学、数学论证及研究 Web API 的相关回归共 147 项通过；JavaScript 语法、文档本地链接和差异空白检查通过。没有发起真实外部模型调用，未声称完成生成质量基准、全项目测试或依赖版本矩阵复验。

### N05 资源监督初版交付记录

- [x] 新增 [进程监督](core/solver_runtime.py)、[操作系统资源限制](core/solver_process_limits.py) 和 [受控 worker](core/solver_worker.py)，仅开放可信通用 ODE/NLP 数学契约；父进程强制时限、计次回调、有限 JSON 通道、最小环境和临时目录回收。
- [x] Windows Job Object 提交内存与进程数量限制实测；Linux 提供地址空间限制分支但尚未在目标环境复验。限制无法建立时拒绝执行，不回退到主进程。
- [x] 加固 AST 长度/节点/深度、函数参数个数与有限实数检查；单位尺度溢出/下溢按未通过校验处理，不使静态检查异常中断。
- [x] 原有两类已验证数学契约默认接入监督；保留原数值算法及独立审计，失败不产生数值结论，也不升级为数学反例。
- [x] 网页和报告区分强制时限与软预算，显示中文错误原因及处理建议；上游失败时不使用占位数值执行下游，其他成功节点保留。
- [ ] 文件/网络权限隔离、磁盘配额、全后端覆盖、网页取消与跨服务进程资源调度；开放式假设图的执行授权与 CEGIS 也仍未完成。

边界与使用方法见 [受控数值执行](docs/solver_runtime.md)。N05 仅交付资源监督切片，不代表 P4 安全沙箱或 M3 全部完成，也没有声称该改动带来提速。

验证记录（2026-09-05）：资源监督、候选假设、语义编译、机理建模、通用求解器、研究助手、积分动力学、数学论证与研究 Web API 共 220 项相关回归通过；Windows 下实测内存限制、超时、取消、异常输出、限制挂接失败与进程/临时目录回收。JavaScript 语法、文档链接及差异空白检查通过。测试不涉及真实模型 API；未做全项目、Linux 或声明依赖范围内的认证，本机 SciPy 版本不匹配的问题仍保留。

### N06 初版交付记录

- [x] 新增 [结构诊断](core/model_diagnostics.py)：区分资源、数值、观测、参数、结构和语义方向；不把运行失败、大残差或相关噪声当成隐变量存在的证明。
- [x] 动力学开发诊断仅接收训练/选参数组；非重叠窗口筛查残差，区分候选库冗余和活跃参数不稳定。阈值标为未校准筛查，不冒充显著性检验。
- [x] 最终测试诊断只读，不进入结构修复反馈；改变最终测试不能改变开发诊断、参数和反馈指纹。
- [x] 接入研究结果、版本化诊断产物、报告与 [网页只读面板](web/static/js/model_diagnostics.js)；所有方向均为建议，不执行自动修复、不升级证据等级。
- [x] 将回归/分类的 OOF 与内层交叉验证标量诊断接入同一反馈通道；原始标签、预测数组、确认集和锁定测试不会被复制进诊断或搜索反馈，并对跨折不稳定、残差偏差、异方差候选和分类错误率给出白名单检查方向。
- [x] 将聚类可信度审计接入同一只读通道：分离度、随机种子稳定性和极小簇只产生可撤销筛查方向，不把簇标签升级为自然类别或真值。
- [x] 将有序时序结构信号接入同一只读通道：可靠时间列上的周期、变点和单调性候选经过有界聚合与筛查后进入报告；不把信号升级为机制或因果结论。
- [x] 初版扩展到预测、聚类、时序结构、优化、因果、排名和仿真审计；所有记录仍是只读软提示，受限修复执行与反例重放进入 N07，不能据此宣称已覆盖所有模型族。

使用与边界见 [模型诊断](docs/model_diagnostics.md)。验证记录（2026-09-06）：新增 35 项诊断测试，含隔离、退化保护、归因和产物链路；10 个相关 Python 测试模块共 255 项通过，网页渲染转义与加载顺序测试通过。没有调用真实模型 API；全项目、Linux、声明依赖范围内复验仍未完成。

### N07 标量图开发切片与 N08 中间量复用

- [x] 新增 [显式实验与受限 patch](core/graph_experiments.py)：冻结题意、符号、单位、目标、开发数据、区间、性质来源和阈值；没有最终测试字段。
- [x] 新增 [标量图解释器](core/graph_evaluator.py)，在受控 worker 内执行原语组合和训练段参数拟合；不生成或执行任意 Python。未填机制及不支持算子不会被冒充可执行。
- [x] 新增 [有限预算搜索](core/graph_search.py)：可追溯结构变异、参数与结构分层、固定检查、反例去重归档和复检；新反例会使早先候选等待复检，预算耗尽不晋级未检查候选。
- [x] 提供 [本地运行入口](scripts/run_graph_search.py)、[合成示例](examples/graph_search_scalar.json) 和版本化产物；不调用模型 API，不增加题目名称分支。
- [x] 缓存同图、同有序输入的参数无关中间量；参数依赖部分重算，内存有界且不缓存结论；开关对照保持结果和检查计数一致。
- [x] 标量图基础原语扩展：`abs/sqrt/sin/cos/minimum/maximum` 通过 IR 量纲/形状校验、受控解释器和有限结构变异；三角与指数/对数拒绝有量纲输入，平方根保留分数维度。
- [ ] 接入受预算约束的真实模型变异适配、多结构/多后端编译、自动数据角色绑定、模型组的预登记确认与网页交互；当前自动变异仅为小型语法基线，不代表通用生成质量已验证。
- [x] 跨图增量编译、全上下文持久缓存、多保真晋级、保留最终检查预算及固定预算基准。`IncrementalCompileCache`/`PersistentCompileCache` 覆盖上游图键和源/域签名，`run_multifidelity_search` 保留高保真与反例预算，`cache_performance_benchmark` 记录冷/热/增量一致性；报告仍不把计时当作普遍提速或覆盖率证明。

使用方法与边界见 [数学图搜索](docs/graph_search.md)。验证记录（2026-09-06）：新增 57 项测试，覆盖真实 worker、无数据条件性质、训练/选参隔离、单位/权限拒绝、非线性节点插入、反例重放、容量停止、旧候选失效、预算、缓存开关和命令行产物。12 个相关模块共 315 项通过；网页脚本语法、转义渲染、文档本地链接及差异空白检查通过。示例实测得到 1 个待确认候选，但这不构成未知题覆盖率或现实正确性证据。运行快照位于 Git 忽略目录；未调用真实 API、未做完整依赖矩阵或 Linux 验证。

### N07–N09 冻结留出审计切片

- [x] 会话私有结果冻结候选图、参数、阈值和开发证据；返回结果的外部修改不能改变冻结对象，冻结后不能换选另一候选。
- [x] 新增 [只读留出评价](core/graph_confirmation.py)，没有参数拟合路径；候选选择先于文件读取。逐点检查误差、有限性和声明性质，失败不流入自动搜索反馈。
- [x] 新增 [持久使用登记](core/confirmation_registry.py)，在数值执行前原子保留同一研究的输入点；重命名病例、排序、改标签、并发请求或重启不能自动解除登记，超时和中断不释放已用数据。
- [x] 命令行与版本化产物接入冻结/留出过程；保留开发结果并显示中文失败原因，导出独立的有限留出证据记录，不冒充现实正确性证明。
- [x] 测试开发点上完全一致而新点上分歧的机制、未见奇点、回退评估预算、登记并发和断点保留；对最终标签的修改不能改变已拟合参数。
- [x] 多模型组配对确认底层：2–4 个冻结快照共享同一开发实验，整组一次登记留出使用，每臂等额预分配资源；失败保留、不重试、不转移额度、不依据留出选择冠军。
- [x] 整组冻结文件入口与审计报告：保存整组快照后首次读取留出文件，输出分模型报告和文件校验清单；坏文件、重复消费和中断保留记录，不更换模型刷通过。
- [x] 单实验同开发预算对照编排：共享题意、候选与数值额度，固定开发选模规则，先冻结全部方法再读留出；没有候选的方法保留在分母，支持缓存开关对照。
- [x] 单模型网页实验：开发/留出分请求，私有会话候选冻结并保存后才允许上传留出样本；异步状态、取消、会话隔离、阶段视图及证据下载，不自动调用 API。
- [x] 表格到数学图的开发绑定入口：显式选择 1–4 个输入、目标、单位和 12–360 行范围，自动 SI 换算、系数量纲推导和原语组合；训练段决定参数搜索尺度，编译预览不自动执行，绑定变更使旧准备失效。
- [x] 冻结后的验证表映射：量纲限制单位选项、自动 SI 换算、固定最多 256 行数值快照、拒绝旧快照令牌与重叠输入，映射证据可下载；不修改模型、参数或容差。
- [ ] 拓展时间/实体/近邻污染隔离、数据来源认证、多题/多种子统计基准、统一预算和完整多模型网页判决；当前只保证本地登记范围内的精确输入点隔离。

使用方式见 [冻结后执行留出检验](docs/graph_search.md)。合成示例在 8 个留出输入上检查通过，随后同一研究的重复使用被明确拒绝；这不构成统计覆盖率、未见题成功率或样本代表性证据。登记库不属于可清理缓存；故意修改本地登记或研究标识不能视作取得新证据。

验证记录（2026-09-06）：新增 44 项冻结/留出测试；13 个相关 Python 模块共 359 项通过。实测命令行的首次确认与重复使用拒绝；没有真实模型 API 调用。声明依赖范围、Linux、全项目回归和固定预算对照仍需继续完成，未据此勾选完整 N09 或 P12。

验证记录（2026-09-07）：新增 14 项配对面板测试，与冻结确认、图搜索和资源运行时共 180 项通过，包括真实受控子进程、并发整组登记、不同协议拒绝与中断消费。底层不认证外部调用者的留出读取顺序，不保证开发预算公平或整组总墙钟上限；未据此宣称算法提速、覆盖率或显著准确率提升。

文件入口验证（2026-09-07）：新增 10 项测试，与面板、单模型确认、图搜索、运行时和产物管理共 193 项通过。包含首次读取前整组快照检查、清单校验和、编码/规模拒绝、输出目录变化不重置消费、真实 worker 命令行的通过与失败退出码。尚未实现完整开发阶段同预算基准，也不认证外部快照历史。

单实验对照验证（2026-09-07）：新增 14 项测试，7 个相关模块共 207 项通过。覆盖真实 worker 的三策略对照、单方法成功/全失败分支、先冻结再读文件、无最终标签反馈、同预算缓存对照、资源失败保留及取消。另修复搜索取消时使用非法产物状态导致报错的问题，取消现保存为 `incomplete`。合成示例得到 2/3 方法通过不代表总体成功率；峰值内存和首次有效结果耗时尚未测量，多题统计基准及完整 N09/P12 仍未完成。

网页实验切片（2026-09-07）：新增 15 项后端测试与 1 项真实浏览器测试，检查阶段门、持久消费、会话/文件访问隔离、跨源与超大请求拒绝、双任务配额、异常回收和取消。已用本机 Edge 执行完整开发→冻结→留出流程并检查桌面截图与窄屏布局；渲染转义测试通过。只覆盖显式绑定的单模型实验，不据此勾选全部网页交互或自然语言自动求解。操作与存储限制见 [网页数学图实验](docs/graph_lab.md)。

表格绑定切片（2026-09-07）：新增 15 项编译器测试和会话表格隔离测试；浏览器增加从上传 CSV 到选择变量/单位、准备、搜索、冻结及最终审计的完整路径。没有为具体题目新增求解器。仍从仿射组合起步，不保证找到任意非线性结构；当前行序划分不保证实体/时间独立，缺失值/重复观测设计与模型提议自动绑定仍待实现。

标量原语扩展验证（2026-09-07）：评价器升级为 v4，新增 `abs/sqrt/sin/cos/minimum/maximum`，并在 IR 中传播绝对值保持量纲、平方根半量纲、三角函数无量纲及上下界算子的同量纲前置条件；图搜索仍受同一资源预算和反例闭环约束。相关图搜索、参数拟合、面板、确认、基准和 IR 回归共 216 项通过。该扩展不等于已支持向量、微分方程或任意新题自动求解。

验证表映射切片（2026-09-07）：新增 13 项数值映射测试及 3 项网页阶段/快照测试；真实浏览器路径改为“开发表→冻结→分钟/厘米验证表→审计”，保留 JSON 路径。快照覆盖数据被原地修改和整表替换、过期令牌、任意起始行的有界窗口；沿用持久输入消费登记。仍不认证数据来源和独立性，不支持所有单位或样本设计，未据此勾选完整通用编译与验证阶段。

## P0：基准、接口与安全边界

### 任务

- [x] 建立 `OpenModelBench` 协议初版，划分开发题、未见题、结构变换题和对抗题；案例清单、数据指纹、结果状态和失败归因可复现，但真实题库与执行编排仍待建立。
- [x] 建立数据题、无数据机理题、多表题、优化题、动力学题的有界合成最小测试集；真实竞赛题库、隐藏真值和统计代表性仍待建立。
- [x] 增加有界的变量改名/单位词/故事替换结构变换协议；默认清除原数据指纹，拒绝复用原指纹，独立变体真值必须显式绑定新 fixture；真实数值单位换算和公式结构重写仍待实现。
- [x] OpenModelBench 结果协议定义题意契约、IR、可执行性、数值、约束、反例、区间覆盖、耗时和峰值内存字段；缺失字段保持缺失，不按零计入，真实运行统计仍待接入。
- [x] 新增版本化 [基线注册表](core/baseline_registry.py)，登记回归均值、分类多数类、聚类、动力学零增量和优化可行参考点；预测 Pareto 竞争携带注册表指纹。实际题库运行快照和跨版本统计仍待建立。
- [x] 外部模型客户端初版执行调用次数、提示体积和输出 token 硬上限；密钥不进入客户端 repr/返回值，未配置外部 API 时保留本地/规则路径。跨请求费用核算、供应商级重试策略和完整隐私审计仍待扩展。
- [x] `OpenModelBench.summarize` 分别统计表示、提议、类型、编译、数值、证据和验证失败；当前仅完成协议级统计，尚未接入所有运行入口。
- [x] 增加 `BenchmarkRunner` 薄适配器：统一案例迭代、状态/失败阶段归因和异常脱敏；尚未绑定真实模型流水线，因此不宣称基准准确率。
- [x] 监督预测初版按时间、实体或随机/分层方式建立开发与独立确认划分，并保存方式、规模和索引哈希指纹；完整搜索验证/最终测试协议与全部预处理组件仍待扩展。
- [x] 对搜索验证集的重复使用记录试验次数；最终测试集只用于冻结方案后的评价，若据其结果调整方案，必须更换新的最终测试集。
- [x] 动力学分开评价导数/积分一致性、利用已观测历史的条件预测、只用初始条件与已知外部输入的独立轨迹预测；缺失通道保持 `not_assessed`。
- [x] 增加隐状态、高噪声、观测偏差、机制切换、不可辨识、矛盾条件及纯题面任务的有界对照集。`build_diagnostic_case_catalog` 提供 7 个确定性 fixture；它们是协议压力样例，不代替真实题库代表性。
- [x] 多表关系初版要求显式记录实体键、时间键、单位绑定、联接基数和字段来源；相关性本身不能授权联表或因果连线。
- [x] 登记公开真实题源与官方链接，并与本地封存案例分离；`public_benchmark_catalog` 只保存来源与结构标签，不把题面或答案带入公开仓库。独立评分标准和未见题正确率仍需另行验证。
- [x] 提供公开实验配置，绑定题源目录摘要、固定预算、随机种子、方法对照和留出划分；配置只保证运行条件可复核，不替代独立评分。

### 验收

- [x] 基准运行可复现，并通过 manifest 固定随机种子、依赖版本、运行器版本和基准数据指纹。
- [ ] 训练/提示开发样本与最终未见题不存在内容泄漏。
- [x] 每次提交能运行快速回归集；发布前运行完整基准。新增 `scripts/quick_regression.py`，默认覆盖原语、通用注册表、CEGIS、反例、受监督 worker 和 AST 安全；`--full` 运行完整 pytest。
- [x] 改动最终测试段的值，不会改变训练段的选列、预处理参数或拟合结果；滚动特征仅访问预测时点已经可用的数据。`audit_final_test_isolation` 比较训练摘要，`audit_point_in_time_features` 检查 `available_at <= prediction_time`；仍需在每个生产特征入口接入该审计。
- [x] 明确错误接受率的分母，同时报告“错误候选被接受比例”和“获准结论中的错误比例”；没有真值时不伪报准确率。`core/acceptance_metrics.py` 显式返回两类分母，无真值为 `not_assessed`。

OpenModelBench 协议验证（2026-09-07）：新增 [基准清单与结果协议](core/open_model_bench.py)、4 项测试和 [使用说明](docs/open_model_bench.md)。已验证划分指纹、数据指纹跨划分拒绝、失败阶段强制归因、未解决与未运行分离，以及缺失指标不计为零。尚未建立真实题库、执行编排和未见题真值，因此不宣称任何覆盖率或准确率。

真实未见题封存协议验证（2026-09-08）：新增 `core/blind_benchmark.py`、`scripts/blind_benchmark.py`、[封存基准说明](docs/blind_benchmark.md) 和 5 项回归。公开清单只保留题面/附件/参考解摘要、固定预算和解封承诺；运行账本拒绝预算漂移，仓库扫描返回稳定的泄漏原因而不返回题面，解封前报告保持 `not_scored`，令牌与参考解摘要不匹配时拒绝计分。当前只用非题库 fixture 验证协议，尚未拥有外部真实题面、独立参考解和盲测统计，因此 P0 的内容泄漏验收与真实未见题成功率仍保持未完成。

本地真实题源封存（2026-09-08）：从官方 COMAP 页面下载 2023–2025 年 15 份题面和 3 个公开数据附件到被 `.gitignore` 忽略的 `workspace/sealed_benchmarks/2026-09`；公开哈希清单 digest 为 `c9807cd05529ef8522622021988a92b9959ae24b718ab5cb831f752f8a26c636`，泄漏扫描覆盖 12,803 个公开文本文件并通过。由于建模题通常没有唯一数值答案，当前 15 个案例仍标记 `pending_independent_rubric`，没有伪造正确率；独立评分标准和参考解完成后才可解封计分。

公开题源索引（2026-09-09）：新增 `core/public_benchmark_catalog.py`、`examples/public_benchmark_sources.json`、[公开题源说明](docs/public_benchmark_sources.md) 和 4 项测试。目录登记 COMAP 2023–2025 题源页、2025 CUMCM 官方题源页及 2025 A–F 可复核链接，所有链接经过 HTTPS/官方域名校验；只公开来源、结构标签和封存案例 ID，不含题面、附件内容或参考解。`python scripts/validate_public_benchmark_sources.py` 输出 9 条来源，目录摘要为 `198778541eebd246b0c27f0fc5ec94b5aec1170598653bd8d8f0ac180e82ca8f`。来源登记不等于答案正确率，独立评分标准仍需单独封存和解封。

污染筛查扩展（2026-09-09）：`core/data_provenance.py` 新增有界归一化词片段指纹；开发/提示/最终数据现在同时支持精确 SHA-256 和词片段重叠告警，且不保存原文。该筛查仍不能证明语义等价或排除模型记忆，P0 内容泄漏验收保持未完成。

真实未见题基线执行（2026-09-08）：新增被忽略的本地批量执行器 `workspace/sealed_benchmarks/2026-09/run_batch.py`，每题独立进程、固定种子和有界数据采样，先关闭绘图/可信度审计以测量求解可达性。15/15 题完成、0 超时、0 worker 失败；其中 3 题带附件进入 `data_assisted`，其余 12 题进入 `mechanistic_no_dataset`。该结果只证明执行链路可达，不是正确率；无独立参考解的案例仍保持 `not_scored`。运行账本位于被忽略的 `workspace/sealed_benchmarks/2026-09/runs`，不会进入公开仓库。

纯题面稳定性修复（2026-09-08）：空数据且被识别为评价/排名的题目不再调用空集合 TOPSIS，也不再产生误导性的“缺少数值指标”警告；这保证纯机理题继续生成结构化结果而不是被观测数据附加分析打断。新增无数据运行回归并通过。

英文题面识别补强（2026-09-08）：题型识别器新增受词边界约束的英文优化、预测、网络、仿真、统计、因果、分类/聚类、异常和降维词族；中文规则保持兼容。真实英文题面的重跑显示数据题从泛化的 `evaluation_ranking` 转为 `prediction_forecast` 并进入模型竞争/仿真支路。新增英文识别回归 2 项通过；仍需在完整批次上复核误触发率。

本轮全量回归（2026-09-08）：`1958 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 350.24 秒。新增英文题型识别、纯题面运行和空排名分流均通过；警告数量未增加，仍主要来自第三方依赖、模型收敛和字体环境。

英文数学契约抽取（2026-09-08）：英文题面现在保留带单位数值、`subject to`/`must`/上下界和 maximize/minimize/predict 等目标候选，并过滤 “25 pages/words/report” 一类竞赛提交要求，避免把文档格式误当成模型约束。新增 3 项回归通过；这些候选仍是待核验事实，不会绕过单位、变量角色和可执行约束审计。

英文多问分解（2026-09-08）：题面分解器支持 `Question n`/`Part n` 标记，保留每一问的独立任务节点和来源文本，避免英文竞赛题被压成单一总任务。新增分解回归通过。

题面噪声隔离（2026-09-08）：过滤 PDF 抽取造成的竞赛年份伪单位（如 `2025 m`）以及“必须遵守投稿/AI 规则”类文档指令，避免它们进入变量和数学约束契约。英文题面回归 4 项通过。

通用结构候选切片（2026-09-08，2026-09-13 更新）：新增 `core/structure_candidates.py` 并接入主研究链路。它根据题型组合有限的基础原语搜索种子，显式列出未知机制、澄清问题和待执行状态；候选不会进入事实、数值证据或求解结论。新增 `problem_statement` 动态编译路由，将任意题面送入确定性问题分析、机理 IR、候选结构和缺失条件审计；已验证的 `contracts` 可在同一入口进入受限执行，未闭合题面返回 `needs_input`。这关闭了“新题只能从完整题型注册表选择”的入口缺口，但不把缺少变量/单位/约束的自然语言伪装成数值答案；ODE/优化/多表全后端竞争和反例闭环仍按各自验收门推进。

基线对照协议验证（2026-09-07）：`OpenModelBench.compare_runs` 增加同一开发/结构变换集的配对方法对照，拒绝未见题和对抗题参与选模，保留缺失案例分母且不自动选择赢家。当前仍未接入注册表实际运行结果，因此 P0 的“保存基线结果”保持未完成。

最小基准清单验证（2026-09-07）：新增 `examples/open_model_bench_minimal.json` 与 `scripts/validate_open_model_bench.py`，覆盖 data、pure_mechanistic、multi_table、optimization、dynamics 五类和四种划分；命令输出稳定清单指纹 `25b0d42e5a6c21ab5fbd5cbae99860e78737bd4a009266b0b29c89ead2608621`。该清单是协议样例，不是真实题库或准确率基准。

诊断对照集验证（2026-09-08）：新增 `build_diagnostic_case_catalog` 与回归测试，覆盖隐状态、高噪声、观测偏差、机制切换、不可辨识、矛盾约束和纯题面七种失败归因；fixture 不包含真实竞赛题真值。

基准运行适配验证（2026-09-07）：`BenchmarkRunner` 新增 2 项测试，统一公开案例迭代、运行异常脱敏和结果契约错误归因；它仍不连接真实模型执行流水线，不据此宣称任何准确率。

结构变换协议验证（2026-09-08）：新增 `core/benchmark_transformations.py`、`docs/benchmark_transformations.md` 和 4 项测试；变量/单位采用词边界替换，叙事替换有长度/数量上限，变体默认移除原数据指纹和真值并拒绝复用原指纹，独立真值必须绑定新指纹。该协议只防止基准泄漏，不代表结构泛化准确率。

线性单位换算验证（2026-09-08）：`mathematical_reasoning` 新增 `unit_conversion_factor`/`convert_values`，要求量纲兼容、有限值和数组预算，拒绝未经定义的仿射温度转换；新增 2 项测试通过。该接口不自动修改列名或推断未知单位。

机制删减验证（2026-09-08）：新增 `core/model_simplification.py`、`docs/model_simplification.md` 和 2 项测试；支持主目标/可行性/压力域的有界消融比较，删减建议只在当前评价域内成立，不冒充全局误差界。

主动澄清信息价值验证（2026-09-08）：新增 `core/information_value.py`、`docs/information_value.md` 和 2 项测试；对声明候选权重的答案分区计算期望熵下降，未知候选和分区重叠会拒绝。该计算不等于真实后验或因果信息增益。

观测不确定性验证（2026-09-08）：新增 `core/observation_uncertainty.py`、`docs/observation_uncertainty.md` 和 2 项测试；统一单位后分解观测实体间与处理分支内波动，方差重构残差可审计，单次观测标记 `partial`。

观测噪声与交互分解增量（2026-09-09）：增加显式 `observation_noise_variance` 元数据、处理单元内波动、处理分支边际方差及观测×处理交互项；不平衡设计保留重构残差，5 项观测不确定性测试通过。该结果仍是经验账本，不是测量误差定理或后验。

数值策略路由增量（2026-09-09）：新增 `core/numerical_strategy.py`，根据有限条件数、刚性比、事件数和梯度验证状态给出可审计的保守后端提示；缺失诊断保持 `not_assessed`，未经交叉验证不启用梯度加速。新增 2 项测试通过，提示不授予求解正确性证书。

切分隔离增量（2026-09-09）：新增 `core/split_integrity.py`，检查训练/验证/最终测试指纹分离，并比较读取最终测试前后的特征选择、预处理和拟合摘要；新增 2 项测试通过。该摘要审计仍不能替代所有端到端流水线的无泄漏证明。

结构扩展增量（2026-09-09）：新增 `core/structure_extensions.py`，提供非因果交互筛选、UDE 修正项契约和有界 PDE 导数库契约；新增 2 项测试通过。它们只扩展候选空间，不冒充 GNN/UDE/PDE 的完整训练、求解或物理证明。

对比消融协议增量（2026-09-09）：新增 `core/ablation_protocol.py` 和 2 项测试，固定基线/处理臂、任务网格、预算、锁定最终测试和人工评分字段，并将超时/拒绝/错误纳入分母；该协议不产生未执行的性能结论。

失败归因增量（2026-09-09）：`failure_diagnosis` 扩展为语义、结构、参数、数据、数值和资源六类根因字段，并保留观测误差竞争解释；新增回归通过，仍要求具体求解入口提供对应证据。

效率与隔离增量回归（2026-09-09）：加入缓存政策、切分隔离、流式任务、结构降维、预览抽样、有界队列和数值策略的定向测试；`scripts/quick_regression.py` 通过 255 项，`compileall` 通过。该数字是当前快速回归规模，不是未见题成功率。

增量回归更新（2026-09-09）：加入渐进保真度晋级、最终确认/反例预算、假设控件闭包、对比消融、建模政策、调度压力和 SymPy 安全构造测试；快速回归现为 271 项，定向新增测试均通过。

调度压力协议增量（2026-09-09）：新增 `core/stress_protocol.py` 和 1 项测试，记录有界队列在完成/取消/超时/准入拒绝下的状态与释放快照；未将该协议冒充主服务端到端压力认证。

SymPy 后端增量（2026-09-09）：新增 `core/sympy_backend.py`，先过安全表达式 AST 再逐节点构造 SymPy 对象；不使用 `sympify/eval`，符号结果仍需独立量纲、域和数值证据。

评价协议验证（2026-09-08）：新增 `core/evaluation_protocol.py`、`docs/evaluation_protocol.md` 和 2 项测试；记录验证集重复使用、冻结最终测试指纹并阻止最终测试驱动适配，同时分离四类动力学评价通道。

多表证据验证（2026-09-08）：评价协议新增 `validate_multi_table_manifest` 与 1 项测试，要求实体/时间键状态、单位绑定、来源字段和联接基数齐全；相关性单独不能通过关系契约。

基准可复现清单验证（2026-09-08）：`OpenModelBench` 新增 `build_reproducibility_manifest` 与 1 项测试，固定 seed、依赖版本、运行器版本、基准指纹和 manifest 摘要；不包含用户原始数据。

类型化 IR 契约复核（2026-09-08）：复核 `core/model_hypotheses.py` 及其回归，确认 `ProblemContract`、`HypothesisIR`、`EvidenceLedger`、`SearchController`、`UnknownMechanism` 与 `MathType/VariableDescriptor` 均具备版本、来源、量纲/形状、循环依赖和预算边界；完整后端编译及动态反馈块仍未完成。

动态结构合成切片复核（2026-09-08）：复核假设提议、图 patch、图搜索、参数拟合和模型竞争回归，确认模型/API 只产生受限候选，结构与参数分层，候选有预算/哈希/多样性审计，patch 需父版本和完整重验证；当前覆盖标量图，尚未覆盖 ODE、优化和多表。

CEGIS 与主动澄清复核（2026-09-08）：通用 CEGIS 控制器与信息价值模块通过定向回归；修复/轮次/候选预算、重复候选、未评估状态、答案分区和契约 revision 均有测试证据。正式概率校准、全后端反例搜索和增量重算仍未完成。

全量回归验证（2026-09-08）：`1883 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 313.25 秒。覆盖新增单位换算、CEGIS 控制和机制删减模块；警告类别未扩大，主要仍来自第三方依赖、模型收敛、字体和可视化弃用提示。

主动澄清增量回归（2026-09-08）：信息价值、机制删减和单位换算相关测试 6 项通过；新增模块编译检查通过。信息价值模块加入完整矩阵后的全量回归待下一轮统一执行。

安全表达式与规模路由切片（2026-09-08）：新增 `core.safe_expression`，将原机理编译器内的标量 AST 解释器抽为公开、可测试的受限数学语言；拒绝属性/下标/导入/赋值/字符串/关键字参数和非有限绑定，保留“语言限制不等于操作系统沙箱”的边界。新增 `core.backend_planner` 并接入四层求解计划，依据变量、点数和矩阵单元数选择可信 worker、可信后端 API 或延期，不把超规模/未验证节点降级为近似答案。相关 82 项表达式/监督测试与 30 项规划/通用求解测试通过；尚未声称任意生成代码可安全执行，也未完成文件/网络权限隔离。

模型竞争切片（2026-09-08）：新增 `core.model_competition.compete_models`，把 Pareto 比较与保守判决书组合：缺少指标的候选保留为未决，硬失败/反例不进入数值比较，Pareto 非支配不自动获准，反例会撤销显式推荐。新增 5 项回归测试；尚未完成全模型族接线、统计基准或正式后验概率。

观测模型竞争接线（2026-09-08）：主研究链路对同一目标的交叉验证候选计算验证损失、已拟合结构规模代理、折间不稳定性和训练墙钟成本，执行 Pareto 比较并输出 `model_competitions`、独立证据产物和网页/Markdown 摘要；缺少决策输出不再误报“决策一致”。预测证据有界到 512 点，开发/OOF 比较不会自动获准。相关模型集合、竞争和双目标研究回归 12 项通过；尚未覆盖优化、ODE、多表结构候选或冻结最终测试的统一竞争。

可视化资源回收修复（2026-09-08）：保存型绘图在写出报告后关闭当前及残留 pyplot figure，避免长批量绘图累积内存；可视化回归 `60 passed`。该策略仅作用于保存型批处理，返回 figure 对象仍保持兼容；字体缺失和 seaborn 弃用警告仍属于依赖环境提示。

全量回归复核（2026-09-08，2026-09-13 更新）：完整本地 `pytest -q` 为 `2533 passed, 7 skipped, 8 subtests passed`，耗时约 718 秒；新增统一动态编译同步/异步回归和完整 CI 工作流（Python、JavaScript、Linux sandbox 三个 job）。警告仍来自第三方依赖收敛/版本、字体和 seaborn 弃用提示；这只证明当前测试集和 CI 编排可运行，不等于 ROADMAP 中尚未勾选的开放世界覆盖率、独立评分或所有平台安全证明已经完成。

澄清回答契约切片（2026-09-08）：`ProblemContract.record_confirmation` 将用户对主动问题的回答追加为带精确文本跨度的新事实，生成递增 revision 和父哈希；可选标记为硬约束，旧证据不会跨契约版本匹配；默认确认 ID 按 revision 自动生成。新增 3 项测试；尚未把网页选项按钮和候选增量编译全部接线。

澄清交互闭环切片（2026-09-08）：新增严格的 `ProblemContract.from_payload`，网页把当前结果中的问题渲染为回答框；`/api/research/clarify` 仅接受服务端已有的问题索引，写入会话绑定的新契约 revision，并用哈希与逐字题面匹配保护下一次研究重跑。旧结果不原地改写，API Key 不持久化，标准新运行会清除旧契约基线。当前采用安全的全流程重算，尚未完成依赖图级增量重算，因此 P7 对应总任务仍不勾选。

澄清选项交互切片（2026-09-08）：假设提议器的问题现在可携带 2–3 个带影响说明的结构化选项；当模型只给两个选项时，服务端追加“暂时无法确定”保留分歧。网页仅将选项安全地填入自由回答框，仍由用户确认并创建新契约，后端不接受模型直接选择或改写事实。保留纯字符串问题的兼容格式；这只是减少澄清摩擦的 UI/协议切片，不是正式信息增益计算，也不代表选项本身为证据。

澄清问题去重切片（2026-09-08）：提议器读取当前契约中的用户确认事实，规范化并抑制已原样回答的问题，同时保留 `suppressed_questions` 审计字段；只保证精确/轻度文本重复，不声称完成语义重复识别。

澄清近重复提示切片（2026-09-08）：对与历史问题文本相似但不完全相同的提问生成 `possible_repeated_questions` 提示，网页和报告不自动删除，避免把文本相似误当成语义等价。

模型引导图变异切片（2026-09-08）：新增 `GraphPatchGenerator`，外部/本地模型只接收去堆栈的有界诊断与最小反例，并返回最多 4 个窄范围 JSON patch；父版本、改动数、符号、输出、类型、单位和图接口由既有 IR 校验器复核。标量图 CEGIS 可选接入具有 HTTP 超时保证的模型后端，模型提议仍需完整执行与反例重放，模型本身不参与裁决；任意 Python 回调不得进入自动循环。本地网页数学图实验可显式启用该链路，密钥不写入证据文件，公开模式仍禁用实验室。相关图变异、搜索与网页回归 94 项及前端语法/渲染测试通过。尚未接入 ODE、优化、多表或真实 API 质量基准，因此 P3/P5 总任务不整体勾选。

通用 CEGIS 控制验证（2026-09-08）：新增 `core/cegis_controller.py`、`docs/cegis_controller.md` 和 3 项测试；在不理解领域题型的情况下统一处理反例反馈、候选哈希、修复/轮次/候选预算、回调失败和“最后候选未检查不晋级”。该层只负责搜索编排，不能替代数学评价器、受控 worker 或独立留出验证。

来源依赖增量缓存切片（2026-09-08）：`IncrementalCompileCache.compile` 新增可选 `source_facts` 与节点级 `source_fact_ids`，并提供 `contract_source_facts` 将 `ProblemContract` 的精确引文和硬约束状态转成受控来源快照。新增确认事实不会使无关显式来源节点全部失效；直接依赖改变的节点及其后继仍会重编译，未声明来源的节点保守按全局契约签名失效。新增缓存/契约回归 63 项通过；缓存只复用中间产物，任何数学数值证据仍须独立重算。

全量回归验证（2026-09-07）：`1681 passed, 5 skipped, 214 warnings, 8 subtests passed`，耗时约 309.57 秒。警告主要来自第三方依赖收敛、字体和可视化弃用提示；本批新增能力未引入失败。该结果不等于未完成的 P1–P12 验收项全部完成。

全量回归验证（2026-09-08）：`1875 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 312.19 秒。新增 IR 信封、原语组合图和结构变换测试未引入回归；警告仍主要来自第三方依赖收敛、字体、模型收敛与可视化弃用提示。该结果仍不等于开放世界覆盖率、真实题库准确率或操作系统级权限隔离已经完成。

全量回归复核（2026-09-08）：最新 `python -m pytest -q` 为 `1909 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 321.55 秒。新增缓存契约、判决追溯、发布审计、代理域监控、资源准入、观测模型、图语义、候选硬门、题意不确定性、问题证据动作、配对基准统计和梯度门测试均通过；警告类别未扩大。该结果仍不等于 ROADMAP 中所有研究性后端和发布验收已经完成。

全量回归复核（2026-09-08）：最新 `python -m pytest -q` 为 `1955 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 359.68 秒。新增封存真实未见题协议、原语运行时、条件等价重写图、日志脱敏边界、变形检查协议和离线语义后端均通过；警告类别未扩大。该结果仍不等于开放世界覆盖率、外部后端可用性或真实未见题上的统计改进。

全量回归复核（2026-09-08）：最新 `python -m pytest -q` 为 `1941 passed, 5 skipped, 204 warnings, 8 subtests passed`，耗时约 312.96 秒。新增求解器结果契约、可选后端发现、执行清单和发布锁文件/许可证存在性报告均通过；新增警告仅为现有依赖/收敛/字体提示。未勾选的开放世界基准、完整后端接线、跨平台权限隔离和统计验收仍未被这次回归替代。

全量回归复核（2026-09-08）：最新 `python -m pytest -q` 为 `1946 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 352.53 秒。新增研究敏感性 API、固定任务基准运行器、IR 通用性守门及其回归测试通过；警告类别仍为第三方依赖、模型收敛、字体和可视化提示。

全量回归复核（2026-09-08）：最新 `python -m pytest -q` 为 `1924 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 321.13 秒。新增运行清理配额、假设敏感性、研究表述、错误接受率、渐进结果和追溯下载相关测试均通过；警告仍主要来自依赖版本、模型收敛、字体和可视化弃用提示。

增量全量回归（2026-09-07）：加入语义澄清、无量纲分析和梯度校验后，`1690 passed, 5 skipped, 214 warnings, 8 subtests passed`，耗时约 312.20 秒；警告类别未扩大。新增代码仍只提供候选/校验工具，不宣称开放世界建模已完成。

## P1：类型化数学 IR v3

### 任务

- [x] 将变量表示为 `值域 + 形状 + 单位维度 + 时间语义 + 可观测性 + 来源`；当前通过 `MathType` 与 `VariableDescriptor` 表达，自动字段绑定仍需人工/数据证据。
- [x] IR 变量/参数提供可选 `descriptor` 初版：校验值域、时间语义、可观测性与来源，并与 latent/observed 角色一致性联检；旧 IR 保持兼容，尚未接入自动数据角色绑定。
- [x] 候选提议 Schema 已允许带类型化 `descriptor`，并在模型生成提示中明确其只能表达待核验语义；仍不自动把字段名绑定成真实观测。
- [x] 等价重写初版：交换律候选带来源/目标哈希和浮点容差声明，图搜索按代数等价键去重；目前仅覆盖四个交换运算，不代表完整等价理论。
- [x] 建立最小节点签名初版：变量、常量、方程、微分、积分、分布、事件、图、目标、约束、控制和观测；当前是非执行接口，完整 IR 编译和后端语义仍待完成。
- [x] 新增 `SemanticHypothesisSet`：并列解释绑定契约事实、假设和待澄清问题，且不改变事实契约或授予结果权威；尚未接入网页交互和自动澄清排序。
- [x] 每个节点保存题面事实引用、假设引用和字段/用户确认来源坐标；未绑定来源的候选不能通过 HypothesisIR 校验。
- [x] 建立版本化 IR 信封、显式迁移器和稳定 SHA-256 哈希；当前覆盖传输完整性与版本边界，尚未替代每类 IR 的完整 JSON Schema。
- [x] 初版加入维度、定义域、形状和循环依赖检查；覆盖 HypothesisIR 与原语组合图，动态反馈块和所有后端仍待接入。
- [x] 引入等价重写图：微分/积分、显式/隐式、最小化/最大化等价形式。`core/equivalence_graph.py` 记录条件、来源和可执行边。
- [x] 定义 `ProblemContract`：事实、观测语义、目标、硬约束及来源；修改生成独立契约版本并使旧证据失效。
- [x] 定义 `HypothesisIR`：候选机制、观测关系、潜在状态、参数及适用域，并区分事实、假设、数据推测和用户确认。
- [x] 定义 `EvidenceLedger` 与 `SearchController`：记录证据/反例/误差作用域、版本、种子、方法和搜索预算；数值后端接入仍需继续。
- [x] 增加 `UnknownMechanism`：声明输入输出类型、允许算子、性质和未填充状态；未填充节点不能冒充可执行关系。
- [x] 同时表达过程模型与观测模型，支持延迟、偏置、噪声、分组及缺失机制的竞争假设。`core/observation_model.py` 提供类型化并列契约；尚未自动估计真实观测噪声或隐状态。
- [x] 区分计算依赖图、数学耦合图和因果假设图；循环依赖需识别为动态反馈或隐式求解块，不能统一按非法 DAG 拒绝。`core/graph_semantics.py` 已提供三类图契约，因果/计算循环拒绝，数学耦合循环显式保留。
- [x] 将原语的前置条件与后置性质沿组合图传播，生成待验证义务；类型通过不代表存在性、唯一性、稳定性或现实正确性通过。`propagate_obligations` 只生成可审计待办，不发布证明。
- [x] 等价重写条件保留切片：`core/equivalence_graph.py` 为交换、显式/隐式、最小化/最大化、约分和微分/积分提议保存源/目标哈希及定义域、唯一性、初值/边界和积分常数义务；未满足条件的边标为 proposal，不可执行。完整等价理论、浮点重排误差和所有后端仍待完成。
- [x] 将 Decapodes/离散外微分作为后期 PDE 后端方向：`core/discrete_exterior_calculus.py` 已补充有限复形、形式次数、方向 incidence 矩阵和 `d²=0` 义务；明确不将其宣称为完整范畴论/Decapodes 编译器。

### 验收

- [x] 新增 `core/ir_genericity.py` 作为执行前启发式守门，检测常见赛题、无人机/导弹编号和商品字段硬编码；发现时阻断该 IR。它不是完整本体或开放世界泛化证明，未知领域泄漏仍需未见题基准。
- [x] 非法量纲、未定义变量、错误张量形状和缺失边界条件在执行前被拒绝。`validate_primitive_graph` 在后端选择前检查引用、方程量纲/形状、节点形状和原语义务；未满足义务只返回结构化拒绝，不执行。
- [x] 同一模型的等价表达具有可追踪的重写证明，不重复计为独立候选。`certify_rewrite` 对已登记交换律、方程表示和定义域条件生成精确规则证书；未登记或缺条件的边仍为 `not_proved`，不依赖有限数值测试冒充证明。
- [x] 假设生成器不能把猜测标成事实；修改契约后相关编译结果、缓存与证书按依赖失效。`SemanticHypothesisSet` 区分 facts/assumptions；`IncrementalCompileCache` 把 source facts、上游键、编译版本和域签名纳入键，契约变更不会复用旧中间产物或证书。

等价重写验证（2026-09-07）：`tests/test_graph_search.py` 新增交换律审计与搜索去重测试，64 项通过。当前只验证标量图中四种交换运算；结合律、分配律、微积分重写和完整证明义务仍未完成。

求解器能力契约验证（2026-09-07）：`UniversalSolverRegistry.describe` 为 17 个内置执行器提供稳定的数学族、输入契约、复杂度提示和后端映射，并允许自定义执行器显式声明元数据；新增 2 项测试通过。描述仅用于规划，不构成求解正确性或性能保证。

观测模型契约验证（2026-09-08）：新增 `core/observation_model.py` 与 2 项测试，显式表示观测/潜在状态、延迟、偏置、噪声、分组和缺失机制，并允许并列竞争；契约不把假设升级为现实证据。

图语义分离验证（2026-09-08）：新增 `core/graph_semantics.py` 与 2 项测试，分别校验计算依赖、数学耦合和因果图；只有数学耦合图允许显式循环，避免将动态反馈误判为普通拓扑错误。

原语义务传播验证（2026-09-08）：`core/primitive_graph.py` 新增 `propagate_obligations` 与回归测试，把积分域、边界等局部义务传到输出节点，并明确标记为未证明。

## P2：最小数学原语运行时

### 任务

- [x] 代数：表达式、线性系统、非线性方程、最小二乘。当前提供线性系统、受限多项式根、线性最小二乘和标量图表达式；未支持的非线性/符号后端仍明确返回不支持。
- [x] 微积分：数值积分、常微分方程、边值问题、事件检测。当前提供有界多项式积分、线性 ODE 和阈值事件；边值问题仍由能力描述拒绝，不伪装为已实现。
- [x] 概率统计：分布、似然、条件概率、重采样和假设检验。当前提供样本期望、正态似然、bootstrap 均值和置换检验，并保留校准/样本预算边界。
- [x] 优化：连续、整数、多目标、鲁棒、随机和最优控制的最小构件。当前提供 LP/MILP/QP、多目标、鲁棒、随机和有限状态动态规划；未实现的最优控制仍不放行。
- [x] 图与网络：路径、流、匹配、连通、扩散和时变图。当前提供最短路、最大流、最小费用流、二分图匹配及 Markov 转移；时变/扩散扩展仍需专门契约。
- [x] 几何与事件：距离、相交、区域成员、视线和区间并集。`PrimitiveRuntime` 与 `UniversalSolverRegistry` 已提供受限距离、线段相交、盒/多边形成员、障碍物视线和区间并集；所有输入均有有限性、形状和规模门。
- [x] 为现有通用求解器定义能力描述、输入契约、复杂度估计和后端映射；未知自定义执行器默认标为 `custom/unknown`，不改变执行权限。
- [x] 使用性质测试验证守恒、单调、对称、可逆和上下界等数学性质。`property_testing` 现在同时提供标量边界/单调/对称和状态转移守恒/可逆检查，全部有限探针结果仍标记为经验测试。
- [x] 新增独立的有限性质检查内核：上下界、单调性和奇偶对称均返回结构化反例/未评估状态；通过只记为 `tested_not_falsified`，尚未接入全部求解入口。
- [x] 原语签名初版包含参数数量、输入/输出类型、量纲要求、义务和结构化错误；资源估计、完整定义域与所有后端接入仍待完成。
- [x] 原语组合图初版检查引用完整性、重复节点、普通拓扑循环、关键前置义务、等式两端量纲及可选形状元数据；动态反馈块和后端执行仍待完成。
- [x] 性质验证以原语真实适用条件为前提，不强制每种原语都具有守恒或单调性；未证性质不能当作硬约束。守恒必须显式提供不变量、可逆性必须显式提供逆映射，不满足前置条件则 `not_assessed`。

### 验收

- [x] 新模型只通过组合原语表达，不增加题目专用函数。`PrimitiveRuntime`/`UniversalSolverRegistry` 只接受结构化数学契约；`ir_genericity` 对题目实体和字段硬编码做执行前拦截，扩展通过注册最小原语而不是新增题目分支。
- [x] 每个已登记基础原语均有正常/最小与最大元数、非有限量纲和超尺度量纲契约回归；执行语义仍由对应后端单独验证。
- [x] 求解器结果统一契约切片：`core/solver_result_contract.py` 标准化状态、结果、残差、误差界状态、迭代信息和资源用量；缺失误差界保持 `not_assessed`，不从拟合分数推断证明。尚未强制所有历史后端统一调用。

数学原语执行切片验证（2026-09-08）：新增 `core/primitive_runtime.py`，在不执行代码字符串的前提下提供有界线性系统、最小二乘、多项式根、固定高斯积分和加权期望；返回状态、残差/秩/求积阶数等可审计字段，并拒绝未知操作、非有限输入和规模超限。ODE、边值、完整概率推断、优化/图/几何后端仍未因该切片完成。

受限动力学原语扩展（2026-09-08）：`primitive_runtime` 新增无回调的有界线性 ODE（矩阵指数）和一维阈值事件检测，限制状态维度、时间点数、矩阵乘积预算和单调时间，并返回轨迹/事件交叉证据；新增正常、边界、非有限和非单调输入测试 4 项通过。该切片不代表非线性 ODE、边值问题或完整事件语义已完成。

动力学原语注册接线（2026-09-08）：将 `linear_ode/v1` 与 `threshold_event/v1` 接入 `UniversalSolverRegistry`，同步提供数学族、输入契约、复杂度和后端能力描述；注册表执行直接复用受限原语运行时，不接受代码回调。相关注册、执行和原语测试共 17 项通过。

受控 worker 接线（2026-09-08）：将上述两个动力学原语加入 `solver_runtime`/`solver_worker` 的显式执行白名单，继续沿用独立进程、输入/输出上限和资源监督；未把整个通用注册表开放给 worker。受控进程、原语和注册表回归共 81 项通过。

规模路由一致性修复（2026-09-08）：`backend_planner` 同步将两个动力学原语识别为 `trusted_worker`，避免规划层把实际已受监督的执行错误降级为普通后端 API。相关路由、worker、注册表和原语回归共 89 项通过。

动力学规模估计修复（2026-09-08）：规模规划现在识别 `matrix`、`times` 和 `values` 字段，不再把 ODE 的状态矩阵或事件时间网格当成零成本输入；新增超点数回归通过。

候选门接线（2026-09-09）：结构候选现在携带类型、单位、形状、边界、约束和资源六类待检查项，并通过 `candidate_gate` 输出 `candidate_set_inadequate`/`not_assessed` 等状态；未完成硬检查不会进入数值比较。研究助手、候选门和结构候选回归共 45 项通过。

候选指标鲁棒性修复（2026-09-09）：`candidate_gate` 对 NaN、无穷、不可转换指标不再抛出异常或参与 Pareto 排名，而是记录 `metric_invalid` 并保留 `not_assessed`。新增回归通过，避免“似对非对”的无效分数污染模型竞争。

候选可复现指纹（2026-09-09）：结构候选输出 `candidate_digest`，由规范化候选 JSON 计算，便于缓存键、重放和跨运行比对；测试确认相同题型契约和预算生成相同指纹。

等价重写条件验证（2026-09-08）：新增 `core/equivalence_graph.py` 与 3 项测试；`x/x` 未确认非零域时保持不可执行，微分到积分转换保留积分常数和初值/边界义务，所有边携带可复现哈希。该模块是受限表达语言的可审计基础，不是形式化证明器。

性质检查验证（2026-09-07）：新增 `core/property_testing.py` 与 3 项测试，覆盖有限上下界、单调性、奇偶对称、预算耗尽和评估器异常分流。结果严格区分 `tested_not_falsified`、反例和 `not_assessed`；尚未宣称连续域证明或完成全部求解器接入。

几何/统计原语扩展验证（2026-09-09）：`PrimitiveRuntime` 新增距离、线段相交、区域成员、视线、区间并集、正态对数似然、固定种子 bootstrap 和置换检验；`UniversalRelationValidator` 与 `UniversalSolverRegistry` 同步提供类型/规模验证和受限执行，新增正常、边界、非有限及随机重放测试通过。随机检验结果仍是样本证据，不是总体定理。

语义并列解释验证（2026-09-07）：`SemanticHypothesisSet` 新增 2 项测试，能够在同一 `ProblemContract` 下保存多个解释、事实引用、假设与待问问题，并拒绝虚构事实绑定；正式信息价值估计仍未实现。

主动澄清启发式验证（2026-09-07）：`SemanticHypothesisSet.rank_questions` 现在按假设分歧与声明成本排序最多三个问题，并明确返回 `not_formal_information_gain`；用户不回答时仍保留全部解释。

主动澄清选项验证（2026-09-07）：`rank_questions` 增加受控的 2–3 选项、影响说明和假设绑定校验；选项仍只创建用户确认入口，不自动改写契约或数值证据。

候选多样性审计验证（2026-09-07）：图搜索新增机制拓扑键和 `diversity_audit`，65 项图搜索测试通过；变量改名造成的伪多样性可被报告，但不会因启发式相似直接删掉候选。

## P3：动态结构合成器

### 任务

- [x] 外部 API/小模型只生成符合 Schema 的候选 IR，不直接生成最终结论；当前已覆盖假设提议和标量图 patch。
- [x] 实现严格 JSON/Schema 校验，禁止自由文本进入执行层；约束解码仍可作为后续优化。
- [x] 从题面、数据画像、单位和已有证据生成多个结构假说；候选不足时允许返回空集合。
- [x] 实现受限结构变异：增加/删除项、局部耦合改变、非线性节点和隐状态提议；ODE/多表专用变异仍待扩展。
- [x] 实现预算化候选池与局部结构变异，并保留候选哈希、多样性和停止状态；MCTS/多岛进化尚未启用。
- [x] 初版加入复杂度、拟合、稳定性、约束和成本的 Pareto 比较；全模型族指标统一仍待完成。
- [x] 增加机制拓扑多样性审计：忽略符号名称检测候选坍缩，并明确只作诊断、不自动删除；公式背诵和跨题语义相似仍需基准集。
- [x] 标量图初版分离外层结构搜索和内层参数拟合，禁止 LLM 猜参数；ODE/优化/多表仍需接线。
- [x] 修复器输出带父版本、目标节点和受限变更的 IR patch，并重新执行既有验证；不能修改判卷标准。
- [x] 初版将残差、尺度、跨折稳定性、分类错误率、聚类可信度及周期/变点/单调信号路由为可撤销候选优先级；提示不修改 IR 或硬约束，完整分组/条件数搜索预算仍待统一编排。
- [x] 跨域类比必须附变量、单位、交互、守恒及边界的映射，映射缺失时只保留提议，不能直接套用领域方程。`core/analogy_mapping.py` 固化映射摘要、用户确认状态和稳定指纹，但始终不授予执行权限。
- [x] 不强制主控定律、删除固定比例变量、锁定因果 DAG 或停用黑箱基线；复杂度与解释性必须接受独立证据比较。`audit_modeling_choice` 对四类越权选择硬拒绝，并不替用户选择模型。

### 验收

- [ ] 有多种合理解释的未见题能保留结构不同且类型合法的候选；唯一确定或候选不足的任务不制造伪多样性。
- [x] 关闭外部 API 时可显式选择 `offline` 语义后端；它不联网、不猜测、不执行，返回空候选，规则/图搜索/已有原语仍可独立运行。离线自动语义提议质量不宣称等同外部模型。
- [x] 候选生成有次数、时间和费用硬上限，不会无限自循环。`CEGISConfig`/`run_cegis` 现在支持轮次、候选、修复、反例、墙钟和 cost units 硬预算，超预算候选只能 `not_assessed`，不能晋级。

## P4：受限代码生成与沙箱执行

### 任务

- [x] 建立 `IR -> backend plan`，优先直接调用 API，而不是拼接任意 Python。`core/backend_planner.py` 只输出受控路由计划。
- [x] 支持 SciPy、SymPy；将 CVXPY、Pyomo 和 JAX 作为可选后端。`core/optional_backend_runtime.py` 只接受结构化 SciPy 求解契约、安全 SymPy AST、受限 CVXPY LP 和可微 IR；缺包或缺外部求解器显式返回 `unavailable`，不冒充执行。
- [x] 新增 `core/optional_backends.py` 做无副作用的 SciPy/SymPy/CVXPY/Pyomo/JAX 可用性发现；发现结果不能绕过类型、资源和结果证据门，实际后端编译/求解仍按各后端逐一验收。
- [x] 必须生成代码时，只允许白名单 AST、白名单模块和受限数据访问。`core/safe_code.py` 负责语言白名单，执行仍需受控 worker。
- [x] 子进程执行并限制 CPU 时间、内存、输出大小、线程数和临时工作目录。`SolverProcessRunner` 已实现；Windows/Linux 权限隔离仍受平台能力约束。
- [x] 禁止网络、Shell、动态导入、反序列化不可信对象和目录逃逸。执行器白名单和安全表达式拒绝任意代码；不是对任意第三方库的完整安全承诺。
- [x] 执行元数据切片：`core/execution_manifest.py` 保存运行 ID、依赖版本、输入哈希、随机种子、代码/日志摘要并生成清单摘要；原始代码/日志仍留在受限产物目录，不进入对外证据包。
- [x] 根据问题规模选择精确、松弛、近似、代理或多保真路线。`BackendPlan` 和 `SurrogateModel` 已提供规模路由/域外回退；完整多保真策略仍待后续。
- [x] 输入解析前限制字节数，解析后限制节点数、深度、常量和数组规模；限制幂、广播、稠密展开及输出成本，错误不能混为成功字符串。`safe_expression`/`solver_runtime` 已有输入、AST、线和输出上限。
- [x] AST 白名单仅作为语言限制，不称为“绝对安全”；优先执行可信 IR 原语，不能直接解析不可信代码字符串。文档和运行元数据明确语言限制不等于 OS 沙箱。
- [x] 工作进程使用最小权限及净化环境，不继承 API Key；文件、网络和进程权限由适用的操作系统隔离机制约束。当前权限隔离能力不足时保守标记 `permission_isolated=false`。
- [x] 独立监督进程实施墙钟超时、内存/进程限制和取消；启动时检查隔离能力，不支持时拒绝任意生成代码，仅开放受控 IR 路线。
- [x] 运行日志与对外反馈分离切片：`core/log_safety.py` 的 `safe_diagnostic` 只保留白名单错误字段，复用敏感字段/本地路径脱敏并实施字节上限；LLM/证据边界不得直接使用原始 traceback。各历史 API 的调用点仍需逐个迁移并做端到端审计。

### 验收

- [x] 恶意题面不能读取环境变量、API Key、任意文件或启动外部进程。执行入口只接受结构化 IR/原语，生成程序先过 AST 语言白名单且 `execution_authorized=false`；Worker 不接收 `eval/exec/module/path` 参数。该保证不等于操作系统权限隔离。
- [x] 超时、内存耗尽和求解器崩溃只终止单个候选，不拖垮主服务。`SolverProcessRunner` 对 worker 施加墙钟/内存/输出/取消边界，异常子进程被回收并转为 `not_assessed`，主进程不接收 traceback。
- [x] 相同输入和版本能够复现相同执行计划与结果容差。`execution_manifest` 提供声明条件比较；数值输出仍需独立容差复核。

受限执行切片验证（2026-09-08）：`backend_planner`、`safe_expression`、`solver_runtime` 和 `solver_process_limits` 的现有回归确认受控 IR 路线、输入/输出上限、进程超时、内存限制、取消和净化环境；权限隔离在当前运行环境不提供时显式拒绝或标记，未宣称任意代码绝对安全。

## P5：模型竞赛与反例驱动修正

### 任务

- [x] 按任务建立适用的零模型、简单基线与合理竞争模型；允许候选不足，不要求固定数量。`BaselineRegistry` + `baseline_first_report` 先输出有据基线，再比较有限候选；没有候选或未注册任务时保留 `baseline_only`/`not_assessed`，不凑模型。
- [x] 统一计算拟合、复杂度、量纲、约束、稳定性、理论与成本指标。`compare_unified_candidate_metrics` 要求七目标同构、有限值与明确方向。
- [x] 保留 Pareto 前沿，不用未经校准的单一总分掩盖权衡。硬门先于 `gate_candidates`，结果只返回非支配集合。
- [x] 自动搜索边界、极端参数、扰动输入和约束冲突反例。`build_adversarial_cases` 生成有界端点、退化、中点、近边界、随机和扰动输入，需与 `run_counterexample_suite` 的模型评价器组合；生成器本身不伪造违反标签，约束冲突由评价器提供证据。
- [x] 将失败分类为语义、结构、参数、数据、数值或资源问题。`core/candidate_gate.py` 统一分类并保留“分类不等于根因证明”政策。
- [x] 实现题型无关的有界 CEGIS 循环：反例进入下一轮候选修改和重新验证；具体数学评价器仍由各后端提供。
- [x] 标量数学图切片已实现 CEGIS 闭环：历史反例追加、候选重放、旧候选失效与预算/取消状态均保留；尚未覆盖 ODE、多表和所有通用求解器。
- [x] 新增题型无关的有界 CEGIS 控制器：统一候选去重、评价/变异回调、反例摘要、修复预算和 `candidate_set_inadequate`/`budget_exhausted`/`not_assessed` 状态；回调本身仍需外部受控执行器隔离。
- [x] 为修复次数、重复候选、轮次和候选数量设置停止条件；收益阈值由评价器/模型竞争层提供。
- [x] 硬约束先过门，再对合格候选比较 Pareto 权衡；不能用更好拟合抵消量纲错误或已确认的约束违反。`core/candidate_gate.py` 提供独立硬证据门和 Pareto 入口。
- [x] 诊断先比较数值容差/步长、观测误差、参数可辨识性与结构缺失，输出证据和备选解释，不让大残差自动触发增加隐变量。`compare_residual_explanations` 只做有证据的可撤销排序，并保留待澄清问题。
- [x] 新增 `core/failure_diagnosis.py` 按数值容差→观测误差→局部可辨识性→结构缺失顺序汇总诊断，并明确大残差不会自动触发隐变量；完整跨后端数据证据仍需调用方提供。
- [x] 反例分为定义域内的规范违反、经验不匹配、数值失败和范围外行为；有限搜索无反例只记 `tested_not_falsified`，不能记为数学证明。`core/counterexample_protocol.py` 固化四类枚举与有限搜索状态。
- [x] 先运行端点、退化输入、已知可行/不可行样本和历史反例，再进行随机/优化搜索；每次评价都检查非有限值、负值和约束，保存所有找到的见证。协议对套件顺序和见证摘要做了固定化。
- [x] 最小化反例并记录输入、契约版本、违反性质、数值容差和复现步骤；修复后重放历史反例及新增确认测试。`minimize_counterexample` 提供有界缩减、局部最小性复核和重放接口。
- [x] 增加通用变形测试协议：`core/invariance_checks.py` 允许显式登记单位变换、实体重排、等价表示或步长/网格变换及比较器，失败记录为反例，有限通过只记 `tested_not_falsified`；具体任务需继续登记适用变形。
- [x] 区分普通参数扰动、情景分析与有识别假设的因果干预；不在适用域外按“违背常识”自动否定模型。`core/scenario_protocol.py` 对三类契约分开记账，因果干预缺少识别假设/设计时只能 `not_assessed`，确认也不直接授权因果结论。
- [x] CEGIS 控制器保留 `candidate_set_inadequate`、`budget_exhausted`、`not_assessed`，并禁止最后一次未检查候选晋级；`unidentifiable`/`needs_input` 由上游契约层继续传播。
- [x] 有数学证明时保存可复核的条件与证书；执行成功、经验测试与证明分别记录，不使用单个 `verified=true` 混合表达。`core/proof_certificate.py` 只接受独立检查器的显式 `pass`。

### 验收

- [x] 被反例推翻的候选不能成为最终推荐。`build_model_verdict` 对对应反例撤销推荐。
- [x] 修复后必须重新执行全部受影响证据，禁止复用失效证书。图 patch/CEGIS 记录父版本并要求重新评价，经验库作用域变化时标 stale。
- [x] 候选分数接近但结论相反时明确报告结构分歧。模型集合评估和判决层保留 decision disagreement，不压成单一分数。
- [x] 修复器不能修改测试输入、阈值或事实来源；契约合理修订须作为独立版本显式记录，不得静默规避反例。patch 生成器限制父版本/节点/字段，契约回答创建 revision。
- [x] 无反例、候选全错、最后一轮未检查、NaN/Inf、域外输入均有防止“假通过”的回归测试；有限无反例仍输出 `tested_not_falsified`。

候选硬证据门验证（2026-09-08）：新增 `core/candidate_gate.py` 与 2 项测试，硬检查先于 Pareto 比较，资源/数值等失败不会被拟合分数抵消；分类状态不自动推断根因。

## P6：四层不确定性传播

### 任务

- [x] 题意不确定性：维护多个语义解释及其证据权重。`core/semantic_uncertainty.py` 输出声明权重熵和证据引用，不冒充后验。
- [x] 初版结构不确定性：`assess_model_set` 保留 Pareto 候选、预测包络和决策分歧，不把未校准分数冒充后验；正式后验加权仍未完成。
- [x] 参数不确定性：使用 bootstrap、轮廓似然或贝叶斯后验。`bootstrap_parameter_uncertainty` 与 `profile_likelihood_uncertainty` 提供有界经验估计；没有统计假设时明确不称为后验或置信区间。
- [x] 参数不确定性初版：固定种子的有放回索引 bootstrap、分位区间和拟合失败分流；尚未声称后验或全局辨识。
- [x] 数值不确定性：记录离散化、停止容差、条件数和求解器误差界。`assess_numerical_error_budget` 校验统一单位、非负元数据和有限预算；声明性误差估计不会被升级为普适误差定理。
- [x] 数值不确定性初版：同一求解器的多容差有限比较、输出 schema/失败分流和超阈值反例；离散化误差界与条件数理论仍待补齐。
- [x] 使用全概率与全方差分解传播不确定性，不直接相加异质量纲分数。`propagate_probability_variance` 要求显式叶节点概率质量和统一单位，再复用四层条件方差账本；声明概率不是自动后验。
- [x] 初版有限层级方差传播：要求统一单位，按语义→结构→参数→数值复制层计算条件方差并报告重构残差；仍不是后验、置信区间或候选集合覆盖证明。
- [x] 初版输出候选预测包络、最坏点差异和结构分歧；预测分布与校准曲线仍需概率模型和独立校准集。
- [x] 初版独立校准检查：预测区间覆盖率/区间得分和二分类 Brier/ECE 只在外部评价样本上计算，并显式标记小样本与非校准状态。
- [x] 显式记录观测噪声与数据处理不确定性；分层分解须说明条件与交互项，不能将有相关性的分数硬相加。`assess_observation_uncertainty` 增加显式噪声方差元数据、处理分支、观测×处理交互和不平衡分解残差，禁止自动扣除或硬相加相关项。
- [x] 初版观测/处理不确定性记账：按观测实体和处理分支分解组间与组内经验方差，统一单位并保留单例 `partial` 状态；不解释为测量误差定理或后验。
- [x] 缺少概率模型或校准证据时输出候选集合与经验区间，不把排序分数归一化后称为后验概率。判决和模型集合评估均保留经验包络与非后验政策。
- [x] 候选间高度一致不等于覆盖真实机制；候选集合不足与集合内部结构不确定性分开报告。`assess_model_set`/`build_model_verdict` 显式保留 `candidate_set_inadequate` 与结构分歧。

### 验收

- [x] 在合成真值数据上检验覆盖率、Brier 分数或校准误差。`assess_synthetic_interval_calibration` 对固定种子和显式生成器执行有限样本覆盖率诊断；结果仅适用于登记的生成分布，不是现实数据覆盖证明。
- [x] 改变求解容差时，数值不确定性变化方向正确。`assess_solver_tolerance_stability(..., uncertainty_field=...)` 单独检查更紧容差不应增加报告的不确定性，并把缺失字段记为 `not_assessed`。
- [x] 单个高分模型不能消除仍有证据支持的结构不确定性。`assess_model_set` 保留 Pareto 非支配候选、预测包络和 `structural` 分解状态，不因单模型分数高而清除候选间分歧。

题意不确定性验证（2026-09-08）：新增 `core/semantic_uncertainty.py` 与 2 项测试，保留多个语义假设及声明证据权重，零证据显式降级；权重不被解释为真实后验。

## P7：信息价值主动澄清

### 任务

- [x] 从语义分叉、缺失单位、边界条件和风险偏好生成候选问题；当前问题来源与影响说明由语义契约提供。
- [x] 初版按声明候选权重和答案分区计算期望熵下降，并与启发式分歧/成本排序并列；不把声明权重称为真实后验。
- [x] 初期提供明确标注的启发式问题排序：覆盖分歧乘以未覆盖分歧，再除以声明获取成本；不冒充正式信息增益，最多返回三个问题。
- [x] 新增条件信息价值计算初版：对声明的候选权重和答案分区计算期望熵下降，保留未覆盖分支并拒绝重叠分区；结果不解释为真实后验，不自动选择答案。
- [x] 一次最多展示 2–3 个清晰选择，并说明各选择影响；保留“暂时无法确定”入口。
- [x] 用户回答写入契约来源并创建新 revision，旧证据不跨契约复用；当前采用安全全流程重算。
- [x] 同时提供明确标注的分歧/成本启发式和条件信息价值计算；二者均不自动选择答案。
- [x] 候选结构不同但决策一致时输出共同支持的最小结论；分歧会改变决策时优先询问能区分它们的问题。`build_model_verdict` 与信息价值模块保留共同结论和问题分歧。
- [x] 区分可采集新数据、可调用独立可信仿真、只有固定附件三种情形；候选自产数据不作为证明现实正确性的独立证据。`core/question_actions.py` 显式分类并拒绝把自产数据标为独立证据。
- [x] 选项不强迫用户在未经支持的机制中二选一，允许不知道、保留多假设或自由回答。

### 验收

- [x] 不询问不会改变结论的问题。问题排序现保留分歧/成本和条件信息价值字段，无法区分候选的问题不自动晋级。
- [x] 相同信息不能被换一种措辞重复询问。服务端已实现精确/近重复提示并保留审计字段；语义等价仍需人工确认。
- [x] 用户不回答时保留多假设结果，而不是擅自选择。`SemanticHypothesisSet` 与判决层均保留未选解释。

问题证据动作验证（2026-09-08）：新增 `core/question_actions.py` 与 2 项测试，澄清问题必须声明证据来源；候选自产数据降级为非独立证据。

## P8：数学结构自动发现

### 任务

- [x] 初版 Buckingham π：由单位矩阵的精确有理数零空间生成无量纲候选群和矩阵指纹；物理相关性与尺度选择仍需后续证据。
- [x] 初版守恒律候选：对有限差分导数矩阵做完整 SVD 零空间搜索，并用训练/验证时间段和轨迹指纹审计近似线性不变量；当前仅是可反驳候选，不是守恒定律证明。
- [x] 初版单调性、周期性和尺度无关信号筛查：`structure_diagnostics` 只生成带分数的搜索提示，不替代正式检验。
- [x] 初版变点信号筛查：用有界滚动窗口候选定位并保留尺度证据；分段机制仍需独立验证。
- [x] 初版隐状态候选：有界延迟嵌入 + 训练/留出 SVD 重建；结果明确标记为数学潜变量，物理命名和可辨识性仍未完成。
- [x] 初版 SINDy 风格 ODE 候选：受限多项式库、顺序阈值最小二乘、弱形式积分、留出验证和有界反例记录；噪声稳健性、单位约束和 PDE 仍未完成。
- [x] 数据与背景公理共同筛选候选，并检查域外外推。`filter_candidates_by_axioms` 要求显式数据/公理证据引用，并将域外有限反例降为候选拒绝；未支持证据保持未决，不把拟合分数当作公理支持。
- [x] 初版将周期、单调、变点和开发段残差相关/尺度诊断接入 P3 图搜索：只生成待验证搜索提示；单位、分组差异和阈值模式仍待补齐。
- [x] SINDy 初版候选库接收状态/时间维度限制，保存每个方程允许的基底；PySR 接入、先验库与较宽库独立比较仍未完成。
- [x] 初版诊断路由：把单调、周期、变点及开发段残差记忆/尺度信号转成可撤销搜索提示；提示不修改 IR、不改变硬约束，最终测试诊断不进入反馈。
- [x] 初版 CEGIS 修复路由：把已记录反例原因映射为封闭原语优先级并写入图搜索报告；未知原因不发明算子，所有候选仍需完整重验证。
- [x] 初版域外压力检查：在声明域的有界扩展上记录非有限值、输出约束反例和尺度漂移；结果只限测试点，不替代现实外推证明。
- [x] 初版隐状态竞争筛查已与观测偏差、外部输入、异方差、非平稳和函数库/数值失配并列；残差小仍不证明状态空间已闭合，完整状态搜索和观测模型竞争待后续完善。
- [x] 初版延迟嵌入/SVD 只用训练段拟合基底并在验证段评估重建；坐标默认标记为数学潜变量，未提供物理命名或可辨识性证明。
- [ ] GNN 交互预筛、UDE 修正项和完整 PDE 发现作为 P8b/P9 后期可选能力；不把交互权重直接解释为因果边。
- [x] 实现面向结论/决策的简化：评估删除变量或机制后的目标误差、可行性及极端情景影响；理论误差界与经验误差分开记录，不设固定删除比例。`core/model_simplification.py` 对完整模型与删除候选分别评估常规/压力目标，压力域无反例只标 `tested_not_falsified`。
- [x] 初版机制删减评估：对单变量/机制消融比较主目标、可行性和可选压力域损失，保留 `tested_not_falsified`/`counterexample_found`/`not_assessed`；不自动修改模型，不设固定删除比例。

### 验收

- [x] 使用未出现在提示中的合成方程验证真正发现能力；`symbolic_discovery_benchmark` 将合成轨迹与隐藏支持标签分离，报告结构恢复率和留出残差，不把夹具结果当作真实题准确率。
- [x] 结构正确率与仅看拟合误差的符号回归基线比较。未见方程基准支持配对的 `fit_only` 验证切分消融；结果隐藏真实支持标签，且明确不是独立算法比较。
- [x] 无法辨识时返回等价模型集合，不虚构唯一机制。`identify_observationally_equivalent_models` 仅在有限评估点上报告观测等价组，并明确不升级为代数或域外等价证明。
- [x] 用“无隐变量但高噪声”的对照测量隐变量误报；`assess_latent_false_positive_control` 报告固定生成器下的压缩误报率，并明确不把压缩候选解释为物理隐变量；未见初值/轨迹的动力学泛化仍需独立基准。

Buckingham π 验证（2026-09-07）：新增 `core/dimensional_analysis.py` 与 3 项测试，使用精确有理数零空间生成无量纲候选和矩阵指纹；候选仍需物理、数据和边界证据，未宣称自动发现真实机理。

守恒律候选验证（2026-09-07）：新增 `core/conservation_discovery.py` 与 3 项测试，搜索导数矩阵的线性零空间，保留完整右奇异向量以适配短序列，并用留出时间段区分候选与反例；非线性不变量、积分守恒和噪声稳健导数仍未完成。

结构诊断验证（2026-09-07）：新增 `core/structure_diagnostics.py` 与 3 项测试，提供有界单调、周期自相关和滚动变点候选信号；所有信号均标记为搜索提示，未升级为因果或机制结论。

潜状态候选验证（2026-09-07）：新增 `core/latent_state_discovery.py` 与 2 项测试，使用延迟嵌入和训练/留出 SVD 重建产生数学潜变量候选；没有把坐标命名为物理隐变量，也没有把重建误差当作可辨识性证明。

未闭合系统竞争筛查切片（2026-09-08）：新增 `core/unclosed_state_competition.py`，并接入 `diagnose_development` 的开发段检查，在开发窗口内并列报告滞后/潜状态、外部输入、异方差、非平稳和整体函数库/数值失配信号；分数明确不是概率或因果结论，并用高噪声合成对照防止自动把噪声命名为隐变量。完整 SINDy/观测模型/独立轨迹竞争仍未完成。

回归基线竞争切片（2026-09-08）：预测候选 Pareto 比较新增同一 OOF/开发切片上的均值基线；基线只用于判断复杂模型是否超过最简单解释，不读取锁定测试，也不会自动授予获准状态。

分类基线竞争切片（2026-09-08）：同一机制扩展到数值编码标签的多数类基线；非数值标签不强行编码，安全降级为无基线比较。

诊断扩展与请求护栏验证（2026-09-08）：预测 OOF/内层交叉验证、聚类审计和有序时序信号接入统一只读反馈；外部 LLM 客户端增加调用次数、提示体积和输出 token 上限；新增基线注册表与软提示路由。全量回归 `1849 passed, 5 skipped`；随后新增切片定向回归通过，未调用真实外部 API。

跨模型审计路由修复（2026-09-08）：优化、因果、排名和仿真执行审计可进入执行阶段软提示，锁定测试仍被拒绝；定向诊断/路由/研究助手回归 `48 passed`。

监督划分泄漏修复（2026-09-08）：外层确认集划分后才估计数值缺失填充值，并保存“开发集拟合”标记；避免确认段统计量反向影响开发预处理。内部交叉验证仍需后续改成折内完整流水线，不能据此宣称无泄漏证明。

时序 OOF 证据索引修复（2026-09-08）：`CrossValidator` 为每个 `CVResult` 保存精确的 `oof_mask`，明确标记实际进入验证集的行；诊断、共形区间、近优模型竞争和研究报告均优先使用该掩码，不再用折数近似推断 `TimeSeriesSplit` 的未验证前缀。兼容旧序列化结果时仍保留降级路径。新增时序交叉验证回归，建模引擎与研究助手共 73 项通过；这修复了 OOF 对齐错误，但不等于完成折内预处理流水线或时间独立性证明。

OOF 集合融合边界修复（2026-09-08）：多模型融合计算共同 OOF 掩码；时间序列未覆盖行保留形状但标记为 `NaN`，并随结果返回 `oof_mask`，避免零初始化被误当作真实预测。模型选择仍不把缺失 OOF 变成通过证据。

数值稳定性进入判决契约（2026-09-08）：`build_model_verdict` 可接收有限求解容差比较的摘要，把稳定状态标为 `tested_not_falsified`、不稳定状态标为 `counterexample_found`，仅保留计数和指纹，不复制原始求解输出；该状态不会自动批准候选，也明确不是数值误差证明。新增判决/数值稳定性回归 19 项通过；具体求解入口仍需按任务接入该摘要。

四层方差摘要接入判决契约（2026-09-08）：判决边界可接收 `propagate_layered_uncertainty` 的单位一致性和四层方差组件，只把已评估层标为 `tested_not_falsified`，保留单位、记录数和重构方差摘要，拒绝复制原始评估行；仍是经验方差记账，不是后验、置信区间或真实机制覆盖证明。研究链路已预留 `uncertainty_propagation` 接口，具体任务仍需生成该摘要。

五类最小合成基准验证（2026-09-08）：新增 `core/synthetic_bench_cases.py`、`scripts/validate_synthetic_bench.py` 和 2 项回归，固定生成 data、pure_mechanistic、multi_table、optimization、dynamics 五类案例；公开清单只携带数据指纹和可检验检查项，不复制原始 fixture。10 项基准测试通过；这些是协议/回归夹具，不是真实题库、未见题成功率或现实正确性证据。

最小数学原语签名验证（2026-09-08）：新增 `core/math_primitives.py`，提供变量、常量、方程、微分、积分、分布、事件、图、目标、约束、控制和观测的统一签名、量纲字段、输入/输出类型和待验证义务；未知原语、输入类型和输出类型不通过。新增 5 项测试通过；该层只做类型契约，不授予执行权限，也不等于完整求解器覆盖。

原语组合图验证（2026-09-08）：新增 `core/primitive_graph.py` 与 4 项测试，检查引用、重复 ID、环、等式量纲/可选形状和积分/微分等前置义务，并输出边数、依赖深度和保守成本单位供规模路由；返回 `type_checked_not_executed`，不把静态检查冒充数值求解或数学证明。动态反馈块和各后端接入仍未完成。

IR 版本信封与迁移验证（2026-09-08）：新增 `core/ir_schema.py` 与 `docs/ir_schema.md`，提供 `mathmodel.ir-envelope/v1`、四类显式 IR kind、规范化 JSON 摘要、裸 payload 显式迁移和篡改/非有限值/复杂度限制；新增 4 项测试通过。当前只提供通用完整性边界，尚未把所有历史 IR 自动改写为单一 schema。

## P9：可微数字孪生与逆问题

### 任务

- [x] 将受限可微 IR 降低到 JAX（可用时），并提供显式有限差分回退；JAX 不可用时返回 `unavailable`，不伪造自动微分结果。
- [x] 实现参数梯度、局部/全局敏感性和有限时域线性二次控制候选；有边界时只输出裁剪候选并保留独立最优性验证义务。
- [x] 使用有限差分交叉检查自动微分梯度。`core/gradient_validation.py` 对边界采用单边差分、内部采用中心差分，并将预算耗尽、非有限梯度和局部误差单独分流；通过只允许局部梯度加速，不宣称全局梯度证明。
- [x] 新增后端无关的有限差分梯度交叉检查：支持边界单侧差分、预算、非有限值和结构化误差；通过不等于全局梯度证明。
- [x] 初版局部可辨识性：有限差分 Jacobian、Fisher 近似、边界单侧差分和预算；全局唯一性、轮廓似然和噪声下稳定性仍未完成。
- [x] 初版全局敏感性筛查：固定设计上的置换总效应、方向一致性和预算/非有限分流；只用于搜索优先级，不解释为因果或全局可辨识性证明。
- [x] 对昂贵仿真建立带误差监控的代理模型与多保真策略。`core/surrogate_guard.py` 提供受限岭代理、训练残差和域外自动回退；尚未接入所有昂贵后端的多保真调度。
- [x] 网页加入假设滑块、约束状态、敏感性曲面和 Pareto 前沿。`renderResearchResult` 渲染受限 what-if 滑块、约束表、有限敏感性点和无权重 Pareto 表；滑块只生成局部重算预览，不越过完整验证门。

### 验收

- [x] 滑块变化只重算受影响的图节点。`affected_nodes_for_control` 按图下游闭包输出局部重算计划，并明确要求与全图对照；尚未把所有网页控件接入每个求解后端。
- [x] 梯度检查超限时禁止使用梯度优化结果。`gradient_optimization_gate` 只有有限差分状态为 `pass` 才允许导数加速。
- [x] 代理模型超出训练域时自动回退或显示不可信提示。`SurrogateModel.predict` 返回 `out_of_domain/fallback_required`，`predict_or_fallback` 要求调用者提供并校验全保真回退；没有回退时不产生代理结论。

梯度校验验证（2026-09-07）：新增 `core/gradient_validation.py` 与 3 项测试，覆盖内部中心差分、边界单侧差分、错误梯度、预算和非有限值分流；新增 `gradient_optimization_gate` 后只有校验通过才可使用梯度，仍未接入 JAX/PyTorch 数字孪生优化。

局部可辨识性验证（2026-09-07）：新增 `core/identifiability.py` 与 3 项测试，报告有限差分响应 Jacobian 的数值秩、奇异值和 Fisher 近似；结论限定为当前点附近的经验检查。

模型集合评估验证（2026-09-07）：新增 `core/model_set_assessment.py` 与 3 项测试，保留 Pareto 候选、预测包络、决策共识和结构分歧；未将模型排序分数解释为后验概率或置信区间。

稀疏动力学候选验证（2026-09-07）：新增 `core/sindy_discovery.py` 与 6 项测试，生成受限多项式 ODE 候选并在留出导数段复核；未把候选方程升级为真实动力学证明。

未见方程与可微 IR 验证（2026-09-09）：新增 `core/symbolic_discovery_benchmark.py` 和 `core/differentiable_ir.py`，分别用隐藏支持标签的合成方程夹具评估结构恢复、用白名单算术图执行 JAX/有限差分梯度；所有结果仍限定为合成/局部数值证据，不等于现实方程发现或全局梯度证明。

有限时域控制候选（2026-09-09）：新增 `core/optimal_control.py` 与 2 项回归，使用 Riccati 递推生成线性二次控制；边界裁剪结果明确标记为 candidate，不冒充带约束全局最优。

弱形式动力学验证（2026-09-07）：在 `core/sindy_discovery.py` 增加紧支撑测试函数的分部积分回归和留出窗口，共 4 项测试；这降低直接微分依赖，并保留有界留出反例，但不等于噪声稳健或真实 ODE 证明。

诊断路由验证（2026-09-07）：新增 `core/diagnostic_router.py` 与 4 项测试，将结构/开发残差信号映射为带证据、优先级和澄清问题的软搜索提示；锁定测试记录被明确排除，提示不具备机制或因果证明效力。

域外反证验证（2026-09-07）：新增 `core/domain_extrapolation.py` 与 8 项测试，区分声明域与扩展域，保留非有限/边界违反反例和尺度漂移信号；未将有限扩展通过升级为全局外推正确性。

全局敏感性验证（2026-09-07）：新增 `core/sensitivity_analysis.py` 与 5 项测试，覆盖参数排序、零输出变化、预算不足和非有限评估；有限置换设计不构成因果效应或全局可辨识性证明。

参数 bootstrap 验证（2026-09-07）：新增 `core/parameter_uncertainty.py` 与 7 项测试，输出条件于当前样本/拟合器的经验参数分布，并保留 schema 变化、拟合失败和成功重复不足；不把区间称为后验或置信定理。

数值稳定性验证（2026-09-07）：新增 `core/numerical_stability.py` 与 7 项测试，记录多停止容差下的输出差异、失败阶段和超阈值反例；未将有限比较升级为误差界或数值定理。

CEGIS 修复验证（2026-09-07）：新增 `core/cegis_repair.py` 与 4 项测试，图搜索报告保存反例对应的受控修复指令；指令仅影响候选顺序，不修改事实/硬约束，也不直接晋级候选。

四层不确定性传播验证（2026-09-07）：新增 `core/uncertainty_propagation.py` 与 5 项测试；统一单位后按嵌套层级计算经验方差贡献，缺失层级、权重和单位不一致均显式降级或拒绝。尚未实现概率后验、相关噪声模型或独立校准覆盖率。

校准检查验证（2026-09-07）：新增 `core/calibration.py` 与 4 项测试，独立报告预测区间覆盖率、上下界越界、interval score、Brier 分数和分箱 ECE；小样本只返回描述性结果，不声明覆盖定理或后验校准。

代理域监控验证（2026-09-08）：新增 `core/surrogate_guard.py` 与 2 项测试，代理记录训练残差并拒绝域外预测，必须由调用方提供全保真 fallback；未将近似结果称为精确仿真。

## P10：验证后经验库

### 任务

- [x] 初版用内容寻址保存脱敏后的题意契约、IR、执行计划、证书摘要和失败轨迹；配方对象与索引分离。
- [x] 初版对敏感字段、原始观测集合和本机绝对路径脱敏，禁止把原始 API Key 写入对象。
- [x] 只有通过完整复现、反例和安全测试的组合才能标记为 `verified`。`ExperienceStore.promote_verified` 强制三项布尔证据和非空 validation_scope，状态不能由写入方直接指定。
- [x] 初版相似检索使用数学图结构、单位签名和任务目标，不依赖题目文本。
- [x] 初版可迁移建模概念库：保存最小原语实现、适用/失败条件、反例和证据引用；概念检索仍只是搜索种子。
- [x] 初版检索结果明确标记为搜索种子，不能绕过当前题目的重新验证。
- [x] 初版提供按运行、主题和创建时间预览/清理临时候选的接口，并默认保护 `verified`。
- [x] 学习可检验的建模概念，保存适用证据、数学实现、不适用条件与历史反例；概念提示不等于已证规律。`ConceptLibrary` 保存这些字段，反例修订生成新 provisional 对象。
- [x] 提前允许保存隔离的临时候选和性能缓存，但不能提升为可信跨题经验；`verified` 标签必须绑定验证方法、适用域与版本。经验库/运行缓存均标记 seed/disposable，验证作用域变化会失效。
- [x] 缓存键包括 IR、契约、数据视图/划分、预处理、求解器版本、容差和种子；API 响应缓存另含模型、提示及配置版本。`core/cache_contract.py` 提供显式契约；缓存命中仍不跳过独立验证。
- [x] 运行产物采用 `runs/<run_id>/manifest.json` 及版本化 schema，分别索引 IR、证据、日志和临时缓存；配方与运行产物分离。`RunArtifactManager` 与研究助手默认按 run_id 创建目录并写入版本化清单。
- [x] 清理提供预览、配额、过期策略和共享对象引用检查；保护活跃运行，验证路径边界，脱敏清单不得包含密钥或不必要的本机路径。`RunArtifactManager.cleanup_disposable` 支持 dry-run、文件/字节配额、TTL 过期清理和运行中拒绝。

### 验收

- [x] 删除一次运行不会破坏其他共享内容寻址对象。经验库按引用计数式对象检查，运行产物仅清理精确 cache/temp；运行中默认拒绝清理。
- [x] 旧配方在依赖或 Schema 变化后自动失效并重新验证。`ExperienceStore.assess_revalidation` 对验证作用域逐字段比较，变化时只保留 seed 状态并要求重新验证。
- [x] 经验库提升搜索速度，但关闭后不改变正确性判定标准。经验条目按来源/适用域/版本隔离，`cache_consistency` 可对比缓存与冷启动结果；命中不跳过当前契约、反例和证书门。

经验库基础设施验证（2026-09-07）：新增 `core/experience_store.py` 与 5 项测试；覆盖内容地址去重、凭据/原始观测/路径脱敏、验证晋级门、结构签名检索、临时候选清理和已验证记录保护。当前经验仍只作为搜索种子，尚未接入自动晋级或跨题正确性证明。

建模概念库验证（2026-09-07）：新增 `core/concept_library.py` 与 4 项测试；概念记录要求实现、适用条件和失败条件，反例修订产生新的 provisional 内容地址，不能覆盖旧的 verified 对象。尚未实现跨任务统计迁移或自动概念学习。

缓存身份契约验证（2026-09-08）：新增 `core/cache_contract.py` 与 3 项测试，强制 IR/契约/数据视图/预处理/求解器版本/容差/种子进入中间缓存键，API 响应额外要求模型、提示和配置摘要；未将缓存命中当作数学证据。

经验重验证验证（2026-09-08）：`ExperienceStore.assess_revalidation` 与回归测试比较已验证配方的 solver/schema 等作用域，变化时返回 `stale` 并要求重新验证，不静默复用旧证书。

运行缓存保护验证（2026-09-08）：`RunArtifactManager.cleanup_disposable` 增加活动运行保护并新增回归测试；完成/失败运行才允许清理 disposable cache，证据、报告和图表不受影响。

清理配额验证（2026-09-08）：运行产物清理新增 dry-run、`max_files`/`max_bytes`、TTL 过期筛选和共享缓存索引保留，新增回归测试覆盖过期与配额。

## P11：模型判决书与交互界面

### 任务

- [x] 新增 `build_model_verdict` 机器可读判决边界，严格区分获准、条件、未决和被反证，并输出共同结论、证据引用和下一步缺口；网页展示与各类后端的完整接线仍待完成。
- [x] 展示题意—结构—参数—数值四层不确定性。`build_model_verdict` 已输出四层状态；前端接线仍需逐页面复核。
- [x] 展示结论适用范围、最小可信结论和已找到的反例。判决对象包含 `minimum_common_conclusion`、assumptions、counterexamples；缺少字段时保持未建立。
- [x] 展示假设滑块和“哪个假设会改变结论”的敏感性地图。`core/assumption_sensitivity.py` 提供有界 what-if 结果和决策变化标记，网页控件接线仍需按图表页复核。
- [x] 新增 `/api/research/sensitivity` 有界聚合入口：浏览器只能提交可信层已经产生的 base/variant evaluation，服务端不执行回调或代码；缺失评估保持 `not_assessed`，结果返回决策变化与策略说明。
- [x] 展示下一条最有价值的问题或数据采集建议。信息价值模块和 `question_actions` 已输出排序、成本、影响与证据来源；前端仍需统一展示动作类型。
- [x] 允许下载机器可读 IR、执行程序、证书、证据图和完整清单。现有运行产物下载接口加 `build_trace_bundle` 提供统一追溯包，网页新增 `/api/research/trace` 下载入口；具体后端产物覆盖仍按运行清单裁剪。
- [x] 论文 API 只能读取已批准结论，不能修改数字、假设和边界。`approved_writing_projection` 拒绝未批准候选并返回深拷贝只读投影。
- [x] 首先展示已有依据的基线与当前预算，再渐进更新竞争结果；粗筛/预览结果明确标记，不提前呈现为最终可信结论。`ProgressiveRun` 阶段状态机禁止 preview/development 直接成为 final。
- [x] 提供取消、失败阶段、下一步修复及费用/内存状态；区分执行、证据与语义三种状态，避免“已完成”掩盖验证缺失。`ProgressiveRun.public` 分离这些状态；前端状态轮询接线仍需逐页面复核。

### 验收

- [x] 用户可以从任一结论追溯到输入、模型、运行和验证证据。`build_trace_bundle` 将这些对象及缺失引用状态关联，网页提供追溯包下载。
- [x] “未知”“不可辨识”“被反证”在视觉和语义上明确区分。判决状态、诊断码和前端颜色/文案分别保留。
- [x] 报告中不存在无证据支持的确定性表述。判决层无显式 evidence 时不会输出 approved，报告保留限制与警告。

判决导出验证（2026-09-08）：新增 `core/verdict_export.py` 与 3 项测试，提供判决—输入—IR—执行—证据—清单追溯包，并将写作投影限制为显式批准且有证据引用的候选；缺失引用和无批准候选均拒绝或显式标记。

渐进结果验证（2026-09-08）：新增 `core/progressive_result.py` 与 2 项测试，强制 baseline/preview/development/confirmation/final 单调推进，取消和失败阶段可追踪，未获准候选不能发布为 final。

## P12：论文级评估与发布

### 任务

- [ ] 与当前注册表、单次 LLM、AutoML、符号回归和 MM-Agent 风格流程比较。
- [ ] 分别消融类型系统、结构搜索、反例循环、主动提问和经验库。
- [x] 报告成功率之外的校准、约束、稳定性、资源和安全指标。OpenModelBench 结果协议、校准/稳定性模块和受限运行元数据分别保留这些字段；真实大规模实验汇总仍待接入。
- [x] 公开可复现实验配置、匿名失败案例和人工评分准则。`build_ablation_manifest` 固定任务、预算、锁定测试指纹与评分标准，`record_ablation_results` 只保留状态和错误码，不写入原始 traceback。
- [x] 对“开放世界”“自动修复”“可信”等表述给出严格操作定义。`core/research_claims.py` 要求案例、预算、失败政策和独立确认，不满足则 `not_assessed`。
- [x] 发布前执行完整测试、依赖锁定、密钥扫描和许可证审计。`core/release_audit.py` 对密钥/私密文件/大文件、锁文件和许可证做有界审计；发布脚本仍需在目标环境补充依赖矩阵和人工许可复核。
- [x] `core/release_audit.py` 现在额外报告顶层依赖锁文件和许可证文件是否存在，并继续对源代码密钥/隐私文件硬阻断；它明确不是完整许可证法律意见或 Git 历史扫描。
- [ ] 首轮比较现有系统、无诊断结构搜索、诊断驱动结构搜索；使用相同后端、模型调用/数值计算预算与锁定最终测试。
- [ ] 分别消融分级淘汰、增量缓存、诊断分流、先验约束和反例重放；检查提速是否来自减少必要验证或丢弃难题。
- [x] 固定任务集合，记录超时、拒绝和未完成，不只统计成功任务耗时；报告冷/热缓存、首次有据结果、最终结果的 P50/P95 与峰值内存。`run_paired_budget_benchmark` 固定 case/repeat/seed/预算并保留失败分母，记录时延、峰值内存和调用成本；`run_cache_performance_benchmark` 另行记录 cold/warm/incremental，一律不把基准结果当现实改进。
- [x] 新增 `core/benchmark_runner.py` 固定任务协议：保留 completed/failed/timeout/not_run_budget，输出 P50/P95/max 延迟和状态计数；无独立真值时明确禁止生成成功率或准确率结论。冷/热缓存与峰值内存仍需在真实基准环境测量。
- [x] 测试前定义成功、错误接受及可接受风险差的阈值，报告配对比较和置信区间；样本不足或差异不显著时不能宣称“不增加错误率”。`core/benchmark_statistics.py` 提供预声明最小样本门槛和固定种子 bootstrap，仍需接入正式基准运行器。

### 验收

- [ ] 在冻结的公开基准和可自动判定的结构/来源留出任务上，相对当前系统报告配对效应大小与置信区间；显著性检验可报告，但 `p<0.05` 不是验收硬门槛。
- [ ] 自动修复提高有效解率，并报告错误通过率及其不确定性；风险门是否通过按预登记阈值判定，不能把未检出差异解释为证明零增加。
- [ ] 不确定性输出在独立测试集上达到预设校准标准。
- [ ] 论文结论能够由公开实验和机器可读证据复算。

发布审计切片（2026-09-08）：新增 `core/release_audit.py`、`scripts/release_audit.py` 和 2 项测试，扫描源代码中的高信号密钥/私有环境文件、生成目录和超大文件，并对测试哨兵降级为警告；当前仓库审计无源代码阻断项。完整依赖锁定、许可证审计、历史提交扫描和跨平台发布矩阵仍需人工/CI 门禁。

结构化后端与配对比较切片（2026-09-09）：新增 `core/optional_backend_runtime.py`，将 SciPy 通用注册表、受限 SymPy AST、可微 IR、CVXPY 线性规划和 Pyomo 缺失/无求解器状态统一为显式运行契约；新增 `core/comparison_protocol.py`，在预注册 arm×task 网格上保留失败分母并输出仅描述性的配对效果。295 项快速回归通过；真实外部系统、未见题统计比较和跨平台可用性仍未完成，相关 P12 勾选项保持未完成。

配对基准统计验证（2026-09-08）：新增 `core/benchmark_statistics.py` 与 2 项测试，固定案例上报告配对差、样本不足降级和可复现 bootstrap 区间；不把区间解释为显著性或零风险证明。

## 推荐实施顺序

1. N01–N02 / P0：修复评价基础，保存基线，锁定最终测试。
2. N03–N04 / P1：分离事实与假设，加入未知机制和观测模型。
3. P2 最小切片 + N05 / P4：复用已有原语，补齐契约与强制资源监督。
4. N06 / P8a：轻量诊断提前，不等待完整符号发现后端。
5. N07 / P3 + P5：实现结构/参数双层搜索、诊断分流与反例重放。
6. N08：落实下方效率专项的基础项，临时候选缓存保持非认证状态。
7. N09 / P12 最小切片 + P11 最小界面：固定预算验证闭环收益，展示可追溯结果。
8. P6 + P7：扩充不确定性校准与决策导向主动澄清。
9. P8b + P9：按基准失败归因引入深层符号发现、PDE、可微数字孪生和混合模型。
10. P10 可信经验 + P11 完整界面 + P12 完整评估：跨题复用、清理管理和研究发布。

安全执行层先于开放式代码执行；轻量诊断先于昂贵结构搜索；可信经验晚于验证体系。MCTS、RL、GNN、完整 Decapodes 后端和形式化证明均不作为首个有效闭环的前置条件。

## 效率与质量专项：少做无效计算，不降低验证要求

以下是待实验验证的设计，不预先承诺倍数提速。每项都比较固定预算下的有效结果与错误接受，而不是只比较运行速度。

### E1：分级筛选与自适应预算

- [x] 新增后端无关的 `run_staged_evaluation` 调度器，支持 Schema/类型/低成本拟合/高保真/独立确认阶段，预留确认预算并输出逐候选审计记录；静态无效候选不占用后续数值预算。当前仍需把它接入各个具体求解后端。
- [x] 同一任务和可比较保真度内实施逐轮晋级；先检查低/高保真排名相关性，保留探索配额，避免粗网格、短轨迹和稀有事件遗漏导致错误淘汰。`plan_progressive_promotion` 记录低/高保真重叠和 Spearman 状态，保留探索配额。
- [x] 缓慢收敛或高噪声候选设置最小评估预算和复核机会；“因预算未晋级”不能记作“被反例推翻”。晋级计划把未达到最小评估次数或无有效分数的候选列为 `unresolved_ids` 并继续保留。
- [x] 每次运行预留最终确认和反例测试预算；低保真成绩只能用于搜索，最终获准结果必须执行适用的完整检查。`reserve_confirmation_budgets` 在运行前同时锁定两类预算并在不足时拒绝；实际求解与反例套件仍必须回填证据。
- [x] 使用全局截止时间及模型/数值调用配额，逐级下传剩余预算；提前停止搜索时保留已确认的基线，不伪造更优结果。CEGIS/分阶段执行器均保留预算耗尽状态与已确认基线，不把预算截断当反例。

### E2：增量编译、去重与计算复用

- [x] 新增 `IncrementalCompileCache`：按节点定义、上游键、编译器版本、来源快照和适用域签名复用未变中间子图；上游变化自动使后继失效，缓存不保存最终判决。当前尚未接入全部求解后端，也未实现隐式耦合块的专用分区策略。
- [x] 仅对有条件等价依据的表示去重；不得因字符串相似、近似拟合或未经证明的浮点重排合并不同候选。`IncrementalCompileCache` 对 `equivalence_key` 强制要求 `proved` 等价证书。
- [x] 复用数据视图、训练段特征库、稀疏结构和满足键一致性的分解；结构改变后先验证参数映射，再尝试 warm start，并保留冷启动对照。`build_reuse_plan` 固定四类指纹和参数映射门，结构变化拒绝 warm-start，要求冷启动对照；只规划中间量，不复用最终结论。
- [x] 确定性类型错误可缓存；超时、网络失败、资源不足和预算内未找到反例不能缓存为永久数学失败或永久有效证书。`classify_cache_failure` 固化可缓存类型错误与不可缓存瞬时/未完成状态。
- [x] 缓存命中仍执行来源、版本与适用域检查；没有完整证据键时只能复用计算中间量，不能复用结论等级。`validate_cache_hit` 检查来源、版本、域、数据视图和预处理指纹，并在结论复用时拒绝缺证据键。

### E3：按数学结构减少求解维数

- [x] 标量图局部拟合增加至多三个训练专用确定性起点；固定训练残差阈值早停、记录全部尝试，预留开发检查及逐点回退次数。零梯度与初值越界可恢复；耗尽预算不当作数学反例。此切片不包含全局优化、线性子块消元或物理可辨识性证明。
- [x] 对可由图语法证明“全部参数仿射”的标量候选构造训练设计矩阵并执行有界线性最小二乘，记录秩、预算和原始参数；独立检查仍完整执行。混合线性/非线性参数子块的 variable projection 尚未实现。
- [x] 对可保守识别的混合参数图加入变量投影：内层有界线性最小二乘、外层有界局部搜索，逐次投影的模型评估计入统一预算；参数乘积/分母/非线性冲突自动回退。当前只保留确定性贪心线性子集，未声称全局结构搜索或参数唯一性。

- [x] 识别参数中的线性子块，适用时以稳定的线性最小二乘消去，仅对剩余参数做非线性搜索；记录秩亏和可辨识性。`variable_projection_search` 对有界非线性候选执行线性块投影，保留秩、条件数和残差；有限网格诊断不冒充全局可辨识性。
- [x] 优先利用稀疏性、可分离性、块结构和已证对称性；不能把删除约束或忽略弱耦合当成无误差优化。`analyze_design_structure` 只生成稀疏存储、分块和经过容差审计的对称复用计划，强制保留完整问题冷启动对照，不自动删除约束或弱耦合。
- [x] 对尺度差异大的变量使用可逆尺度变换或已知单位下的无量纲化，保存回变换并复核原始单位下的残差和约束。`core/reversible_scaling.py` 保留单位签名、中心/尺度、常量列和 round-trip 审计；不会把不同单位强行互换。
- [x] 依据条件数、刚性及事件结构选择后端；梯度可信时才使用导数加速，敏感节点以独立差分或其他方法复核。`select_numerical_strategy` 已接入 `plan_backend` 的资源预算，缺失诊断保持 `not_assessed`，未经验证的梯度选择独立差分复核。
- [x] 降阶、代理和近似模型附误差监控；超出有效范围回退或报告未确认，不把快近似标成精确解。`SurrogateModel` 返回训练残差、域外状态和全保真回退要求；该能力仍不等于所有后端已自动接入。

### E4：大数据与内存感知调度

- [x] 网页 CSV/XLSX/Parquet 上传增加流式只读、确定性覆盖读取：内存中最多保留 100,000 行，原始文件不丢失，source_rows 与非完整表示显式传递；合并/关联拒绝受限预览，避免静默产生错误全量结论。超过阈值的旧式 XLS 明确拒绝并提示转换格式。

- [x] 核心 `DataLoader.iter_chunks` 与 `DataModule.stream_profile` 支持 CSV/TSV/TXT/XLSX/Parquet 惰性分块；画像精确聚合行数、缺失数和数值充分统计量，对唯一值超限返回显式下界，并声明分位数、相关系数和全局重复数仍需完整扫描。

- [x] `DataLoader.load(..., materialize=False)`、`load_chunked(..., materialize=False)` 与 `DataModule.stream_chunks` 提供向后兼容的真正惰性入口；默认旧行为不变，避免把“分块读取后再 concat”误当成内存有界。
- [x] 增加 `DataLoader/DataModule.stream_reduce`：按块维护调用方充分统计状态，支持行数/时间预算并区分完整扫描与提前停止；不替代需要全局排序、精确关联或全量矩阵的外存算法。

- [x] 按任务投影所需列、分块读取和计算充分统计量；需要全局排序或跨块状态的算法使用明确的外存/状态协议，不能假装可逐块独立求解。`plan_external_state_operation` 要求排序/联表/窗口声明状态协议并固定列投影；已有流式统计与有界联表执行器仍按预算拒绝。
- [x] 联表先估算基数并检查实体/时间键；爆炸风险下提供有语义依据的聚合候选，不擅自先聚合改变问题。`estimate_join_cardinality` 根据键频数给出多对多上界，超限返回 `needs_semantic_aggregation`，不自动聚合。
- [x] 参数化控制样本、候选基底数量、稀疏矩阵非零元和成对操作规模，禁止无提示构造全量 O(n²) 距离或交互矩阵。`core/resource_scheduler.py` 在分配前显式估算资源并拒绝超限工作；后端仍需逐一接入准入调用。
- [x] 工作队列同时限制总内存、候选并发、BLAS/求解器线程和 API 速率，避免嵌套并行；不按 CPU 核数盲目放开所有候选。`BoundedWorkQueue` 增加原生线程预约与 API 时间窗口，超限显式拒绝；进程级资源仍需 `solver_runtime` 独立监督。
- [x] 按任务/运行清理临时对象，限制日志及图表采样；交互预览保留异常和稀有组，抽样不能替代最终评价。`RunArtifactManager` 限定 cache/temp 清理，`sample_preview_rows` 固定上限并保留稀有组/异常记录，预览结果显式禁止替代最终评价。
- [x] 保存型可视化在写盘后立即关闭 pyplot figure，避免批量研究图表累积导致内存增长；无 `save_path` 时仍返回开放句柄，由调用方负责关闭。

E4 流式切片验证（2026-09-07）：`tests/test_data_module.py`、`tests/test_data_quality.py` 与 `tests/test_research_web_api.py` 共 50 项通过。覆盖 CSV/Parquet/XLSX 惰性迭代、编码边界、工作表与列投影、数值充分统计量合并、有界唯一值下界、日期类型保护、deferred 质量检查，以及网页对受限预览的源文件质量回读和自动修复拒绝。尚未勾选整项“按任务投影/外存协议”，因为排序、关联和跨块算法仍需独立实现与基准。

输入级内存护栏验证（2026-09-07）：`MathModelingAssistant` 新增 `max_input_rows`/`max_input_memory_mb`，对直接传入的大型 DataFrame 执行确定性均匀跨度采样并保留 `source_rows`、采样策略和不完整警告；新增 2 项回归测试。该护栏不替代需要全量数据的外存排序、关联或统计协议。

凸二次规划原语验证（2026-09-09）：`PrimitiveRuntime` 新增受限 `quadratic_program` 原语，支持有限维对称半正定二次目标、线性不等式和显式边界；对非凸目标返回 `not_assessed`，对不可行/未收敛运行返回 `not_converged`，并输出目标值、约束违反量、迭代次数和凸性证据。拒绝空维度、非法无穷边界及非有限输入，不接受用户回调或代码字符串。`tests/test_primitive_runtime.py`、`tests/test_universal_math_solvers.py`、`tests/test_solver_runtime.py` 共 85 项通过；这只完成连续凸二次规划原语切片，不代表整数、非凸、多目标或现实约束已通用解决。

二次规划受控执行接线（2026-09-09）：将已注册的 `quadratic_program/v1` 纳入 `backend_planner`、`SolverProcessRunner` 和显式 worker allow-list；进程 worker 重新通过统一注册表执行，不接受任意执行器键。新增 worker 回归测试并与线性 ODE/事件原语一并验证，相关规划、进程和注册表测试共 88 项通过。该接线只解决资源监督边界，不增加二次规划模型的语义或全局正确性。

未决机制 IR 切片（2026-09-09）：`math_primitives`/`primitive_graph` 新增非执行型 `unknown_mechanism`、`latent_state`、`regime_switch` 原语，分别要求搜索预算/候选语言、潜在状态识别策略、事件边界语义。图验证会检查这些义务但不会把它们升级为公式或执行权限；新增原语与图测试 9 项通过。该切片解决“表示不了未知机制”的问题，不声称已经完成隐变量发现或机制切换求解。

候选图合成切片（2026-09-09）：`structure_candidates` 为每个领域无关候选附加 JSON primitive graph 草案，并在输出前执行引用、拓扑、量纲和未决机制义务检查；所有候选保持 `type_checked_not_executed`，不会因为图合法就放行数值执行。新增候选回归断言图状态和无环性，相关测试通过。该切片实现“从原语组合结构骨架”，尚未实现模型/API 生成、参数绑定或 CEGIS 搜索。

CEGIS 反例归档切片（2026-09-09）：`run_cegis` 为每轮候选记录 `parent_hash`，维护有界、脱敏的反例归档并传给后续变异器；候选内容、原始堆栈和评价器输出不进入反馈。归档超限显式记录丢弃数量，父候选通过状态不会继承给子候选。新增 lineage/归档/预算回归，`tests/test_cegis_controller.py` 4 项通过。该切片仍依赖调用方提供受控 evaluator/mutator，不等于已经完成模型生成或操作系统沙箱。

分级竞争接线（2026-09-09）：新增 `compete_models_staged`，把低成本筛选、最终确认和 Pareto 比较串联；只有最终阶段幸存候选进入比较，被剪枝、预算耗尽或执行错误的候选保持 `not_assessed`。新增 2 项模型竞争回归，与原分级评价共 13 项通过。该适配器统一调度策略，不替代求解器、留出集或独立证据门。

增量缓存一致性切片（2026-09-09）：`IncrementalCompileCache.compile_with_consistency` 新增冷缓存/当前缓存对照入口，只比较稳定中间产物、节点键和显式来源依赖，明确忽略命中遥测且不缓存判决；漂移返回 `counterexample`，不静默采用缓存结果。新增命中一致性与中间量漂移回归，缓存相关测试共 10 项通过。该切片尚未接入所有求解后端，也不替代最终数值验证。

等价重写图增强（2026-09-09）：`EquivalenceGraph` 支持显式 `source_hash` 链式重写，并输出仅由可执行边合并的 `verified_equivalence_classes`；缺少非零域、唯一解或积分常数条件的提议边仍保留但不会用于候选去重。新增链式来源、未知源拒绝和未证边隔离测试，与图搜索回归共 72 项通过。该功能只提供代数形式溯源，不等同于形式化证明或现实模型等价。

重写图输入护栏（2026-09-09）：等价表达式解析新增 32 层递归深度、元数据长度/类型、有限数值和 `allow_nan=False` 摘要检查，并将常量规范化，避免畸形题面造成栈耗尽或不稳定哈希。新增元数据与深度边界回归；等价图/搜索测试共 73 项通过。该护栏属于输入与指纹安全，不增加等价规则覆盖或形式化证明能力。

候选指标模式闸门（2026-09-09）：`candidate_gate` 现在先记录每个候选的完整指标模式，再进行 Pareto 比较；当候选缺少指标或混用不同指标集合时，统一标为 `not_assessed` 并记录 `metric_schema_incomplete`，不再把不可比候选静默放入 Pareto 集。输出增加 `metric_schema_groups` 与 `incomparable_metric_schema_ids`，同模式候选仍按原方向执行比较。新增模式冲突/同模式回归 2 项，候选闸门与模型竞争测试共 15 项通过。该切片只防止错误比较，不替代指标语义、校准和独立验证。

候选身份一致性（2026-09-09）：候选闸门在进入指标和 Pareto 计算前拒绝重复或仅由空白区分的候选 ID，避免结果覆盖、血缘指向歧义和错误去重。新增重复 ID 回归；相关候选闸门测试共 14 项通过。

假设敏感性输入护栏（2026-09-09）：`assess_assumption_sensitivity` 新增变体预算、唯一 ID、覆盖键数量/名称和 evaluator 返回类型检查；参考评估或单个变体返回非映射对象时保留为 `not_assessed`，不让畸形回调异常穿透研究链。新增回调失败与重复/非法变体回归，相关测试通过。该切片不把有限 what-if 评估升级为全局敏感性或因果效应。

语义假设证据输入护栏（2026-09-09）：`assess_semantic_hypotheses` 现在拒绝重复（含空白差异）假设 ID 和字符串伪装的证据引用，引用统一去空白并限量；权重仍只是声明的证据分数，不被改写为后验概率。新增输入回归，相关测试 3 项通过。

不变性测试预算护栏（2026-09-09）：`run_invariance_checks` 增加 1–256 个变换用例的硬预算、唯一且非空 ID 检查；无限生成器或重复变换会明确失败，不再允许交互任务因无限元变换卡死。单个变换的执行错误仍记录为 `execution_failed`，不会伪装成通过。新增 2 项回归。

竞争层局部失败隔离（2026-09-09）：`compete_models` 在调用集合比较器前逐候选检查预测是否为有限一维向量、四个必需指标是否存在且为非负有限数，以及预测长度是否一致；坏候选被保留为 `not_assessed` 并附失败原因，其余候选继续进入 Pareto/判决流程。新增缺指标和长度冲突回归，竞争测试共 10 项通过，避免单个畸形候选拖垮整个候选集。

缓存一致性遍历护栏（2026-09-09）：`compare_cached_uncached` 增加比较节点/递归深度预算、循环引用检测和 `ignored_fields` 类型校验；缓存结果中的循环对象不再导致递归耗尽，而是返回 `counterexample`。新增循环输出与预算回归，缓存一致性测试共 4 项通过。

渐进结果状态机硬化（2026-09-09）：`ProgressiveRun` 统一候选/失败阶段 ID 的空白规范化，禁止取消后继续选择候选；最终阶段可复用当前已批准证据状态，不要求重复传参，同时取消阶段必须非空。新增状态机回归 2 项，避免 UI 重复提交和状态覆盖。

研究声明证据校验（2026-09-09）：`operationalize_claim` 不再把任意 truthy 值当成“已操作化”证据；现在检查测试案例数、非空预算、失败策略文本和布尔独立确认，错误输入返回 `not_assessed` 并列出 `invalid` 字段。新增 1 项回归。

量纲分析输入规范化（2026-09-09）：Buckingham-π 入口增加数量名称、基准量纲名称的非空/长度/唯一性校验，避免畸形键在精确零空间计算或摘要哈希阶段抛出原生异常。新增 2 个输入回归；仍明确输出候选无量纲组而非物理有效性证明。

图语义输入护栏（2026-09-09）：`validate_graph_semantics` 增加节点/边预算、真实字符串端点、空白规范化、重复边、关系长度和严格布尔 `feedback` 校验；`"false"` 等字符串不再被当成因果反馈标记。新增 3 个回归，避免畸形图进入拓扑判定。

主动澄清信息值护栏（2026-09-09）：`estimate_question_information_gain` 增加问题预算、假设 ID 规范化/去重、非空问题与答案分支、字符串假设引用校验，避免不可哈希对象或空白 ID 破坏分区计算。新增 3 个回归；信息增益仍是声明权重下的启发式，不是后验真值。

验收率分母修正（2026-09-09）：`assess_acceptance_rates` 对 case ID 空白规范化并去重，拒绝把缺失 ID、非映射结果和非布尔真值悄悄纳入分母，新增 `malformed_result_count`、`missing_case_id_count` 与 `invalid_ground_truth_count`。新增重复/畸形结果回归，避免公开基准的错误接受率被重复记录污染。

配对基准统计资源护栏（2026-09-09）：`paired_benchmark_effect` 校验基准/处理列名、随机种子、最小样本和 bootstrap 次数，并限制样本量及 bootstrap 矩阵总元素，超限明确拒绝而不触发大内存分配。新增 5 个契约/资源回归；置信区间仍是有限重采样描述，不是显著性证明。

未见结构变换替换安全（2026-09-09）：`transform_statement` 的符号/单位替换改用字面量回调，不再把新符号中的 `\1` 等内容解释成正则回溯引用，防止变换题生成损坏题面或抛出替换异常。新增字面量替换回归。

开放基准协议硬化（2026-09-09）：`OpenModelBench` 创建清单时采用 10,000 案例的惰性上限，避免先完整物化无限生成器；汇总与多方法对照拒绝非对象结果、非失败状态携带失败阶段和篡改的指标值。新增生成器上限/汇总篡改回归，开放基准测试共 11 项通过。

固定基准执行隔离（2026-09-09）：`run_fixed_benchmark` 先规范化并校验全部案例 ID，避免时间预算触发时读取未校验的后续案例；evaluator 返回空状态、非有限分数或非法内存值时仅将当前重复标为 `failed`，后续案例继续执行。新增 1 项回归。

可选后端发现稳定性（2026-09-09）：`discover_optional_backends` 拒绝重复/非字符串请求，并将导入元数据异常降级为不可用发现结果；发现层仍显式保持 `execution_authorized=False`。新增 2 项输入回归。

全局敏感性参数护栏（2026-09-09）：`assess_global_sensitivity` 规范化并去重参数名，拒绝布尔边界和超长名称，避免 Python 中 `bool` 作为数值边界造成假敏感性结果。新增 2 项回归；有限置换筛选仍不宣称因果效应或全局可辨识性。

参数不确定性输出护栏（2026-09-09）：bootstrap 拟合结果现在要求字符串参数名、空白规范化和唯一性，避免通过 `str()` 强转不同键后读取错误参数值；歧义输出保留为失败重复而不是伪造稳定性摘要。新增 1 项回归。

失败诊断与路由输入护栏（2026-09-09）：`diagnose_failure` 拒绝非有限残差和超量诊断信号；`diagnostic_router` 对证据执行有限 JSON/32KB 检查，严格验证信号、主题和诊断代码类型，仍只产生软提示，不修改 IR 或硬约束。新增 3 项回归。

经验库脱敏递归护栏（2026-09-09）：`redact_payload` 增加 32 层/100,000 节点/单容器 10,000 项限制、循环引用检测和严格字段名校验，畸形经验在写入 SQLite/对象文件前明确拒绝；原有密钥、原始数据和本地路径脱敏策略保持不变。新增循环与深度回归，经验库测试 7 项通过。

概念库复用边界（2026-09-09）：概念实现的文本列表统一去空白，重复反例 ID、字符串伪装的搜索签名和非字符串反例 ID 均拒绝；修订仍生成新的 provisional 种子，不会修改已验证对象。新增 1 组输入回归。

资源准入类型护栏（2026-09-09）：`estimate_workload` 严格要求布尔 `pairwise`、正的候选数和字节单元；`admit_workload` 拒绝负数/字符串强转和错误预算对象，统一使用规范化整数估计，不静默抽样或缩小任务。资源调度测试 3 项通过。

本轮完整回归（2026-09-09）：`python -m pytest -q` 得到 `1978 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 691.37 秒。新增二次规划边界/worker 路由与未决机制原语均通过；警告类别未扩大，仍主要来自第三方依赖、模型收敛、字体和可视化弃用提示。该结果是当前工作树回归，不等于声明依赖版本矩阵、Linux 隔离或真实题库正确率认证。

本轮后完整回归（2026-09-09）：在候选指标模式、竞争层局部失败隔离、缓存/图/量纲/信息值/基准协议等护栏继续扩展后，`python -m pytest -q` 得到 `2004 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 407.25 秒。新增警告未出现；现有警告仍主要来自第三方依赖版本、模型收敛、字体和可视化弃用。该结果只说明当前环境下测试通过，不等于所有外部 API、操作系统沙箱和未见题正确率已认证。

资源准入验证（2026-09-08）：新增 `core/resource_scheduler.py` 与 2 项测试，按行列、候选数、成对单元和估计内存做前置准入；超限明确标为资源拒绝，不当作数学反例、不隐式抽样。

分阶段执行取消与总时限护栏（2026-09-09）：`run_staged_evaluation` 新增总墙钟预算和 `threading.Event` 取消协议；取消/截止时间/评估器异常/预算耗尽均保留为 `unresolved`，不会被降级成反例。评估器返回的 `evaluations_used` 现在必须是有界整数，拒绝字符串强转和越界记账，新增 3 项回归。该协议只负责调度与资源语义，不能替代候选模型的独立数学验证。

竞争层取消传播（2026-09-09）：`compete_models_staged` 将总墙钟预算和取消事件向下传递到分阶段执行器；取消后不启动评估器，也不会把空的最终阶段误报为已批准模型。新增竞争层回归，确保调度状态与数学判决状态分离。

子进程回收兜底（2026-09-09）：受控求解器在超时/取消/输出超限后的清理阶段增加二次强制终止和有限回收等待；即使异常子进程不响应第一次 `kill`，也不会无限阻塞请求线程或长期占用并发槽位，并记录内部清理警告而不发布结果。

后端规模估计拒绝静默强转（2026-09-09）：`backend_planner` 不再把字符串/浮点规模字段强转成整数；非法标量或矩阵结构会产生保守的超预算估计并延后执行，避免恶意/畸形契约因估计为零而进入求解器。新增 2 项回归，路由计划仍只是执行建议，不替代契约验证。

产物与缓存契约硬化（2026-09-09）：`RunArtifactManager` 拒绝 `artifact_id`、`required`、`disposable` 和 TTL 的隐式类型强转；缓存 TTL 限制在一年内，错误输入不会生成不可清理或错误归类的文件。新增 1 组回归，持久化产物仍按 cache/temp 与 durable 目录隔离。

本轮完整回归（2026-09-09）：`python -m pytest -q` 得到 `2026 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 425.26 秒。警告数量和类别未因本轮改动扩大，主要仍来自第三方依赖、模型收敛、字体和可视化弃用提示；该结果不等于外部 API、操作系统矩阵或真实未见题正确率认证。

大文件物化上限（2026-09-09）：`DataLoader.load/load_chunked` 新增默认 2,000,000 行物化上限；超过上限明确抛出并提示 `materialize=False`/`stream_reduce`，惰性读取不受该上限影响，避免“分块读取”最后仍把全表和拼接副本同时留在内存。新增大文件行上限回归。

通用有界工作队列（2026-09-09）：新增 `core/work_queue.py` 的 `BoundedWorkQueue`，按待处理任务数和声明内存做原子准入，任务成功、异常、取消 pending 和提交失败都会释放预留；关闭后禁止新任务。该层是调度护栏，不替代数值子进程隔离；新增 3 项并发/异常/类型回归。

跨块关联与充分统计量（2026-09-09）：新增 `core/streaming_operations.py`；有界哈希关联保留多对多行但在右表物化或输出超限时明确拒绝，数值分组统计使用在线 Welford 状态而不保存全量行。新增 3 项跨块/预算回归，避免受限预览或静默抽样产生伪关系。

文件级有界关联入口（2026-09-09）：`DataLoader.bounded_join` 将 CSV/TSV/XLSX/Parquet 的惰性块读取接入有界哈希关联，调用方必须显式声明键、右表行上限和输出上限；多对多结果完整保留，超限明确失败。新增文件级关联回归。

求解器聚合内存准入（2026-09-09）：`SolverProcessRunner` 在并发槽位之外增加进程级声明内存预留，多个候选不会同时超过 2048 MB 的聚合预算；等待预算耗尽返回 `queue_timeout`，取消可打断等待，且所有退出路径释放预留。新增“内存已占满时不启动 worker”回归。

本轮完整回归（2026-09-09）：新增队列、跨块关联、文件级有界 join 和聚合内存准入后，`python -m pytest -q` 得到 `2035 passed, 5 skipped, 204 warnings, 8 subtests passed`，耗时约 410.76 秒。新增警告仅为第三方/环境提示，未出现测试失败。

本轮全量回归（2026-09-09）：加入反例协议、模型 API 重试预算、执行清单兼容性、证明证书、原语非有限输入和安全表达式除零归因后，`python -m pytest -q` 得到 `2049 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 423.20 秒。警告仍来自第三方依赖、收敛、字体和可视化弃用提示；该结果不等于外部服务或未见题正确率认证。

本轮全量回归（2026-09-09）：加入生成程序 AST 白名单、可替换提议后端路由、反例初始判定去重及候选预算护栏后，`python -m pytest -q` 得到 `2060 passed, 5 skipped, 204 warnings, 8 subtests passed`，耗时约 416.23 秒。未出现测试失败；警告仍为第三方依赖、模型收敛、字体和可视化弃用提示。

四层不确定性预算校验（2026-09-09）：`propagate_layered_uncertainty` 先严格验证记录上限，再计算层级方差，避免非法预算在长度比较阶段抛出原生类型异常；仍明确区分经验方差分解与概率/置信证明。新增预算输入回归。

CEGIS 反例局部最小化（2026-09-09）：`cegis_controller.minimize_counterexample` 新增有界 shrinker 搜索；只接受仍复现违反且复杂度更低的候选，记录测试步数与局部最小状态，异常/非布尔判定保持 `not_assessed`。输出明确不是全局最小性证明；新增 2 项回归。

反例分类与有序重放协议（2026-09-09）：新增 `core/counterexample_protocol.py`，要求反例显式标记为定义域内规范违反、经验不匹配、数值失败或域外行为，并校验对应证据字段；新增端点→退化→已知可行/不可行→历史→随机→优化的有界测试顺序。历史反例重放时回调异常保留为 `not_assessed`，有限无反例只输出 `tested_not_falsified`，不会伪装成数学证明。新增 4 项分类、排序和回放回归。

外部模型请求预算（2026-09-09）：`SemanticCompilerConfig`/`HttpSemanticBackend` 新增显式重试次数、指数退避和请求间隔；只对超时、连接错误及 408/429/5xx 重试，重定向、4xx 和响应解析错误不重试，所有尝试仍受同一响应上限。默认重试为 0，避免改变既有离线/测试行为；新增瞬态失败重试与预算类型回归。

执行复现前置条件（2026-09-09）：`execution_manifest` 新增 `compare_execution_manifests`，比较输入哈希、随机种子、依赖版本、代码摘要和日志摘要，忽略运行 ID；结果明确标为执行条件兼容检查，不把相同清单升级为输出等价证明。新增 1 项同条件/输入变化回归。

基础数值原语输入护栏（2026-09-09）：`PrimitiveRuntime` 的线性系统、最小二乘、积分和二次规划入口现在在调用 NumPy/SciPy 前拒绝空矩阵、非有限值、非法迭代预算和非有限多项式系数；新增边界回归，避免底层求解器异常泄漏或把 `NaN` 结果标为执行成功。

数学证书与经验证据分离（2026-09-09）：新增 `core/proof_certificate.py`；只有外部检查器明确返回 `pass` 且声明了假设、证明步骤、版本和作用域时才生成 `deductively_verified`，否则为 `not_assessed`。证书摘要可独立验完整性，但不会重新执行检查器，也不把数值执行/有限反例搜索当作证明。新增 2 项完整性与缺失条件回归。

表达式数值失败归因（2026-09-09）：`SafeNumericExpression` 将除零统一转为有界的 `FloatingPointError`，与指数溢出和定义域错误一致，不向上层泄漏原生除零异常；新增回归。该语言限制仍不是操作系统级沙箱，生成代码必须走受控 worker。

候选集合预算校验（2026-09-09）：`assess_model_set` 先验证候选数/预测点预算类型和范围，再做长度比较，避免字符串或布尔预算触发原生类型异常；新增 2 项回归，候选比较仍保留 Pareto 与决策分歧而不转成后验。

生成程序 AST 白名单（2026-09-09）：新增 `core/safe_code.py`，只接受受限的数值赋值/表达式，拒绝导入、属性、索引、循环、动态调用、未知输出和非有限常量；返回 AST 摘要但保持 `execution_authorized=False`，必须继续经受控 worker 的资源隔离。新增 7 项恶意语法/合法语法回归；该层不宣称操作系统级安全。

可替换模型提议路由（2026-09-09）：新增 `core/proposal_router.py`，按声明的合法率、修复收益、时延和费用做确定性排序，在总调用预算内尝试本地/外部后端；所有响应必须经过同一个显式 validator，后端异常、拒绝和未评估均保留在尝试记录中，不得单独授权模型输出。新增 3 项排序、失败隔离和预算回归。

反例最小化确定性修正（2026-09-09）：`minimize_counterexample` 不再重复调用初始违反判定，避免有副作用或非确定 evaluator 导致局部最小化结果漂移；新增单次调用回归。

语义后端配置类型收紧（2026-09-09）：模型 provider、模型名和输出 token 预算不再通过 `str()`/`int()` 静默强转；非法配置在发起网络请求前明确拒绝，保留离线后端的确定性行为。

通用几何/统计原语与受监督 worker 接线（2026-09-09）：新增距离、线段相交、区域成员、视线、区间并集、正态对数似然、bootstrap 和置换检验的类型化 validator、统一注册表和显式 worker 白名单；固定种子重放、规模预算和非有限输入均有回归。完整回归 `python -m pytest -q`：`2075 passed, 5 skipped, 204 warnings, 8 subtests passed`，耗时约 403.04 秒。

跨域类比契约（2026-09-09）：新增 `core/analogy_mapping.py`，强制变量/单位/交互/守恒/边界映射，确认只改变提议状态而不授予执行权限；新增 6 项回归与文档。该模块仍需要目标题目的独立类型、数据和反例验证。

修复路由策略（2026-09-09）：新增 `core/repair_router.py`，确定性类型/单位/容差/资源问题不调用模型，新机制或语义分歧才允许进入受验证提议路由，未知错误转为 `needs_input`；上下文只保存有限指纹且不授予执行权限。

本轮全量回归（2026-09-09）：加入基线优先报告、情景/因果干预分类、修复路由和非有限统计结果护栏后，`python -m pytest -q` 得到 `2082 passed, 5 skipped, 203 warnings, 8 subtests passed`，耗时约 360.65 秒。新增代码未引入测试失败；警告仍来自依赖、模型收敛、字体和可视化弃用提示。

候选硬预算扩展（2026-09-09）：`CEGISConfig` 增加墙钟与 `cost_units` 预算，超过预算的评估保留 `not_assessed` 并禁止晋级；新增预算类型、超支和回归测试。

数值误差元数据与批量评价（2026-09-09）：`assess_numerical_error_budget` 记录统一单位下的离散化误差、停止容差、条件数和求解器误差界声明；`evaluate_candidate_batch` 复用结构相近候选的共享特征并限制反馈/重试预算。快速回归 `scripts/quick_regression.py` 为 149 项通过；全量 `python -m pytest -q` 为 `2104 passed, 5 skipped, 206 warnings, 8 subtests passed`（约 349 秒）。这些仍是有限契约测试，不构成未见题成功率或误差定理。

参数/运行一致性与降维切片（2026-09-09）：新增 profile-likelihood level-set、可逆单位签名尺度变换、输入快照 compare-and-swap 提交门和有限变量投影；均保留失败、秩亏、条件数和适用边界，不将有限网格结果升级为全局证明。

数学原语族覆盖登记（2026-09-09）：通用注册表与原语运行时已覆盖有限代数、积分/线性动力学/事件、概率统计、LP/MILP/QP/多目标/鲁棒/随机/动态规划以及路径/流/匹配/Markov 契约；不支持的边值、PDE、最优控制和时变扩散仍显式拒绝，未把能力描述当作完整求解器。

公开题源目录回归（2026-09-09）：`tests/test_public_benchmark_catalog.py` 及封存/开放基准相关测试通过；快速回归 `153 passed`，全量回归 `2108 passed, 5 skipped, 204 warnings, 8 subtests passed`（约 323 秒）。新增目录只登记官方来源和结构标签，不改变未见题的评分边界。

公开实验配置（2026-09-09）：新增 `core/experiment_config.py`、`examples/public_experiment_config.json`、说明文档和 3 项回归；配置摘要绑定公开题源目录，禁止用未见题选模，固定候选/API/时间/内存预算。快速回归 `156 passed`，全量回归 `2111 passed, 5 skipped, 203 warnings, 8 subtests passed`（约 342 秒）。该配置仍不提供独立参考评分，不能据此声称模型准确率。

批量候选评价（2026-09-09）：新增 `core/batch_candidate_evaluation.py` 与 3 项测试；同一特征视图只构建一次，反馈仅保留有限诊断投影，重试只生成有界计划。该模块不执行不受信任代码、不授予候选通过资格。

基线优先分阶段运行（2026-09-09）：新增 `core/baseline_staged_runner.py`、说明文档和 3 项测试；基线始终先记录，探索模式不声明最终证据，严格模式强制唯一最终确认阶段，空候选与预算/资源失败均保留为明确状态。

未见方程、可微 IR 与控制候选（2026-09-09）：新增合成隐藏方程基准及配对 fit-only 切分消融、受限 typed IR 的 JAX/有限差分评估、有限时域线性二次控制候选；快速回归 `197 passed`，全量回归 `2155 passed, 5 skipped, 203 warnings, 8 subtests passed`（约 318 秒）。这些结果只证明固定合成/线性契约可复核，不证明真实新题覆盖率、全局最优或外部后端可用性。

合成校准协议（2026-09-09）：新增 `core/synthetic_calibration.py`，对显式生成器执行固定种子、多名义覆盖率的经验覆盖与有限样本标准误检查，并将失败重复标为 `partial`；该协议不被解释为真实数据覆盖证明或后验校准。

最终回归复核（2026-09-09）：加入统一七目标候选评价与合成校准测试后，快速回归 `202 passed`，全量 `2157 passed, 5 skipped, 203 warnings, 8 subtests passed`（约 319 秒）。现有警告仍为依赖版本、模型收敛、字体和可视化提示；该结果不等于 ROADMAP 尚未勾选的开放世界、跨平台沙箱和统计显著性目标已经完成。

隐变量误报控制（2026-09-09）：新增 `core/latent_state_benchmark.py`，在固定种子的高噪声独立观测生成器上测量延迟压缩候选的误报率；新增定向测试 2 项通过，结果只作为启发式运行特征，不证明真实隐变量存在。

同预算配对基准（2026-09-09）：新增 `core/paired_budget_benchmark.py`，对 baseline/treatment 固定案例、重复、种子和单方法墙钟预算，记录有效率、失败分母、延迟 P50/P95、峰值 Python 内存、API 次数、费用和错误接受；新增定向测试 2 项通过。该报告仍是描述性测量，不自动宣称统计显著改进。

冷/热缓存测量（2026-09-09）：新增 `core/cache_performance_benchmark.py`，按相同输入记录 cold、warm-first、warm-second、incremental 的输出摘要、时间、峰值 Python 分配和有限一致性比较；新增定向测试 2 项通过，未把单机时延差异升级为普遍提速结论。

权限策略增量（2026-09-09）：新增 `core/worker_permissions.py`，提供显式非根目录、网络/子进程默认拒绝和 Python audit-hook 适配器；新增 2 项定向测试。该层是防御性策略，仍不能替代容器/受限令牌等操作系统级隔离，因此 P4 文件/网络隔离总项保持未完成。

不确定性与诊断增量（2026-09-09）：新增概率质量约束的全方差适配、残差解释竞争器及定向测试；快速回归最新为 `214 passed`。残差排序仍是证据筛查，不自动引入隐变量或宣称因果机制。

反例/证据/证明增量（2026-09-09）：新增端点与扰动案例生成、数据/背景公理证据筛选、观测等价模型集合和条件化重写证书；快速回归最新为 `224 passed`。等价组仅表示有限观测不可区分，重写证书只覆盖已登记规则，均不扩张为全局数学证明。

全量回归复核（2026-09-09）：新增上述证据门与基准后 `python -m pytest -q` 为 `2179 passed, 5 skipped, 204 warnings, 8 subtests passed`（约 624 秒）。警告类别仍为第三方依赖、模型收敛、字体和可视化提示；新增模块未引入失败。

本轮完整回归（2026-09-09）：新增观测交互分解、数值策略、结构扩展、缓存政策、流式计划、队列配额、压力协议、SymPy 安全构造、渐进晋级、最终确认预算、假设控件闭包、建模政策与消融协议后，`python -m pytest -q` 为 `2211 passed, 5 skipped, 203 warnings, 8 subtests passed`（约 633.58 秒）；`node --check web/static/js/app.js` 与 `compileall` 通过。该结果仍不替代未见题统计、跨平台权限和真实后端验收。

快速回归增量（2026-09-09）：加入研究 Web 合作式取消/双会话压力、效率节省账本、JSON-only 持久编译缓存、多任务调度协议、交互曲面、临时磁盘配额、来源污染审计、离散外微分契约、因果 DAG 门、配对比较协议、可选后端运行时、归一化词片段污染筛查、多保真运行器和独立评分协议后，`python scripts/quick_regression.py` 为 `300 passed`；定向 Web/控件/切分/复用回归、静态编译和后端适配通过。新增模块均未调用真实外部 API。

本轮全量回归（2026-09-09）：新增上述模块后，`python -m pytest -q` 为 `2242 passed, 5 skipped, 203 warnings, 8 subtests passed`（715.37 秒）；`node --check web/static/js/app.js` 与 `python -m compileall -q core tests scripts` 通过。警告仍为依赖版本、模型收敛、字体和可视化提示，不将其解释为未见题统计或跨平台隔离证据。

多保真与服务压力增量回归（2026-09-09）：新增 `run_multifidelity_search` 的低/高保真与反例预算门、研究 Web 双会话并发压力测试以及 `rubric_review` 双评审协议；快速回归 `300 passed`，随后完整回归已包含这些模块，仍为 `2242 passed, 5 skipped, 203 warnings, 8 subtests passed`。该结果只证明当前仓库测试通过，不替代跨平台隔离或真实未见题统计。

四层不确定性与校准切片（2026-09-09）：新增 `build_uncertainty_audit`，将语义/结构/参数/数值经验分解与独立留出区间或概率校准分开，缺少独立样本时返回 `partial`；定向测试 2 项通过。该切片不把有限样本经验覆盖写成分布无关校准证明。

诊断驱动结构搜索增量（2026-09-09）：`graph_benchmark` 和 `benchmark_graph_search.py` 现在支持与固定结构/通用语法搜索同预算的 `diagnostic_search` 臂；提示只改变封闭原语优先级，仍由同一执行器、反例重放和留出门确认。无 API 合成回归通过，示例在 `--compare-cache` 下保留 4 个方法及失败分母；这不是未见题统计结论。

有限歧义候选回归（2026-09-09）：新增图搜索测试，在开发点无法区分 `observation(x)` 与 `abs(x)` 时保留两个结构不同、类型合法的 Pareto 候选，并记录机制多样性；该夹具没有冒充真实未见题或唯一真值。

搜索控制消融协议增量（2026-09-09）：新增 `build_search_ablation_manifest`/`run_search_ablation`，预登记完整搜索与五个单因素关闭臂，统一任务/种子/预算/最终测试指纹，并把失败保留在配对分母；定向测试通过，尚未以真实未见题数据填充，因此不勾选 P12 的性能验收。

新增协议后的全量回归（2026-09-09）：`python -m pytest -q` 为 `2252 passed, 5 skipped, 205 warnings, 8 subtests passed`（641.76 秒）；新增图搜索诊断臂、盲测词片段泄漏、搜索消融、结论证书和不确定性审计均包含在内。警告仍是第三方依赖、收敛、字体和可视化提示，不构成未见题效果证据。

UDE 数值切片（2026-09-09）：`fit_ude_correction` 增加有界基函数修正、时间留出残差和 1% 特征扰动稳定性审计；它明确不是神经网络或物理定律证明，定向测试通过。GNN 与完整 PDE 仍未完成。

worker 权限边界增量（2026-09-09）：`solver_worker` 在受控数值库预加载后安装 audit hook，默认拒绝网络、子进程和可信根目录外的路径访问，并将 `permission_isolated=true` 写入监督元数据；磁盘临时目录配额继续由父进程检查。受控 worker 回归通过；这仍不是容器/受限令牌认证，也不完成跨服务总额调度。

变更后定向复核（2026-09-09）：图搜索、封存盲测、结论证书、搜索消融、结构扩展与 UDE 相关测试共 `84 passed`；编译、前端语法和 diff 检查通过。

图搜索报告可读性修正（2026-09-09）：对照表新增每个方法的诊断提示数量，避免把诊断搜索与普通语法搜索显示成同一层级；不会改变候选接纳或留出判决。

最终快速回归（2026-09-09）：`python scripts/quick_regression.py` 为 `329 passed`；该集合覆盖本轮新增协议和核心安全/搜索回归，不替代全量测试或外部未见题统计。

网页假设控件增量（2026-09-09）：研究结果支持经后端校验的有限假设 slider schema，前端显示单位、影响节点和“仅预览/需重新验证”提示；`node --check` 及研究 API 回归通过。尚未把所有求解器的敏感性曲面、约束状态和 Pareto 交互统一接入，因此总项仍保留未完成。

### E5：诊断路由、模型调用与渐进结果

- [x] 确定性的类型修正、单位换算、数值容差调整优先由规则和求解器处理；涉及新机制或语义分叉时再调用模型。`core/repair_router.py` 将类型/单位/数值/资源错误路由到规则求解器，将新机制/语义分叉路由为需验证的模型提议，未知错误要求补证据。
- [x] 将本地小模型与外部 API 作为可替换提议后端，按实际合法率、修复收益、时延和费用选择；任何后端均不能绕过同一验证门。`core/proposal_router.py` 仅作有界排序和共享 validator 路由。
- [x] 对结构相近的候选批量评价或共享特征，模型反馈只包含最小诊断摘要和必要反例；`core/batch_candidate_evaluation.py` 按 `feature_key` 复用视图，限制候选/视图/反馈预算，并只生成有界重试计划，不在请求内隐藏重试。
- [x] 先展示有依据的简单基线，再在预算内搜索改进；`run_baseline_then_search` 分离“快速探索”和“严格验证”，严格模式强制最终确认，前者不得冒用后者的可信标签。
- [x] 用冻结数据快照和运行版本保证增量更新一致；`InputSnapshotCoordinator` 用版本/内容指纹和 compare-and-swap 提交门隔离旧任务，用户更新输入或取消运行后旧结果不能提交。

### 专项验收

- [x] 有缓存/无缓存、增量/全量、warm/cold、串行/受限并行在预定容差下结果一致；新增 `run_cache_performance_benchmark` 记录冷/热/增量输出摘要和有限一致性，随机搜索差异仍须由固定种子与候选集合记录。
- [x] 新增 `core/cache_consistency.py` 有限路线一致性协议：对比缓存/非缓存输出、状态、键和数值误差，忽略时间等遥测字段；差异记录为反例，有限通过不升级为全局等价证明。
- [x] 静态拒绝和低保真淘汰节省的计算量可统计；独立检查执行率不因加速降低，稀有事件及慢收敛对照不得被系统性漏掉。`core/efficiency_accounting.py` 要求完整预登记分母、独立检查覆盖和显式特殊案例 ID，缺失时不给出速度报告资格。
- [x] 多任务并发、取消、超时、内存超限和恢复的压力测试通过，主服务仍能响应状态查询与取消。研究 Web 新增双会话并发压力回归；取消、超时、磁盘/内存超限及临时目录回收分别由受控 worker 回归覆盖。该证据限定于当前服务进程和受控 worker，不等于跨主机/容器隔离认证。
- [x] 按 P12 的同预算协议给出速度、费用、内存、有效解和错误接受的配对结果；`run_paired_budget_benchmark` 固定同案/种子/预算并保留失败分母，结果为描述性证据，不宣称统计显著或更准。

## 第一阶段里程碑

- [ ] **M1：可度量**——完成 N01–N02 与 P0 最小基准，得到无泄漏的现有系统基线。
- [ ] **M2：可表达**——完成 P1–P2，20 个未见结构仅靠原语组合表达。
- [ ] **M3：可执行**——完成 P4，所有候选在资源沙箱中运行。
- [ ] **M4：可搜索**——完成 P3 与 P8a，诊断能驱动结构或观测假设变化，而不只更换回归器。
- [ ] **M5：可证伪**——完成 P5 与 N09，反例闭环收益通过固定预算实验，错误接受满足预设风险标准。
- [ ] **M6：可信**——完成 P6–P7，输出校准不确定性和高价值澄清问题。
- [ ] **M7：研究系统**——完成 P8–P12，形成可复现实验和论文材料。

## 设计参考与证据边界

以下来源支持局部方法，不证明上述组合系统已经有效，也不作为“首个系统”或覆盖率承诺的依据。

- [Decapodes](https://arxiv.org/abs/2401.17432)：借鉴组合式 PDE 表示与编译，不将图类型合法等同于现实正确性。
- [PhySO](https://arxiv.org/abs/2303.03192)：借鉴生成时的单位约束，不用量纲检查替代机理验证。
- [SGA](https://proceedings.mlr.press/v235/ma24m.html)：借鉴结构/参数双层优化；本项目先复用已有数值后端。
- [LaSR](https://arxiv.org/abs/2409.09359)：借鉴可迁移概念提示；概念本身不是已证明的规律。
- [KeplerAgent](https://arxiv.org/abs/2602.12259)：借鉴中间结构诊断指导符号搜索，作为局部能力而非通用解题保证。
- [HAVOK](https://www.nature.com/articles/s41467-017-00030-8)：借鉴延迟坐标表示，不把重建坐标自动命名为真实物理变量。
- [FunSearch](https://www.nature.com/articles/s41586-023-06924-6)：借鉴执行评价驱动搜索；评价器本身需要独立可信依据。
- [Model Discovery Agent](https://arxiv.org/abs/2608.09696)：借鉴候选集合不足与实验设计机制；2026 年预印本不视作成熟通用能力。
- [Successive Halving 官方接口](https://optuna.readthedocs.io/en/stable/reference/generated/optuna.pruners.SuccessiveHalvingPruner.html)：分级晋级的实现参考；最大资源预算仍须由运行时另行限制，不预先绑定此依赖。
- [Python AST 安全说明](https://docs.python.org/3.10/library/ast.html#ast.literal_eval)、[RestrictedPython](https://restrictedpython.readthedocs.io/en/latest/)：语言白名单不等于操作系统沙箱或资源安全。

研究 Web 取消增量（2026-09-09）：`/api/research/cancel` 为异步研究线程增加合作式取消信号，状态接口返回 `cancelling/cancelled` 与请求标记；worker 在启动前及提交结果前检查信号，不强杀 Python 线程、不发布取消后的结果。新增 Web 回归覆盖状态查询与取消信号；这不等同于全服务多任务压力认证。

效率账本增量（2026-09-09）：新增 `core/efficiency_accounting.py`，对静态拒绝/低保真淘汰保留完整分母，强制独立检查、稀有事件和慢收敛案例显式覆盖；缺失覆盖时返回不可发布的速度报告资格。该协议不伪造真实加速或统计显著性。

持久编译缓存增量（2026-09-09）：新增 `core/persistent_compile_cache.py`，以 JSON-only 原子文件保存带版本/来源/域/上游键的中间编译产物；损坏或版本不符冷启动，`clear()` 只清理当前缓存文件，预测和判决不入缓存。跨图多保真晋级及固定预算收益仍待独立基准。

多任务调度协议增量（2026-09-09）：新增 `core/multitask_stress.py`，在有界队列上记录完成、异常、取消、超时、准入拒绝及预约回收；该协议明确不冒充 OS 内存隔离或主 Web 服务端到端压力测试。

交互曲面增量（2026-09-09）：新增 `core/interactive_surface.py` 与前端渲染契约，支持受限约束状态、有限敏感性点表和无权重 Pareto 前沿的安全展示；当前仍要求可信求解器显式提供这些摘要，尚未把全部后端统一接入，因此 P9 网页总项继续保持未完成。

取消状态复用修复（2026-09-09）：同步研究请求会清除上一次异步取消事件，避免取消后的会话被错误地立即标记为取消；研究 Web 与交互曲面定向回归 `23 passed`。

临时磁盘配额增量（2026-09-09）：`SolverLimits.disk_bytes` 与父进程临时目录监视器现在会在 worker 中间文件超过声明上限时终止该候选并返回 `disk_limit`；新增 solver runtime 回归后定向测试 `72 passed`。该限制仅覆盖 worker 临时目录，不能替代全盘/容器级隔离，因此 P4 总项仍未完成。

来源与污染审计增量（2026-09-09）：新增 `core/data_provenance.py`，要求来源 URI、内容 SHA-256、所有者、许可和授权状态，并对开发/提示/最终集合执行精确指纹重叠审计；语义相似和模型记忆仍需外部审查，未将精确审计冒充完整泄漏证明。

离散外微分增量（2026-09-09）：新增 `core/discrete_exterior_calculus.py` 及 2 项回归；三角复形的方向边界满足 `d1·d0=0`，缺失边界边会拒绝。该后端仍只提供有限拓扑/形式契约，不承担完整 PDE 发现或物理验证。

因果结构门增量（2026-09-09）：新增 `core/causal_dag.py`，先校验角色、无环性和证据状态，再将未声明边延后；未达到 `verified` 不授权因果执行，避免把交互筛查或 DAG 结构假设写成因果证明。

跨进程配额增量（2026-09-09）：新增 `core/shared_resource_quota.py` 的 SQLite 原子租约、过期回收、续租与快照，并让 `SolverProcessRunner.execute(..., shared_quota=...)` 可选接入跨服务内存/槽位 admission gate；默认仍保持单进程配额。配额集成与权限/求解器定向回归 `77 passed`，未将其扩大解释为容器级隔离或全盘磁盘调度。

跨进程配额复核（2026-09-09）：增加真实 `multiprocessing` worker 竞争同一 SQLite 租约、限制配置不一致拒绝和 runner 集成覆盖；配额/runner 定向回归 `79 passed`，`python scripts/quick_regression.py` 更新为 `334 passed`。仍只证明受控租约协议，不证明多主机文件系统或容器级隔离。

全量回归复核（2026-09-09）：全量运行触达 `2259 passed, 5 skipped, 1 failed`；唯一失败是旧测试仍期待权限隔离为 `False`，与已接入的 audit hook 语义冲突。已将测试期望更新为 `True`，该失败用例随后单独通过；需在发布前再跑一次完整套件确认最终总数。

受控执行回归复核（2026-09-09）：修正期望后，图搜索、solver runtime、worker 权限与共享配额联合定向套件 `148 passed`；全量其余项目在前一轮已通过，完整最终总数仍以重新跑完全量套件为准。

最终全量回归（2026-09-09）：修正权限隔离断言后，`python -m pytest -q` 为 `2260 passed, 5 skipped, 203 warnings, 8 subtests passed`（1482.33 秒）。警告均为第三方依赖、收敛、字体或可视化提示；没有失败项。

发布前复核（2026-09-09）：`scripts/release_audit.py` 仍为 `publishable: true`，依赖锁和 LICENSE 均存在；`compileall`、前端 `node --check` 与 `git diff --check` 通过。审计保留测试中的示例 token 字样和 36.99 MB 封存数据 zip 警告，未把有界审计当作完整密钥历史或许可证法律审查。

外部基准来源增量（2026-09-09）：新增 `docs/benchmark_sources.md` 与 `tests/test_external_benchmark_sources.py`，对现有 15 个真实 MCM/ICM 未见题逐项核验官方 COMAP URL、题面文件、字节数和 SHA-256；MM-Bench 与公开归档仅登记为外部对照，不冒充独立金标准。快速回归清单现为 `336 passed`；公开克隆没有封存语料时测试明确跳过，参考答案/rubric 仍保持仓外封存，避免泄漏。

外部可用来源注册表（2026-09-09）：新增 `examples/benchmark_sources.json` 与 `core/benchmark_sources.py`，登记 COMAP 官方题面/规则、UMAP 46.3/46.4 Judges' Commentary 入口、MM-Agent/MM-Bench、mathworks 归档及两个公开代码参考。注册表按 `task_input`、`rubric_material`、`method_comparison`、`reference_only` 分离用途，强制拒绝把公开题解标为 gold truth；新增 3 项来源边界测试。该增量解决“去哪里找可用材料”，不宣称已经获得独立数学正确率。

MCM/ICM 评审模板（2026-09-09）：新增 `examples/benchmark_rubric_mcm_icm.json`，将官方规则中强调的题意、假设、数学模型、推导、计算复现、验证/敏感性、结论和证据表达拆为八个可双评审维度；模板只产生描述性 rubric 分数，未被升级为 gold truth。

来源—评审绑定增量（2026-09-10）：新增 `core/benchmark_material.py` 与 3 项测试。`build_material_plan` 将题面输入、评审量规、外部方法对照和人工参考候选按角色绑定并生成摘要；`validate_material_plan` 拒绝摘要篡改、来源跨角色复用和公开题解进入输入/量规。该层仍不生成正确率，只有仓外独立 rubric/参考解解封后才允许盲测计分。

标准比较方案增量（2026-09-10）：新增 `core/standard_comparison_plan.py`，预登记注册表基线、单次 LLM、AutoML、符号回归、MM-Agent 风格和诊断驱动开放组合六个 arm，并沿用相同任务、预算、随机种子和锁定测试指纹协议；新增 2 项回归。该方案只冻结比较设计，真实未见题结果和统计显著性仍未完成。

自动修复风险门增量（2026-09-10）：新增 `core/acceptance_risk_gate.py`，在配对运行结果上同时检查预登记的最小样本、有效率提升 bootstrap 下界和错误通过率 Wilson 上界；样本不足返回 `not_assessed`，风险超标返回 `fail`，不会把未检出错误解释为零风险。新增 4 项回归；真实未见题效果仍未宣称。

求解器超时归因修复（2026-09-10）：`SolverProcessRunner` 对显式声明的 ODE/NLP 契约增加父进程预检，并对低于两次确认评估的预算直接返回 `evaluation_limit`；无效契约不再先等待 worker 导入数值库后被误报为 `timeout`，worker 仍保留二次复核。相关求解器回归 4 项通过，编译检查通过。

主动澄清成本排序增量（2026-09-10）：`core/information_value.py` 为每个问题增加正的有限 `cost` 与 `gain_per_cost`，按单位声明成本的信息增益稳定排序，同时保留原始熵下降量；非法成本会在计算前拒绝。文档与回归覆盖了成本权衡，仍明确这是声明权重下的条件信息值，不是后验或现实采集收益证明。

澄清动作输入加固（2026-09-10）：`core/question_actions.py` 现在严格校验问题 ID、证据动作、布尔控制字段和正的有限成本，拒绝字符串真值/非数值成本导致的隐式放行，并规范化 ID 与成本输出；新增 5 项输入边界回归。该校验仍不把用户回答升级为事实或独立证据。

语义问题信息值接线（2026-09-10）：`SemanticHypothesisSet.rank_questions` 在用户选项构成不重叠分区时附加条件熵下降与单位成本字段；选项重叠或不完整仅标记 `not_assessed`，不破坏原有启发式排序。新增回归覆盖有效分区和重叠分支，均不把均匀声明权重解释为后验概率。

未知机制搜索空间增量（2026-09-10）：`structure_candidates` 的未决机制草案现在显式带有有界 `allowed_operators`、`search_budget` 与待检验性质，使后续动态编译可在类型化小空间内变异；候选仍固定为 `proposed_not_executed`，没有因此宣称新题数值求解或现实正确性。

worker 文件事件加固（2026-09-10）：`worker_permissions.check_audit_event` 扩展到删除、目录、权限变更、复制和双路径重命名/链接事件，并逐一校验源与目标均在声明根目录；新增边界回归，受控执行套件 `77 passed`。这是 Python audit-hook 的纵深防御，不能替代容器/受限令牌或全盘配额。

快速回归复核（2026-09-10）：`python scripts/quick_regression.py` 在新增主动澄清、未知机制搜索空间和 worker 文件事件回归后为 `421 passed`；另行编译检查通过。该集合是有界快速套件，不等于全量测试、依赖矩阵或未见题效果认证。

来源 URI 加固（2026-09-10）：`build_source_ledger` 拒绝控制字符、超长 URI、HTTP 明文、嵌入式用户名/密码和远程 `file://` 主机；解析异常统一降级为稳定错误码，认证信息必须走带外配置。新增 6 项来源边界回归，未把 URI 合法性解释为内容真实性或许可法律审查。

统一执行准入增量（2026-09-10）：新增 `core/execution_readiness.py`，要求候选显式提供类型、单位、来源、资源和安全五项检查；候选带有该字段时 `gate_candidates` 自动将缺失/未评估标为待定、失败标为硬失败。新增 19 项定向回归和文档；这是统一准入协议，尚未证明所有后端都已接入。

UDE 数值稳定性增量（2026-09-10）：`fit_ude_correction` 增加 Gram 矩阵条件数上限、非有限系数/预测拒绝，并输出条件数和稳定性审计状态；避免病态特征库被误报为可用修正项。新增条件阈值回归，仍是有界线性基底校正，不等于神经 UDE 或物理定律证明。

配对比较输入加固（2026-09-10）：`run_preregistered_comparison` 拒绝重复任务/arm ID，并把评估器返回的非布尔 `valid` 记录为逐格 `error` 而不是隐式真值；新增比较协议回归 3 项。失败继续留在分母，未因此宣称统计显著改进。

消融协议输入加固（2026-09-10）：`build_ablation_manifest` 拒绝重复 task ID 和非布尔 baseline，`record_ablation_results` 严格校验 `valid` 与有限 score；新增 4 项回归，避免比较统计被字符串真值或 NaN 污染。

固定基准状态加固（2026-09-10）：`run_fixed_benchmark` 将评估器状态限制为预登记的 completed/failed/timeout/rejected/incomplete，未知状态转为失败行并保留在分母；新增基准回归，避免统计摘要漏计自定义状态。

执行准入/基准协议联合回归（2026-09-10）：执行准入、候选门、UDE 稳定性、来源审计、固定基准、配对比较和消融协议定向套件 `33 passed`；编译检查通过。该结果只验证协议输入与失败分流，不替代真实未见题统计验收。

外部方法与未见题来源增量（2026-09-10）：经官方/原始来源核验，将 PySINDy 普通与 weak-form PDE 文档、SciML UDE 文档及原始论文、LLM-SR 官方实现、LLM-SRBench 和 COMAP 2023/2024 题目页登记到方法/题源目录；新增 `docs/external_methods.md` 规定固定版本、分离评测臂和许可证边界。外部来源只用于方法比较或盲测输入，不能替代独立 gold truth，也未声称已完成真实未见题效果认证。

外部题源目录补齐（2026-09-10）：在机器可读注册表中增加 COMAP 2023 与 2024 官方题目索引，与现有 2025 题源保持同一 `task_input` 契约；题目页只登记链接与用途，不抓取或复制题面内容，避免把公开资料泄漏到训练/提示开发集合。

外部方法候选路由增量（2026-09-10）：新增 `core/external_method_router.py`，依据题目是否含时间序列、空间网格、已知机理和噪声证据，规划 SINDy、weak-form SINDy、PDE-FIND、UDE 与 LLM-SR 候选；依赖未声明时保持 `not_assessed`，LLM-SR 默认仅 `proposal_only`。新增路由输入边界测试并纳入快速回归；该层仍不执行第三方代码，也不宣称方法候选已被验证。

路由默认预算修复（2026-09-10）：修正候选路由默认 `max_methods=8` 与方法种类数不一致造成的启动拒绝，改为独立的 1–32 有界预算；路由与来源测试最终 `13 passed`。

Web 方法规划入口（2026-09-10）：新增 `POST /api/research/method-plan`，先根据题目证据返回外部方法候选，再由用户或研究流程决定是否执行；接口不安装依赖、不下载仓库、不绕过候选准入。新增 Web 回归覆盖多方法规划和 `not_assessed` 依赖状态。

本地方法适配增量（2026-09-10）：新增 `core/external_method_runtime.py` 与 `POST /api/research/method-execute`，将 SINDy、weak-form SINDy、PDE 特征库和有界 UDE 线性修正接入统一结构化契约；LLM-SR 永远保持 `proposal_only`，未知字段、未知方法和数值失败进入明确拒绝分流。新增运行时与 Web 回归，第三方仓库仍不执行。

研究结果自动挂接（2026-09-10）：`MathModelingAssistant.run` 依据数据画像保守生成 `specialized_results.external_method_plan`；只有明确时间列、空间坐标或机理任务证据才触发相应候选，缺失值不再被当作噪声。该计划仍是审计提示，未将路由结果升级为模型正确性。

候选结构多样性审计增量（2026-09-10）：`structure_candidates` 为每个候选生成与名称无关的原语图签名，并输出 `diversity_audit`，区分结构不同、候选坍缩和候选不足；该审计只证明表示层差异，不证明语义新颖性、现实合理性或数值正确性。

可执行候选准入加固（2026-09-10）：`candidate_gate` 对显式声明 `executed/runnable/implemented/executable/solver_ready` 的候选强制要求完整 `execution_readiness` 五项证据；缺失时保留在 `not_assessed`，不允许用拟合指标晋级。纯提议候选保持兼容，新增 2 项回归。

运行结果证据回填（2026-09-10）：`external_method_runtime` 的 SINDy、weak-form 和 UDE 结果现在自动附带类型/单位/来源/资源/安全五项准入字段；单位或来源未显式提供时保持 `not_assessed`，防止数值执行成功被误报为可发布结论。

结论证书接线（2026-09-10）：外部方法本地适配器在调用方显式提供适用边界与假设时生成有限 `conclusion_certificate`，携带验证段残差和反例；缺少边界或证据格式错误仅阻止发布证书，不丢弃数值运行记录。该证书仍不是全局证明或现实正确性证明。

四层不确定性 Web 接口（2026-09-10）：新增 `POST /api/research/uncertainty-audit`，复用 `build_uncertainty_audit` 分解语义/结构/参数/数值层变化，并要求独立区间或概率校准样本；没有校准样本时保持 `partial`，不把一次运行或开发段分数写成置信度。

外部方法比较预注册（2026-09-10）：新增 `core/external_method_comparison.py`，固定本地 SINDy、weak-form、PDE 特征库、UDE 和 LLM-SR 提议态五个实验臂，并复用统一任务/预算/最终测试指纹协议；仅完成比较设计，不伪造未见题性能或显著性结论。

外部方法比较运行器（2026-09-10）：`run_external_method_comparison` 已将预注册清单接入统一配对执行协议，按同一任务网格调用本地 SINDy、weak-form SINDy 与 UDE 适配器；PDE 规划态、LLM-SR 提议态、缺失输入和数值失败均保留为拒绝/不完整行。残差只作为描述性分数，不升级为真实题正确率或显著性结论；新增执行、失败分母与缺失载荷回归测试。

外部方法比较 Web 入口（2026-09-10）：新增 `POST /api/research/method-compare`，接收已冻结的比较清单与任务载荷，调用本地配对运行器并返回逐臂状态、残差分数和失败分母；接口不接受回调、不执行第三方代码，LLM-SR/PDE 仍保持提议/规划状态。新增接口回归测试，便于前端或批处理复用同一评测契约。

比较主指标显式化（2026-09-10）：外部方法清单加入 `validation_residual_rmse`，运行结果固定输出 `primary_metric` 与 `score_direction=lower_is_better`，避免把未来的有效率/资源指标误当作当前残差分数；相关文档与回归同步更新。

真实未见题统计评估增量（2026-09-10）：新增 `core/blind_statistics.py` 与 `docs/blind_statistics.md`，在封存清单全部为 `external_real/unseen`、参考评分完成解封、预算与运行账本匹配、独立评审已声明且达到预注册样本量时，输出 Wilson 准确率区间、配对精确 McNemar 检验和 bootstrap 差异区间；新增 `POST /api/research/blind-accuracy`。合成/混合案例、未声明独立评分或不完整覆盖只返回 `not_assessed`，不会伪造真实泛化结论。

盲测统计 CLI 接入（2026-09-10）：`scripts/blind_benchmark.py statistics` 支持从封存清单、运行账本和解封评分文件生成统计报告，统一主评分、成功阈值、样本量、显著性水平和失败处理策略；帮助文档同步给出命令示例，未满足真实未见题门槛时命令仍输出 `not_assessed`。

新增真实题执行回归（2026-09-10）：使用 COMAP 官方 2025 MCM/ICM 题目索引中的 ICM E（Making Room for Agriculture）、MCM C（Models for Olympic Medal Tables）以及 2024 MCM A（Resource Availability and Sex Ratios）进行本地封存 worker 回归，3/3 进程完成。MCM C 的附件路径被正确读取为 5 个数据集、4 条候选关系和 35 个变量交互；ICM E 与 MCM A 进入无数据机理模式并保留“缺少机器可读方程/几何定义”的数值阻断。该批只证明新题执行可达与失败归因，不构成正确率或统计显著性证据。

真实未见题来源审计（2026-09-10）：新增 `core/evaluation_source_audit.py`、`examples/evaluation_sources.json`、`scripts/validate_evaluation_sources.py` 和 [评估来源审计](docs/evaluation_sources.md)。核验 ModelingBench/MM-Bench、COMAP 公开题源、IMProofBench、FrontierMath 与 FrontierScience：前 3 类不能直接支持真实未见题准确率，后 3 类虽有私有题和专家评分但领域不匹配。来源级资格不会绕过案例级封存、独立解封评分、样本量和显著性门槛；当前仍需项目外评审者提供至少 20 道未公开建模题和独立量规。

评估来源 Web 查询入口（2026-09-10）：新增 `GET /api/research/evaluation-sources`，返回来源审计的脱敏元数据和资格状态；接口不抓取题面、附件或答案，新增 Web 回归验证公开来源不会泄漏内容字段。

独立未见题登记增量（2026-09-10）：新增 `core/holdout_intake.py` 与 [独立未见题登记](docs/holdout_intake.md)，在封存前检查至少 20 题/4 题型族、题面与参考解哈希承诺、出题人/评审人隔离和开发冻结摘要；仅输出元数据，不接触题面内容，新增 3 项回归。通过登记仍必须执行 blind seal、泄漏扫描、独立解封评分和配对统计。

独立未见题登记接口（2026-09-10）：新增 `scripts/validate_holdout_intake.py` 与 `POST /api/research/holdout-intake`，支持命令行和网页端复用同一元数据校验；不接收题面正文，不将最小样本参数写入登记摘要，新增 Web 回归覆盖不完整登记的 `not_ready` 分流。

登记—封存绑定增量（2026-09-10）：新增 `core/holdout_binding.py` 与 `POST /api/research/holdout-bind`，逐题校验登记元数据和 `BlindManifest` 的题面/附件/答案哈希、字节数、题型及 `external_real/unseen` 状态；任何替换或篡改都在统计前拒绝，新增绑定回归。

登记—封存绑定 CLI（2026-09-10）：新增 `scripts/bind_holdout.py`，允许评审者在仓外用同一绑定逻辑核验登记文件和公开哈希清单；只输出绑定摘要，不复制题面或参考解。

网络权限前置阻断增量（2026-09-10）：`worker_permissions` 现在在 `socket.__new__` 以及 DNS/连接/绑定事件统一拒绝网络，避免仅拦截连接后仍可创建网络句柄；新增回归覆盖创建阶段拒绝。该改动仍是 audit-hook 防御层，不替代 OS/容器网络命名空间。

评分与题源补充（2026-09-10）：登记 IMMC 官方独立教授评审指南、SIAM GAI MM 建模量规和 Zenodo 分阶段建模评估数据；用途分别限定为评审流程、量规校准和方法比较，均明确不充当真实未见题金标准。

外部未公开题合作路径（2026-09-10）：补充登记 Modeling the Future Challenge、IMMC 和 MathWorks M3 Challenge 的官方入口，并新增 [外部未公开题与评审合作](docs/external_holdout_outreach.md)。明确了申请 20–30 道未公开题、独立量规、双评审和封存哈希所需材料；仅公开页面和竞赛奖项不能直接转成金标准，必须先取得组织方授权并通过 holdout intake/bind/statistics 全链路。

隐藏标签预测评测臂（2026-09-10）：登记 DrivenData 代码执行竞赛、Kaggle 隐藏真值机制和 TML-Bench 私有留出基准，补充 `docs/evaluation_sources.md` 与外部合作说明。该评测臂用于检验数据预测准确率、执行失败率和资源约束，不与开放式建模的专家量规分数混合；来源级登记不代表本项目已获得任何平台分数，仍需按具体竞赛冻结提交协议、权限和测试指纹。

无组织方合作的本地评测路线（2026-09-10）：补充说明现有 15 道 COMAP 历史题可作为冻结留出集，并由两名未参与开发者依据八维 rubric 独立盲审；明确其只能产生“冻结留出集独立盲审质量分”，不能伪称未公开题准确率。当前 `scored_count=0` 保持不变，直到仓外独立评审结果完成解封。

匿名盲审包工具（2026-09-10）：新增 `core/review_packet.py`、`scripts/build_review_packet.py` 和回归测试。工具按 manifest 生成不含年份/题号的评审 ID、两份空白评分表及仓外私有映射；映射不进入公开仓库，评审分数只作为描述性质量证据，不能绕过真实未见题统计门槛。

隐藏标签分数导入器（2026-09-10）：新增 `core/hidden_label_evaluation.py` 与 `scripts/import_hidden_scores.py`，校验外部评测器返回的指标、状态和测试指纹，拒绝预测值/真实标签字段及重复任务，输出仅为描述性统计；不接受平台回调、不绕过独立建模评审或显著性门槛。

回归集同步（2026-09-10）：将匿名盲审包与隐藏标签分数导入测试加入 `scripts/quick_regression.py`；全套快速回归 `475 passed`。该结果只证明当前代码契约稳定，不代表未完成的开放世界泛化、独立评审或操作系统级隔离验收已完成。

盲审汇总闭环（2026-09-10）：`core.review_packet.aggregate_review_forms` 与 `scripts/aggregate_review_forms.py` 将匿名评分表映射回私有案例 ID并调用统一 rubric 聚合器；缺分、重复评审、错映射和跨版本表单均拒绝。该闭环仍只产生独立盲审质量分，不生成数学真值或真实未见题准确率。
外部公开数据接入（2026-09-10）：新增 `core/external_dataset_catalog.py`、`examples/external_dataset_catalog.json`、`scripts/fetch_external_datasets.py` 和数据目录回归。目录固定 UCI Wine Quality、Concrete Strength、Bike Sharing、Air Quality 的官方 HTTPS 来源、许可证、字节数与 SHA-256；其中 Air Quality 明确为研究用途且排除商业使用。数据下载到被忽略的 `data/external`，采用大小/哈希校验和临时文件原子替换。它们可支撑外部数据处理与结构搜索复现，但公开标签不等于真正未见题金标准，独立评审与私有留出仍未伪造。
公开外部数据自动评测（2026-09-10）：新增 `core/external_data_benchmark.py`、`scripts/run_external_data_benchmark.py` 和回归测试。评测器从四个已校验的 UCI 压缩包读取 Wine（红/白）、Concrete、Bike Sharing、Air Quality，固定种子执行时间/随机留出，使用独立 Ridge 基线与 ExtraTrees 处理臂，记录源哈希、划分哈希、MAE/RMSE/R²、失败分母和配对 RMSE 差异。结果标记 `descriptive_public_labels`，不升级为真正未见题准确率。
外部评测量纲修正（2026-09-10）：跨数据集汇总由原始 RMSE 差改为每任务相对 RMSE（处理臂/基线−1），避免把不同物理单位的误差直接平均；报告保留每题原始 MAE/RMSE/R²，并把汇总结果限定为描述性公共标签证据。
外部评测统计增强（2026-09-10）：新增任务级 Bootstrap 区间与精确符号置换检验，汇总使用相对 RMSE 以避免跨单位聚合错误；样本量与公开标签政策仍强制限制为描述性外部证据。
外部数据端到端烟测（2026-09-10）：使用已校验的 UCI Wine Quality 红酒表调用 `MathModelingAssistant`，完成数据画像、目标识别、交叉验证、模型诊断和数学论证报告；产物状态为 `complete`，输出位于被忽略的 `data/reports/external_assistant_smoke`。运行暴露环境中缺少 CatBoost/Plotly 的可选依赖提示，以及质量字段被识别为多分类任务的事实，未把结果升级为未见题准确率。
外部评测数据契约修复（2026-09-10）：发现并修复两处影响结论的错误：Bike Sharing 的 `casual`/`registered` 是目标 `cnt` 的组成列且 `instant` 是行号，现已从输入剔除；Air Quality 的 `-200` 目标缺失标记现已整行剔除。每题报告新增 `data_audit` 与数据契约，旧版结果被覆盖。修复后 5/5 任务完成，平均相对 RMSE `-20.45%`、中位数 `-15.86%`、精确符号置换 `p=0.125`；仍只属于公开标签描述性证据，未宣称显著性。
外部评测网页接入（2026-09-10）：新增 `POST /api/research/external-data-benchmark`，网页端复用 CLI 的固定目录、源哈希、泄漏护栏和配对统计；服务端不接受任意路径，只允许有界随机种子和行数预算，新增两个接口回归。

监督学习目标泄漏下沉（2026-09-10）：`MathModelingAssistant._run_supervised_model` 现在在拟合前识别精确目标加和分解并删除其组成列，同时标记精确复制列供可信度审计；新增连续整数行计数器识别，避免 `instant` 一类索引进入模型。结果新增 `feature_leakage_filter` 与 `dropped_feature_columns`，并补充跨表建模、历史复制列和行计数器回归。该规则只处理可证明的派生关系，不能替代预测时点审计。

本轮回归复核（2026-09-10）：外部基准/数据目录/Web 定向套件 `40 passed`，建模助手目标泄漏回归通过，`python scripts/quick_regression.py` 为 `488 passed`；`compileall`、`git diff --check` 和发布审计通过。发布审计仍仅有既有测试示例 token 与封存大文件警告，无错误；这些结果不替代真实未见题独立评分或 OS 级沙箱认证。

默认模型组合修复（2026-09-10）：监督学习不再固定只运行注册表前 3 个模型，而是依据样本量、特征维度和时序标志选择有界模型组合，并在结果中记录 `model_portfolio`。该改动让已注册的树模型、稳健模型和结构模型有机会参与同一交叉验证/可信度审计；不代表所有模型都适合每道题，也不等同于自动发现新数学结构。

R1 最小执行桥（2026-09-10）：新增 `POST /api/research/graph-search`，接收已完成类型/单位/域/训练点绑定的 `mathmodel.graph-search-bundle/v1`，固定输出到项目工作区并复用本地图搜索 worker；新增 Web 回归 2 项。该入口使显式绑定候选可以从网页执行，但自然语言自动绑定、跨数据角色推断和 ODE/PDE/优化统一编译仍保持未完成。

R1 执行桥实测（2026-09-10）：使用仓库内 `examples/graph_search_scalar.json` 调用 Web endpoint，HTTP 200，返回 `candidate_budget_exhausted`，产物写入版本化 graph-search 运行目录。该结果证明入口和受控搜索可达，不证明候选正确、题意已绑定或开放世界泛化。

R1 数据到图搜索桥（2026-09-10）：新增 `core/data_graph_bridge.py` 和 `POST /api/research/data-graph-search`。调用者必须显式指定特征列与目标列；桥接器对有限数值行做有界清洗、重复输入中位数聚合、互斥训练/搜索划分，生成带 `unknown_mechanism` 的类型化标量图，并交给同一受控搜索 worker。审计同时记录单位为“假定无量纲待确认”、因果关系未评估和被丢弃行数。该功能证明真实表格可进入候选执行闭环，不代表自动角色识别、因果发现或多变量/ODE/PDE/优化已完成。

R1 双特征交互扩展（2026-09-10）：数据桥 `features` 支持一到两列；两列保持完整特征元组分区并生成二元未知机制输入，超过两列明确拒绝而不静默截断。搜索预算从 8 个候选提高到 16 个候选/64 次 patch（仍受 30 秒墙钟约束），在合成平方关系上可返回 `candidates_need_confirmation` 与独立搜索分区的 `eligible_for_confirmation` 候选；这只是有限候选的开发证据，仍需只读冻结确认和外部/私有留出。

本轮快速回归（2026-09-10）：`python scripts/quick_regression.py` 为 `495 passed in 304.63s`；另有双特征数据桥和 Web 端点定向回归 `40 passed`。`compileall` 通过，`git diff --check` 无空白错误；Git 输出的 LF/CRLF 为平台换行警告，不是代码失败。

快速回归更新（2026-09-10）：加入 GNN 交互筛查与主流程开关后，`python scripts/quick_regression.py` 为 `499 passed in 275.05s`，仅有可选 PyTorch 环境的 pynvml 弃用警告。

数据桥审计计数修复（2026-09-10）：修复 `max_rows` 采样后把有限行/不同特征元组数量误报为采样行数的问题，审计现在分别保留清洗前有限行、聚合前不同元组和实际送入搜索的行数；定向数据桥/Web 回归为 `41 passed`。

R0 整数轴误删修复（2026-09-10）：`_is_row_index_like` 现在接收列名；无名调用仍保持旧的行计数器检测，主建模路径对 `time/date/period/year/month/day/step` 等显式时间语义保留连续整数轴，仅对 `row/index/instant/serial` 等明确索引语义删除。新增语义回归；建模助手套件 `45 passed`。这仍不能替代字段来源/预测时点确认。

R1 量纲绑定增量（2026-09-10）：数据桥新增成对的 `feature_dimensions`/`target_dimensions` 输入，将量纲写入 `MathType` 并让候选算子在编译阶段拒绝不相容加法等结构；缺失量纲仍保留探索入口但输出 `assumed_dimensionless_requires_confirmation`。数据桥/Web 定向回归为 `42 passed`。

R2.4 轻量 GNN 交互筛查增量（2026-09-10）：新增 `core/gnn_interaction_screen.py`，使用可选 PyTorch 实现有界一层 sigmoid 边门消息传递，训练/验证分离、标准化仅在训练段拟合、重启稳定性和验证 RMSE 均进入结果；新增 `POST /api/research/gnn-interactions` 与模块目录条目。它只提供预测性交互证据，缺少 PyTorch 时显式 `unavailable`，不把边门解释为因果发现或机制证明。定向 GNN/模块/Web 回归 `42 passed`（有一个可选 PyTorch 的 pynvml 弃用警告）。

GNN 主流程接入（2026-09-10）：`MathModelingAssistant` 新增显式 `enable_gnn_screen` 开关，Web 研究请求可用 `enable_gnn_screen=true` 且必须提供明确目标列后才运行；默认关闭以保护大数据任务时延，结果写入 `specialized_results.gnn_interaction_screen`，失败/缺少依赖安全降级为运行警告。

发布审计复核（2026-09-10）：`scripts/release_audit.py .` 扫描 1296 个文件，`errors=[]`、`publishable=true`；仅报告测试中的示例 token 警告和被忽略的封存大文件警告。公开仓库仍不应提交 `workspace/` 下的私有题面/答案或真实 API key。

R3 组合可达性修复（2026-09-10）：在大样本回归组合中为结构模型保留一个候选槽位，避免可选 LightGBM/XGBoost 永久挤掉 Piecewise、Residual、Hierarchical 或 Constrained；候选仍需同一交叉验证与可信度审计，新增组合路由回归。

R9 模块生命周期目录（2026-09-10）：新增 `core/module_catalog.py`、`scripts/audit_module_catalog.py` 和 `GET /api/research/module-catalog`，为 active/optional/experimental/legacy 模块登记实际入口、证据范围和可选依赖；新增 Web/目录回归。该目录解决能力声明混乱，不把模块存在升级为端到端完成。

多变量交互筛查增量（2026-09-10）：新增 `core/interaction_graph_screen.py`，对每个数据表执行有界岭偏相关和重采样稳定性筛选，并接入 `specialized_results.interaction_graph_screen`；结果明确标记为条件关联而非 GNN/因果边。新增 3 项回归，样本不足和常量变量保持 `not_assessed`。

R2.2 规则网格 PDE 执行增量（2026-09-10）：新增 `core/pde_discovery.py`，在规则一维空间/时间网格上实际拟合 `u/u_x/u_xx` 候选，使用时间留出、边界点残差和有限反例；`pde_find` 有时空场输入时进入该执行器，无场输入仍保持类型化规划。新增模块目录、文档和运行回归；PDE 仍明确限制为有限候选，不是任意 PDE 求解器。

R2.3 神经 UDE 残差执行增量（2026-09-10）：新增 `core/ude_neural.py` 与 `ude_neural` 外部方法路由。可选 PyTorch 小型 MLP 只学习 `known_rhs + g_neural(features)` 的残差，按时间顺序留出验证段，记录纯机理基线、多重启误差、预算和缺少依赖状态；修正训练目标为完整 RHS，避免把已知机制重复扣除。新增 [神经 UDE 文档](docs/ude_neural.md)。当前仍不是 ODE 积分器、联合物理参数估计或物理含义证明，剩余项继续保留在 R2.3 待办。

R2.3 神经 UDE 可复用状态（2026-09-11）：保存验证集最优重启的网络权重、输入维度、隐藏层宽度及训练段标准化统计量，并新增 `predict_neural_ude` 复用接口；测试覆盖重复预测一致性。该状态仍只支持同一特征契约，不代表跨轨迹泛化或物理可辨识性。

R1 类型化子图执行增量（2026-09-11）：新增 `core/primitive_graph_runtime.py`。完成的加减乘除、幂、初等函数子图现在可在通过类型/量纲校验后按拓扑顺序执行；变量必须显式绑定，未决机制和非代数节点被拒绝，禁止把生成源代码送入解释器。新增运行时回归测试和文档；ODE、优化、事件及完整候选竞争仍需各自契约与独立验证。

R1 候选执行门增量（2026-09-11）：新增 `core/candidate_execution.py` 及 `/api/research/structure-candidate/execute`。候选图可在显式绑定后进入受限运行时，返回 `executed/rejected` 和证据边界，同时保留原提议状态；未决机制不会被自动猜测。该入口仍不构成独立正确性或泛化证明。

R2.2 PDE 契约增量（2026-09-11）：一维规则网格发现接口增加场/坐标量纲声明、噪声尺度、边界值形状和缺失掩码检查，并将状态写入结果；缺失值不再被隐式处理，当前明确拒绝并保留支持边界。

R4 候选反例 evaluator 增量（2026-09-11）：`evaluate_structure_candidate` 将完成的类型化代数候选在显式案例上评估为 pass/fail/not_assessed，输出有限 RMSE、反例 witness 和成本，可直接作为 `run_cegis` 的 evaluator；不自动生成变异、不把通过升级为现实正确性。

R4 标量常数修复增量（2026-09-11）：`run_arithmetic_candidate_cegis` 增加受预算约束的常数变异器，仅保留通过同一案例集的候选；结构、单位、边界和现实约束仍不可由该窄变异器修改。

R9 模块引用审计增量（2026-09-11）：模块目录增加不导入代码的源码/测试/Web 引用扫描，新增 `scripts/audit_module_usage.py` 和 `module-catalog?audit=1`，可明确列出只登记未接入模块；该审计不等同于运行覆盖率。

R1 语义绑定增量（2026-09-11）：新增 `core/binding_contract.py` 与 `/api/research/binding-contract`，固定输入表、变量角色、维度、目标、约束、初边值、有效域、来源和未决项，并根据已绑定角色规划保守后端；缺失单位阻断编译，不默认无量纲。

回归验收（2026-09-11）：完整 `pytest -q --disable-warnings` 通过 2427 项，5 项跳过，203 条依赖/弃用警告，8 个子测试；`compileall` 与 `git diff --check` 通过（仅换行格式提示）。

R1 候选证据下沉（2026-09-10）：标量图评估器在通过互斥搜索分区检查后保存逐点 `search_predictions` 与 `search_expected`，使后续统一候选比较可以消费实际执行结果而不重复猜测；字段明确标记为开发证据，最终确认仍必须走独立 registry。新增图搜索回归断言预测/期望长度和数值一致；尚未把所有题型后端接入统一竞争层。

本轮回归复核（2026-09-10）：`python scripts/quick_regression.py` 为 `502 passed`，外部方法/图搜索定向套件 `78 passed`；`compileall` 与 `git diff --check` 通过。发布审计 `errors=[]`、`publishable=true`，仍仅有测试示例 token 与被忽略封存大文件警告；这些结果不代表真实未见题泛化、独立评分或 OS 级沙箱已完成。

R1 多变量数据桥增量（2026-09-10）：`data_graph_bridge` 现在接受最多四列数值特征，保留完整特征元组；无量纲多输入未知机制可由图搜索生成有界二元折叠候选并进入受控 worker，显式非无量纲多输入仍拒绝，避免猜测中间量纲或静默丢列。新增三特征端到端回归；该能力仍不等于自动角色识别、因果发现或任意维度公式搜索。

R1 单位安全折叠增量（2026-09-10）：多输入图搜索在所有输入与目标量纲完全一致时，额外允许加、减、最小、最大折叠；乘除等需要推导中间量纲的结构继续拒绝。新增显式长度量纲三特征回归，避免用“有单位”作为无差别阻断，也避免未经推导的单位传播。

R1 量纲推导折叠增量（2026-09-10）：多输入图搜索现在逐步计算加/减/最小/最大及乘/除的中间量纲，只有最终量纲与目标一致才生成折叠候选；新增长度×时间×无量纲到复合量纲的回归。仍不支持任意单位代数或隐式单位发现。

R2.1 类型化符号回归执行增量（2026-09-10）：新增 `core/typed_symbolic_regression.py`，`llm_sr` 现在可在提交 JSON 表达式树、参数边界和有限数据时调用本地有界数值拟合，并保留时间留出 RMSE、参数和复杂度；源码字符串仍返回 `proposal_only`。新增运行时安全/准确回归与文档。该切片不等于 LLM-SR 论文完整实现、符号证明或真实题泛化。

R2.1 候选池执行增量（2026-09-10）：类型化符号回归支持最多 8 个候选共享同一留出划分，输出逐候选失败原因、RMSE、复杂度和排序 ID；状态为 `candidates_need_confirmation`，不会把最佳候选直接当作最终答案。仍未接入外部 LLM 生成质量评估或独立最终测试。

R2.1 符号树量纲门（2026-09-10）：类型化表达式树增加可选变量/参数/目标量纲契约，拟合前验证加减同量纲、乘除传播、平方根和超越函数无量纲条件；缺失量纲保持 `not_assessed`。新增非法加法回归，未宣称符号等价证明。
R2.1 基底契约增量（2026-09-11）：类型化符号回归增加可选 `allowed_operators`/`allowed_variables` 白名单，结果记录实际使用的基底；该约束只收窄搜索空间，不代表方程发现或符号证明完成。
R2.1 有界搜索增量（2026-09-11）：新增共享时间留出、常数邻域变异和候选哈希谱系搜索；可由 `llm_sr` 的 `search` 配置调用，仍是本地有限基线，不冒充 LLM-SR 官方算法。
R3 组合路由审计增量（2026-09-11）：监督模型选择新增可审计槽位原因和结构模型保留理由，并写入建模结果；仍需结构诊断驱动的完整模型族比较。
R8 假设计划接口增量（2026-09-11）：新增 `/api/research/hypothesis-plan`，校验有限控件并返回下游局部重算计划；接口仅计划，不执行求解或发布判决。
R7 Python 写入配额增量（2026-09-11）：worker 权限审计增加不跟随符号链接的有界目录用量扫描，并在达到 `max_write_bytes` 时拒绝新的写入打开；仍不宣称 OS 级文件/网络隔离。
R4 模型族适配契约增量（2026-09-11）：新增 `core.model_family_adapters`，统一 compile/evaluate/diagnose/patch/replay 的 CEGIS 接入和资源失败分类；尚未替代各模型族的具体数学适配与独立验收。
R3 诊断路由增量（2026-09-11）：监督模型组合读取训练段重复实体、共线性和目标分段提示，必要时保留 Hierarchical/Piecewise/PLS 槽位，并将每个模型的入选/排除原因写入结果；尚未完成所有模型族的诊断适配。
R4 算术候选闭环增量（2026-09-11）：新增 `/api/research/structure-candidate/cegis`，在显式 JSON 案例上运行有限常数变异、反例记录和谱系；ODE/优化/PDE/多表适配器仍需分别实现。
R5 外部公开题源目录增量（2026-09-11）：根据 COMAP 官方题目索引和公开研究基准补充 `data/benchmarks/comap_public_catalog.json` 与 `source_registry.json`，登记 2020–2025 题源及 Mamo/BWOR/EngiBench/MM-Bench；不下载题面、不把公开参考解或论文分数当作独立金标准。

R2.2 前向滚动增量（2026-09-11）：一维 PDE 发现结果现在附带受限显式前向滚动、留出轨迹 RMSE、最大误差和 CFL 风险；边界固定为初始边界，非有限/爆炸状态拒绝。该能力只覆盖规则一维有限差分候选，不是通用 PDE 数值求解器。

R2.2 粗网格复核增量（2026-09-11）：PDE 候选新增空间步长为 2 的粗网格残差复核与粗/细网格 RMSE 比；网格过小返回 `not_assessed`，结果仅支持离散敏感性诊断，不构成网格收敛或连续方程证明。

R2.2 稀疏项选择增量（2026-09-11）：`pde_find` 增加可选稀疏阈值和一次受限重拟合，输出 `active_terms`、完整系数和阈值；默认阈值为零以保持兼容。该功能只提供有限候选库内的项筛选，不宣称唯一 PDE 或任意方程发现。

回归复核（2026-09-11）：加入 PDE 粗网格、前向滚动、稀疏项选择和类型化符号回归池后的 `python scripts/quick_regression.py` 为 `511 passed`，保留一个可选 PyTorch 第三方弃用警告；该结果是代码契约稳定性证据，不是模型正确率或泛化证明。

R2.3 状态域与 RHS 量纲门（2026-09-11）：神经 UDE 支持可选逐特征域边界及目标/已知 RHS 量纲一致性检查；域外样本或不一致量纲在训练前拒绝，未声明仍保持 `not_assessed`。这不是完整状态空间证明或物理守恒约束。

Web 输入契约同步（2026-09-10）：`/api/research/data-graph-search` 的错误提示与接口说明同步到最多四列特征，避免前端/调用者仍被旧的“两列上限”文字误导。

路线图历史描述校正（2026-09-10）：早期 R1 日志中的“一到两列、超过两列拒绝”是当时的历史状态，不代表当前实现；当前契约以最新的四列数据桥和量纲推导条目为准，历史日志保留用于审计时间线。

R5 外部评分渠道补充（2026-09-11）：登记 IMMC 官方评审流程和公开评审指导材料到 `data/benchmarks/source_registry.json`，确认可向赛事方申请由三位教授独立评审的封存题/评分合作；公开页面和指导 PDF 仅作为量规线索，不被当作本项目已有金标准或独立分数。

R5 公开标签隔离回退（2026-09-11）：无法取得封存题和独立专家评分时，运行 `run_external_data_benchmark.py` 生成公开数据标签隔离报告 `public_holdout_fallback.json`；固定源/切分哈希，加入泄漏护栏和配对置换检验。当前 5/5 案例完成、相对 RMSE 变化 `-0.2045`、`p=0.125`，明确标记为 descriptive_public_labels，不能升级为真实未见题准确率或显著性。

R5 独立评分器进程增量（2026-09-11）：新增 `core/independent_holdout.py`。外部数据基准现在默认由单独 Python 进程读取临时真值/预测向量并计算 MAE、RMSE、R²，建模器不再同时声明测试分数；输出记录评分器版本和数据/预测摘要。该进程隔离不是 OS 级沙箱，公开标签仍不能构成真实未见题或专家评分。

R4 ODE 适配器增量（2026-09-11）：新增 `core/ode_cegis.py`，将低维自治多项式 RHS 候选接入统一 compile/evaluate/diagnose/patch/replay CEGIS。候选经 `solve_ivp` 积分，在显式观测轨迹上生成有限反例并做单系数变异；资源/积分失败保持 `not_assessed`。该实现关闭了“ODE 只有协议没有可执行适配”的一小段，不代表通用 ODE/PDE 或机理证明完成。

R8 滑块预览执行增量（2026-09-11）：`core.hypothesis_controls.apply_hypothesis_controls` 与 `POST /api/research/hypothesis-preview` 现在会重新校验控件、绑定显式参数、生成参数版本/digest 和受影响子图，并在提供类型化代数图时调用 `execute_primitive_graph` 实际重算。新增 Web/控件回归；该入口仍是有界 what-if 预览，不替代完整求解、反例复核或独立确认。

R4 线性规划适配器增量（2026-09-11）：新增 `core.optimization_cegis`，复用 `linear_program/v1` 的单位/边界/约束验证和 HiGHS 求解器，支持有限场景扰动、目标偏差反例、目标系数小步变异及历史重放；求解/资源失败保持 `not_assessed`。新增 2 项适配器回归和模块目录登记；该实现不代表整数/非线性/多目标优化已统一完成。

Web 执行桥（2026-09-11）：新增 `POST /api/research/optimization-cegis`，将上述线性规划适配器暴露为有界 JSON 接口，服务端限制案例数量、CEGIS 预算和容差，拒绝任意代码/路径输入；新增接口回归。

R8 前端预览闭环增量（2026-09-11）：`web/static/js/app.js` 保存研究结果中的可执行预览契约，滑块输入经过 180ms 防抖后调用 `/api/research/hypothesis-preview`；旧请求使用 `AbortController` 取消，响应用 revision 丢弃过期结果，服务端输出仅展示为探索结果。若当前研究结果没有显式绑定图，则保留“仅计划”提示，不伪造执行结果。Node 语法检查通过；完整浏览器拖动/取消/双会话验收仍未完成。

研究响应契约同步（2026-09-11）：研究请求显式提供 `hypothesis_controls.bindings` 与类型化 `graph` 时，服务端返回独立的 `hypothesis_preview_contract`；未提供时不自动猜测绑定，前端保持计划态。该设计把可执行预览和展示控件分离，避免把题意滑块误当作已验证模型参数。

R2.2 二维 PDE 执行增量（2026-09-11）：`discover_2d_pde` 现在在规则二维时空网格上实际拟合 `u/u_x/u_y/u_xx/u_yy` 候选，使用时间留出、有限反例和显式 Euler 前向轨迹，并报告扩散 CFL 风险；`pde_find` 可通过 `x_coordinates/y_coordinates` 路由到该执行器。仍不支持任意混合导数、非规则网格或边界物理自动推断。

R2.3 UDE 闭环增量（2026-09-11）：新增 `simulate_neural_ude_linear`，将逐状态神经残差接入线性已知 RHS 的 RK4 闭环轨迹，增加状态域、有限性和幅值护栏；这是真实轨迹执行，不等于任意 UDE、联合物理参数辨识或跨初值证明。

R2.4 GNN 深度增量（2026-09-11）：轻量 GNN 交互筛查支持 1–3 层消息传递、重启稳定性和验证误差，Web 接口增加 `message_layers`；结果仍明确是静态表格的预测性交互，不把边门解释为因果结构或动态图发现。

R7 OS 隔离失败关闭增量（2026-09-11）：新增 `core.os_sandbox` 探测 Docker/Podman/unshare、生成无网络/只读根/PID/内存/CPU 限制命令，并要求外部 supervisor 设置 `MATHMODEL_OS_SANDBOX_ATTESTED=1`；`SolverLimits(isolation_mode="strict_os")` 无证明时直接拒绝，不回退 Python worker。部署实机隔离和 Windows/Linux 平台验收仍属于环境前置条件。

R4 优化族扩展（2026-09-11）：`core.optimization_cegis` 增加 `mixed_integer_linear_program/v1` 与 `quadratic_program/v1` 适配，统一合同验证、场景扰动、目标偏差反例、目标/线性项变异和重放；Web 优化入口按 candidate.kind 路由。新增 MILP/QP 回归，仍不覆盖非凸、多目标和现实约束自动建模。

R4 优化证据门增量（2026-09-14）：优化 CEGIS 评估现在同时记录求解器约束违反量、整数性违反量和收敛状态；违反可行性或证书失败会进入候选失败记录，不再仅凭目标值判定通过。新增伪造高违反量回归，优化/模型族定向测试 `5 passed`；这仍不等于现实约束完整、非凸全局最优或多目标优化已完成。

本轮完整回归（2026-09-14）：`pytest -q --ignore=tests/test_os_sandbox_integration.py` 为 `2543 passed, 5 skipped, 204 warnings, 8 subtests passed`，耗时约 1623 秒。警告来自依赖版本、可选模型收敛、字体和可视化弃用提示；没有新增失败。该结果证明代码回归通过，不等于 C02/C03/C05/C06 的所有开放世界、独立评分或 OS 隔离验收项已经完成。

R2.4 GNN 切分安全增量（2026-09-11）：交互筛查增加 `group_column` 实体组留出和 `time_column` 时间尾部留出，记录 `split_policy`，并在训练前排除元数据列；随机行划分仅在未声明实体/时间契约时使用。该增量降低近邻污染风险，但仍不构成动态图/因果发现。

R7 隔离证明增量（2026-09-11）：严格 OS worker 不再只接受可伪造的环境变量；`core.os_sandbox` 现在要求部署层提供受保护的 `mathmodel.os-attestation/v1` JSON，逐项匹配 runtime/image，并声明禁网、只读根、非 root 与 PID 配额后才生成容器命令。缺少证明文件仍 fail-closed；这强化了执行准入，但不替代部署环境对证明文件、镜像摘要和宿主权限的保护与实机验收。

R2.2 边界执行增量（2026-09-11）：一维和二维规则网格 PDE 发现不再把 `boundary_values` 仅作为声明；滚动诊断逐时应用已声明边界，并输出 `boundary_condition_rmse`，边界误差进入候选准入，二维还检查边界角点冲突。不规则边界和独立通用 PDE 求解器仍未完成。

R4 外部方法族 CEGIS 增量（2026-09-11）：新增 `core.external_method_cegis` 与 `POST /api/research/method-cegis`，将类型化 PDE/UDE/神经 UDE/LLM-SR 运行器接入统一编译、阈值评估、反例、受限变异和重放流程；案例必须提供独立指标上限，执行失败保持 `not_assessed`。这关闭了适配器接线缺口的一部分，仍不等于完整 PDE、任意 UDE 或真实未见题泛化。

R4 ODE 结构变异增量（2026-09-11）：ODE CEGIS 不再只调已有系数；在 `constant/linear/quadratic` 有界语言内允许一次加入一个缺失基底并以零系数启动，再由执行反馈调参。新增结构变异回归，仍不覆盖非自治输入、刚性事件 ODE 或任意方程生成。

R2.3 联合神经 UDE 增量（2026-09-11）：新增 `fit_joint_neural_ude` 与 `ude_joint` 运行时方法，在轨迹训练前缀上联合优化线性机理矩阵和神经残差，并从同一初值滚动评估未见后缀；结果保存学习矩阵、网络状态、基线和轨迹留出误差。仍限定小维度 Euler、无外部控制/刚性处理，不等于任意 UDE。

R2.2 网格证据增量（2026-09-11）：二维 PDE 结果新增步长为 2 的粗网格验证误差和 RMSE 比，避免把单一细网格拟合当作离散稳定性证据；该检查仍不是网格收敛定理或守恒证明。

R2.2 三维 PDE 增量（2026-09-11）：新增规则三维 `u/u_x/u_y/u_z/u_xx/u_yy/u_zz` 候选、时间留出、显式滚动与 CFL 报告，并接入 `pde_find` 的 `z_coordinates` 路由；仍不支持混合导数、非规则网格、三维时变边界或任意 PDE。

R2.3 刚性 UDE 增量（2026-09-11）：新增 `simulate_neural_ude_stiff` 与 `ude_stiff` 运行时，使用 BDF/Radau 处理受限线性+神经 RHS，记录容差和函数评估预算；仍不等于自动刚性证明、任意 RHS 或物理守恒验证。

R3 因果结构增量（2026-09-11）：新增 `discover_linear_causal_dag` 与 `/api/research/causal-discovery`，用有界顺序约束线性结构筛查和 bootstrap 稳定性生成部分证据 DAG 合同，可限制后续搜索边；不把观测数据升级为干预因果结论。

R2.4 主流程切分绑定增量（2026-09-11）：研究助手调用 GNN 交互筛查时，自动传入画像中有足够重复实体的 ID 候选和可靠 datetime 列；GNN 将据此执行实体/时间留出并记录 `split_policy`，避免接口支持安全切分但主流程实际仍使用随机行切分。

R1 主研究数据图执行增量（2026-09-11）：增加显式 `enable_graph_search` 开关；当调用方提供 `target` 时，主研究自动从同一数据表选择最多四个数值特征，建立互斥训练/搜索分区并调用受控图搜索 worker，结果写入 `specialized_results.data_graph_search`。未提供目标或无数值特征时拒绝猜测并保留警告；该结果仍是探索候选，不是因果或物理证明。

R1 列序不变性增量（2026-09-14，2026-09-15 更新）：主研究图搜索不再按画像列顺序截取前四列；`stable_target_association_v2` 在独立选择分区计算 Spearman 目标关联与标准化成对交互增益，以列名作单位尺度不敏感的平局键，原始方差只作诊断。选择、图搜索执行和最终留出三块行级分区互斥并保存位置摘要；列重排、单位缩放和纯成对交互回归通过。筛查最多覆盖按名称稳定选取的 32 列，更高阶交互仍可能遗漏。

R4 ODE 机制表达增量（2026-09-14）：`ode_cegis` 在原有常数/线性/平方语言上增加有界 `cross` 跨状态乘积、`time` 显式时间项、受限 `driver:<name>` 同网格外部驱动和阈值重置事件；修正多状态混合基底的逐方程特征归约，避免把 RHS 错误返回为矩阵。新增解析参考轨迹、外部序列、缺失驱动和事件重置测试，ODE/执行运行时定向套件 `138 passed`，ODE 适配器单测 `11 passed`。驱动外推、复杂事件网络、时滞、刚性积分和单位传播仍未完成。

R4/R2 动力学与竞争增量（2026-09-14）：`ode_cegis` 增加受限 method-of-steps 时滞（含非均匀递增网格）、`auto` 局部 Jacobian 刚性路由、`RK45/RK23/DOP853/Radau/BDF/LSODA` 选择、事件优先级/依赖/次数预算、向量原子复位和显式/自动系数单位传播；积分证据记录实际求解器、谱半径估计、函数评估次数、事件和时滞数。新增 `gnn_model_cegis` 并接入 `compete_dynamic_families`，动态编译现支持 ODE、LP/MILP/QP、多表、GNN、PDE/UDE/LLM-SR 受限族及 `external_method` 别名；各族先执行 CEGIS，再按显式 `comparison_group` 进入统一 Pareto/判决门。定向动力学/竞争/编译/GNN 套件 `47 passed`。这些是有限契约的执行证据，不等于任意时滞/刚性/事件网络、GNN 因果发现、完整多表/PDE 或独立未见题泛化。
全量回归复核（2026-09-14）：`pytest -q` 收集并执行 `2557 passed, 7 skipped, 204 warnings, 8 subtests passed`（约 14 分 27 秒）。跳过项均为显式环境/可选隔离条件，不计为通过；警告主要来自第三方库弃用、可选 Torch 与绘图字体。该结果只证明当前代码契约回归通过，不升级为任意题面正确性或外部泛化证据。

回归复核（2026-09-11）：新增滑块预览、ODE 合同异常和线性规划 CEGIS 后，`python scripts/quick_regression.py` 为 `551 passed, 1 warning`；发布审计 `errors=[]`、`publishable=true`。警告仅为测试示例 token 与被忽略封存数据包，不能替代 OS 级隔离或真实未见题独立评分。

最新回归复核（2026-09-11）：二维 PDE 逐时边界执行、二维 `pde_find` 路由和 ODE 有界基底变异加入后，`python scripts/quick_regression.py` 为 `564 passed, 1 warning`；建模助手/Web 定向回归 `96 passed`，PDE/外部运行时/ODE 定向回归 `29 passed`。警告仍为可选 PyTorch 依赖的 pynvml 弃用提示；这些结果证明代码契约未被本轮改动破坏，不等于完整 PDE、任意 UDE 或真实未见题泛化已完成。

最新回归复核（2026-09-11）：加入联合神经 UDE 轨迹后端、二维粗网格证据和相应运行时路由后，`python scripts/quick_regression.py` 为 `566 passed, 1 warning`；定向 UDE/PDE/外部方法回归 `36 passed`。警告仍为可选 PyTorch 依赖的 pynvml 弃用提示。

最新回归复核（2026-09-11）：三维规则网格 PDE 及 `z_coordinates` 运行时路由加入后，`python scripts/quick_regression.py` 为 `568 passed, 1 warning`；三维 PDE 与外部运行时定向测试通过，编译、JS 语法和差异检查通过。

最新回归复核（2026-09-11）：加入滚动时间窗 GNN、可执行 DAG 结构筛查和 BDF/Radau 刚性 UDE 后，`python scripts/quick_regression.py` 为 `572 passed, 2 warnings`；警告为可选依赖弃用提示，不是测试失败。

R5 外部验证实跑（2026-09-11）：从 UCI 官方页面新增并固定 Airfoil Self-Noise 数据集（1503 行、5 个带物理单位特征、dB 目标；压缩包 SHA-256=`5c7767ba53ad827d3f48ba1eb9434117f4892df8f10bc4c99e118a9e8a7ae07c`），安全下载、解压并接入公开数据评测器。冻结协议 `external-data-v1` 的源码/依赖/预算 digest 为 `45bd59f9c14d8ea71193aa5044b4344a87d4d550ef26d2ecb05615cba094bdb6`。固定种子 `20260910` 下 6/6 案例完成，独立评分器进程成功；ExtraTrees 相对 Ridge 的任务级平均 RMSE 变化为 `-0.263771`，bootstrap 95% CI=`[-0.431034,-0.098282]`，精确双侧符号置换 `p=0.0625`。该结果是可复现的公开外部证据，未升级为真实未见题准确率或统计显著改进；Air Quality 的退化结果被保留为反例。复现命令和边界见 `docs/external_validation_20260911.md`。

R5 外部验证切分敏感性复核（2026-09-11）：使用第二个预注册种子 `20260911` 重跑全部 6 个公开案例，6/6 完成，平均相对 RMSE=`-0.264882`，精确双侧符号置换 `p=0.0625`；该重复仅用于同一公开数据协议的稳定性检查，不增加真实未见题的独立样本分母。

R5 封存题/外部评审渠道查证（2026-09-11）：核对 IMMC、IM²C、Modeling the Future Challenge 和 MathWorks M3 Challenge 官方页面，记录了申请/合作入口、资格限制、盲评或三教授独立评审流程及保密边界，更新 `docs/external_holdout_outreach.md`。当前尚未发送外部邮件、尚未取得封存题或专家评分；因此真实未见题统计门仍保持 `not_assessed`，不得用公开题解、奖项或公开数据分数替代。

R5 评测冻结增量（2026-09-11）：新增 `core/evaluation_freeze.py` 与 `scripts/create_evaluation_freeze.py`，对源码/脚本/Web/公开配置计算内容哈希，记录依赖版本、预算、方法和随机种子，并提供变更复核；自动排除 `data/`、`workspace/` 和题面正文。该冻结清单确保盲评条件一致，不把运行条件一致性升级为模型正确性或专家评分。

回归复核（2026-09-11）：加入评测冻结模块、CLI 和数据/题面排除护栏后，`python scripts/quick_regression.py` 为 `575 passed, 2 warnings`；冻结实例 digest=`24ecde9fdf20bc3def69e8fdc2c60f05737fcf2dd6bbcb6dcf449fb6455d24c5` 且复核状态 `verified`。警告仍为可选依赖弃用提示。

当前系统对照与统一动态编译（2026-09-11）：新增 `core.system_benchmark`，在
同一冻结训练/测试切分上真正运行 Ridge 基线与当前 `ModelingEngine`（预处理、
交叉验证、结构模型组合）；公开 UCI 6 个案例全部完成，当前系统平均相对
RMSE 变化 `-0.237916`，逐题结果和失败分母写入
`data/reports/external_data_system_comparison.json`。该结果是公开标签描述性
证据，不能升级为真实未见题准确率或专家评分。新增
`core.dynamic_model_compiler` 与 `POST /api/research/dynamic-compile`，统一
路由代数图、ODE CEGIS、LP/MILP/QP CEGIS 和外部方法合同；新增
`core.execution_entry_audit` 盘点直接执行入口；新增
`assess_four_layer_uncertainty` 输出条件情景方差分解及 bootstrap 区间。非规则
PDE、完整动态图因果发现、部署实机 OS 隔离和独立四层校准仍保留为未完成，
避免把接口/公开数据运行误报为论文级证明。
冻结哈希已在加入上述代码后刷新为
`d87704112e75060d05f9b517d494debac8faf99561579c66c3316a9e026c287d`；配对结果
同时保存于 `examples/system_comparison_20260911.json`。

### R7 容器执行链代码修复（2026-09-12）

- [x] 修复镜像前后参数顺序及交互式 stdin；以 `create → start/attach` 驱动真实 worker 命令，不再仅生成配置建议。
- [x] 严格模式使用不可变镜像 ID/digest、明确非 root 用户、无宿主挂载、受限 tmpfs、禁网及内存/CPU/PID 限制；拒绝声明额外 VOLUME 的镜像。
- [x] Docker CLI 上下文与容器环境分离；容器清空继承环境，Windows Job 不再误套到 Docker 客户端。
- [x] 创建阶段响应取消，成功/超时/异常均按唯一名称回收；创建中断后的未知状态和回收失败不冒充成功。同一进程在回收未确认后暂停新严格任务，避免持续重试堆积容器。
- [x] 增加专用数值 worker Dockerfile、白名单构建上下文、v2 配置迁移文档及可选真实容器冒烟测试。
- [ ] 专用镜像实际构建及 Docker/Podman 部署隔离验收：当前工具环境无法连接 Docker 服务，真实容器测试未启用；不计为通过。
- [ ] 宿主/服务强杀后的孤儿容器治理、全训练后端容器化、跨主机资源调度及 Windows 原生受限账户验收。以上仍是独立未完成项，不由本次执行链修复替代。

实现与迁移见 [OS 隔离说明](docs/os_sandbox.md)。本轮没有刷新旧实验冻结摘要，
旧实验不代表修改后代码的评测结果。

本轮代码回归：快速套件 `607 passed, 2 warnings`；追加回收异常暂停机制后，
最终隔离/求解器/权限定向套件 `114 passed, 2 skipped`，两项跳过均为显式关闭的
真实容器冒烟测试。动态编译与候选执行关联测试 `9 passed`；修改文件编译与空白检查通过。
警告为第三方 `pynvml` 和 `DataFrame.swapaxes` 弃用提示，不是数值正确性或部署验收证据。

R4/R2 回归复核（2026-09-14）：最终代码快速套件 `608 passed, 2 warnings`；动力学、
动态模型竞争、编译入口和优化定向套件 `46 passed`，修改后的模型族快照又通过
`40 passed`；完整本地套件收集 `2555` 项，前一轮完整回归无失败（5 项显式跳过的
OS/容器或环境测试）。新增结果证明代码链路和契约一致性，不等于任意时滞/刚性系统、
完整复杂事件网络或真实未见题泛化已被证明。

主研究动态竞争接入（2026-09-14）：`MathModelingAssistant.run` 现在在提供
`dynamic_contract` 时自动进入 typed dynamic competition；Web 同步、异步线程和
跨进程研究服务统一透传合同与 `enable_dynamic_competition` 选项，并把已执行的
竞争结果计入主任务 readiness。新增主入口回归测试；全量回归 `2560 passed,
7 skipped`。合同缺失或字段不合法仍明确返回 `not_assessed`，系统不会从题面猜造
可执行案例。该接入消除了“动态能力只在专用 API 可达”的主流程缺口，但不宣称
任意题面、完整 PDE/GNN/UDE、OS 实机隔离或外部盲评已经完成。

题面到竞争桥接（2026-09-14）：`problem_statement` 动态编译合同新增可选
`dynamic_contract`，在保留自然语言仅作语义上下文的前提下，自动调用同一
`dynamic_competition` 编译/执行/比较链，并将结果写入 `contract_executions` 与
`dynamic_competition`。没有 typed 合同时仍返回 `needs_input`，拒绝把候选提议
冒充数值答案；新增题面路由回归通过。

网页动态入口（2026-09-14）：首页研究助手新增可折叠的 typed 动态合同编辑区，
前端先验证 JSON 对象再提交，后端沿用同步/异步/跨进程统一路由；空白合同不改变
普通研究流程。该入口让 ODE、优化、多表、GNN、PDE/UDE 竞争可从网页触发，但不
把手写合同等同于“任意题面自动建模”。

动态证据上下文增量（2026-09-14）：动态竞争合同现在可显式透传四层不确定性、
假设和下一步问题到各比较组的判决书；缺失层仍保持 `not_assessed`，不会把模型
分数转成概率。相关主研究/动态编译/Web 回归共 `118 passed`。

时滞交互筛查增量（2026-09-14）：`core.causal_dag` 新增有界
`discover_temporal_causal_graph`，对时间序列执行自回归基线、来源滞后项、时间尾部
留出和块 bootstrap 稳定性筛查；`/api/research/causal-discovery` 通过 `time_lags`
选择该分支。新增时滞恢复与 Web 回归。该结果可为动态图/GNN 搜索提供候选边，但仍
明确是预测性结构证据，不是干预因果结论、全局 DAG 或机制证明。

Linux 沙箱工作流修复（2026-09-14）：修正 `linux-sandbox.yml` 中将依赖包误作为
`pip -r` 参数的折叠命令，改为逐步安装并保持与完整 CI 一致；工作流 YAML 已通过
解析检查。真实 Docker 镜像与宿主隔离仍需 GitHub Actions 实机运行证据。

动态滑块合同透传（2026-09-14）：当研究请求同时提供显式滑块绑定、类型化图和
`dynamic_contract` 时，结果预览合同会保存动态合同与路径映射，浏览器拖动后将其
发送到 `/api/research/hypothesis-preview`，服务端深拷贝、按白名单路径应用有限值，
再走 `dynamic_competition` 编译/执行/验证。预览仍是探索态，未提供完整绑定时不伪造
重算；浏览器端 Node 契约测试和动态预览回归通过。

评测路线修订（2026-09-14）：外部盲评从“可发表的最小方案/算法有效性的必要条件”
降级为开放式建模质量的可选补充。新增 `core.automated_benchmark` 与
`docs/automated_benchmark.md`，将生成器、被测系统和评分器严格分离，按
`structure_group` 计算独立性并保留失败/超时分母；支持用解析解、独立求解器、约束/守恒
检查和等价变换完成代数、ODE、优化、多表与错误注入的内部冻结评测。统计显著性改为可选
描述性报告，不预设必须达到 `p<0.05`；公开题、外部评审、另一个模型评分不得与自动真值
指标混成总分。原有封存/外部评审接口保留，只有具备独立参考解和解封证据时才允许报告
`conditional_real_unseen`。

端到端建模证据复核（2026-09-15）：新增 `core.modeling_benchmark_suite`，将公开完整合同的
32 题执行回归与从题意/原始附件开始的 13 结构组建模协议分离。`problem_statement` 新增受限
原始记录归纳入口，可在不接收候选方程、优化矩阵或连接计划时选择代数/动力学/资源优化/多表
模型并求解；重复维表缺少口径时主动返回 `needs_input`。评分器要求模型结构与数值答案同时正确，
优化任务独立重算边界、约束、单位、目标一致性和目标差距。三臂代理实跑中旧类型化路径、当前
受限自动建模、简单工具基线分别为 0/13、13/13、8/13；新增差异来自多输入交互、无关键词动力学
选型、最小成本约束和安全拒答。评分器攻击审计的 13 个合法控制全部通过，47 个无效注入全部拒绝；
攻击目录仍非穷尽。三臂现均通过一次性 worker 执行，开发基准 39 行全部记录父进程墙钟和 Windows
Job 内存强制证据，资源比较资格为 `true`。该结果是共同开发的小型夹具，不是冻结泛化结论；当前状态统一见
`docs/current_capability_evidence.md`。

R5 冻结内部确认（2026-09-15）：新增 `core.modeling_confirmation_suite` 与
`scripts/run_modeling_confirmation.py`。种子 `20260916` 在读取确认案例前冻结源码、依赖、三臂和预算，
运行后源码复核仍为 `verified`。39 个参数/表示变体覆盖 13 个既有结构组，旧类型化路径、当前系统、
简单工具分别为 0/39、30/39、14/39；当前系统 9 个失败全部是乱序 ODE 时间记录被拒绝。结果未用于
冻结运行期间的回调。随后该集合降级为开发反例，当前入口加入时间稳定排序并接入进程级资源监督；
因此 v1 确认已消费且不能授予当前版本确认状态。该证据验证既有结构内的有限稳健性，不是未知结构或外部领域泛化。

R5 冻结新结构挑战（2026-09-15）：种子 `20260917` 在案例生成前被原子消费，冻结当前修复版后首次
运行 8 个与开发集互斥的结构：立方/三阶交互、外部驱动/耦合 ODE、整数/等式优化、三表链/桥表歧义。
旧路径、当前系统和简单工具均为 0/8；当前系统失败为 5 个结构错误、2 个模型缺失、1 个歧义口径不完整。
三臂全部任务均有父进程墙钟与 Windows Job 内存证据，冻结前后源码一致。该负结果明确否定当前系统的
未知结构泛化能力；后续不得针对同一 8 题修改后重跑并升级结论。

R5 挑战后结构扩展（2026-09-15）：不重跑已消费挑战，另建不同参数开发集，新增三次/三阶交互、
外部驱动/耦合 ODE、整数/等式优化及三表链/桥表歧义。开发三臂为 0/8、8/8、0/8；关闭任一族级扩展
均使新版降至 6/8，整数评分器能拒绝更优但非整数的连续松弛伪解。之后种子 `20260919` 在 16 个新参数/
表示案例生成前消费并冻结，三臂为 0/16、16/16、0/16，8 个结构组全部成功且资源监督完整。该结果只
确认明确新增的八类结构，不升级为任意未知结构泛化。

自动基准任务集增量（2026-09-14）：新增 `core.automated_benchmark_suite`，默认生成
32 个可判分任务（代数、ODE、优化、多表各 8 个），使用 7 个结构组约束独立样本计数；
评分器分别检查标量误差、留出轨迹 RMSE、优化目标值以及多表行数/分组聚合。任务输入与
隐藏参考严格分离，新增套件回归；这是一套可复现的开发/确认起点，不是现实竞赛题准确率。

结构修复与可辨识性增量（2026-09-14）：算术图 CEGIS 增加可选二元算子变异，默认仍
优先常数局部修复以避免搜索分支爆炸，API 可用 `allow_structural=true` 显式开启；ODE
轨迹评价对不超过四个系数附加局部 Jacobian 秩/条件数诊断。两者都保留失败分母，
不把局部诊断或有限反例通过称为数学证明。

动态合同自动滑块（2026-09-14）：研究请求只要提供 typed `dynamic_contract`，主研究
结果即可自动从 ODE/优化候选的系数类字段生成有限 what-if 控件、绑定和受限 JSON 路径，
无需手写 `hypothesis_controls`。服务端仍深拷贝合同、限制 24 个控件、拒绝时间/观测/
边界字段并重新进入动态竞争；新增生成器单测。该项打通了动态候选到网页滑块的默认入口，
但仍是探索预览，不等于所有后端都已具备实时全精度重算或最终判决能力。

GNN 参数控件增量（2026-09-14）：同一自动发现器现在支持 GNN 候选的有界训练参数
（`epochs`、`hidden_dim`、消息层数、边阈值等），整数控件保持整数并按后端范围裁剪；
数据行、目标列、时间/分组列和图观测仍不可由滑块覆盖。该项只改善统一交互入口，GNN
结果仍是预测性结构证据，不升级为动态图因果证明。

自动基准可执行基线（2026-09-14）：新增 `typed_execution_baseline` 与
`run_typed_execution_benchmark`，将默认 32 个任务实际送入 primitive graph、
`scipy.solve_ivp`、已验证 LP 和多表 CEGIS 后端；新增
`scripts/run_automated_benchmark_suite.py`，默认报告写入
`artifacts/automated-benchmark/latest.json`。本地运行结果为 32/32 完成、32/32
通过。该证据只证明固定类型化合同的执行链、独立评分器和失败分母可运行，不代表
自然语言任意题面、真实未见题准确率或完整动态建模能力。

自动基准汇总（2026-09-14）：`run_automated_benchmark` 现在同时输出按 family 与
`structure_group` 的完成数、有效数、失败/超时分母、误差均值/中位数和耗时，避免只看
一个总 `valid_rate` 掩盖某类结构退化。该汇总仍是描述性诊断，不把参数变体当独立题目。

结构留出切片（2026-09-15）：新增 `core.benchmark_holdout.split_structure_holdout`，
按 `structure_group` 原子切分 development/confirmation/final，默认保留最后两个结构组
作为确认与最终分区；新增重叠、未知组和全覆盖测试。该工具防止参数变体跨分区泄漏，
不把合成结构组升级为真实未见题证明。

回归复核（2026-09-15）：将自动基准协议、32-case 类型化套件和结构留出测试加入
`scripts/quick_regression.py`；快速套件 `620 passed, 2 warnings`。警告仍为第三方弃用
提示，不是测试失败，也不构成外部泛化或统计显著性证据。
