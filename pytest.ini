[pytest]
# Keep test discovery inside the repository's own suite.  `third_party/` is a
# local, ignored vendor directory and may contain incomplete upstream test
# suites whose optional dependencies are deliberately not installed.
testpaths = tests
python_files = test_*.py
